Locations settings isn't getting respected, trying to navigate me to Harmony island which I have toggled off. Also can't skip this step for some reason, when you're on a navigation step like this and the player click skip step, we should skip that location entirely until the next round
> RESOLVED (both halves).
> - The Locations toggles were display-only by design (they hid rows but AvailabilityProfile,
>   which runs are built from, never saw them — and hiding a location also hid the per-patch
>   switches that routing DID obey, so there was no visible way to stop the trip). The toggle
>   now feeds AvailabilityProfile as a location filter: a hidden location is excluded from
>   routing, the loadout and the counts. See Locations.java / AvailabilityProfile.isAvailable.
> - Travel legs had no step to skip (skip acted on GuideStep, travel is a TravelHint). The
>   Skip button now stays enabled while travelling and calls skipTravelDestination(), which
>   drops that whole region from the run via RunPlanner.skipRegion. Cleared on run stop/start,
>   so the location comes back next round. Tests added.

after planting a tree (contract), I am not prompted to pay for payment even though I have it, the payment NPC is not highlighted at all, and neither are the text line items to accept/pay in the chat dialog (if you can use payment on the npc to pay, idk if you can, but that would be cool too, research that)
> RESOLVED. Three stacked bugs:
> 1. The pay step was structurally unreachable: the protection question was asked of the seed
>    ALLOCATION, which is null for any occupied patch, so protecting=false for every crop in
>    the ground. Now derived from the crop actually planted (Seed.forProduce), and suppressed
>    once the farmer has been paid (snapshot.isPatchProtected).
> 2. The step required UNNOTED payment in the pack (carried.getCount); payments travel noted.
>    Now getCountIncludingNoted, matching the loadout. Inventory highlight also matches the
>    noted form now.
> 3. A just-planted patch stopped being "actionable", so the stop completed under your feet
>    and the guide walked off before the payment. RunPlanner.isActionable now keeps a growing,
>    protectable, protection-selected, unpaid patch alive.
> Plus: the farmer's chat dialogue options ("Pay (north patch)" / patch lines / "Yes") are now
> outlined while the PAY_FARMER step is current (GuideInventoryOverlay.highlightPayOptions).
> The NPC outline itself already worked — it just never had a step to fire on.
> Research: you canNOT use-item the payment on the gardener; the fast path is the right-click
> "Pay" option on the NPC (wiki: gardeners have Talk-to / Pay / Trade), which the highlighted
> NPC + dialogue rows now walk you through. Noted payments are accepted.

roots can be noted by the way, from tree dig ups, I don't know if we flag on that today, the tree patch I just harvested only returned 1 root, so our logic might skip it since a noted version takes up the same amount of inventory space when its only 1x
> RESOLVED. There was no quantity threshold — roots simply weren't modelled at all: Produce
> (generated from RuneLite core) carries only the logs for tree crops, so Produce.getByItemID
> returned null for roots and the noting logic never saw them. New hand-written
> data/NotableHarvests table (oak/willow/maple/yew/magic roots); the note-before-leaving step
> and the bank deposit highlight now include them. Even a 1x root gets the note step: noted
> roots stack across the run's later tree patches, loose ones don't.
> FOLLOW-UP from review: grimy herbs had the same hole — Produce's herb rows carry the CLEAN
> herb ids, harvests are grimy — so loose grimy herbs (sack full, closed, or not owned) never
> triggered the note step either. Same table now covers all fifteen grimy farm herbs; no sack
> check needed, since an open sack swallows herbs before they reach the pack and loose ones
> already mean the sack can't take them.

can we do menu item swapping on fill/empty on the seedbox so the user only has to left click?
> RESOLVED. New guide/GuideMenuSwap (born SeedBoxSwap): while the current step is "Fill the seed box" or "Empty
> your seed box", that option becomes the box's left-click (entries reordered on PostMenuSort,
> never renamed — SeedCapture matches the option strings for seed accounting). Step-driven, so
> Fill and Empty each win at the right moment; behind a new "Left-click the seed box step"
> toggle (on by default). This was parked in docs/TODO.md pending a compliance decision; it's
> the same side of the line as core's Menu Entry Swapper (reorders, never clicks).

"via Teleport to house via Catherby Portal" - don't have a catherby portal, is that a SP config issue I have? or a bug we have, the nexus should highlight here and the catherby teleport
> RESOLVED (our half) / SP data-model limitation (the wording).
> - The string is Shortest Path's own displayInfo, passed through verbatim. SP's POH portal
>   data (teleportation_portals_poh.tsv) has no per-portal requirements and NO nexus entries
>   at all — with "POH teleportation portals" on, SP assumes every portal room exists and
>   words the hop as the portal. Keep the setting on: the route through the house is still
>   right for a nexus owner, SP just names furniture it imagines.
> - Our nexus highlight was already correct (furniture matched by what's actually built in
>   your house + destination, so "Catherby" lights the Portal Nexus and its Catherby row).
>   The real bug on our side: while you were ALREADY inside the house, the route still
>   resolved to the house tablet/spell and kept it highlighted, competing with the nexus.
>   travelHint now suppresses house-teleport hints when house.isInside().

I think I found a bit of a bug in SP, the checkbox POH mounted items assumes you have all of them, I had that checked so digsite pendant would highlight, but now for hosidius its trying to take me via mounted xerics, which I don't have. Probably need to open a PR to fix that one.
> CONFIRMED upstream, nothing to fix in this repo. Skretzo/shortest-path
> PathfinderConfig.checkJewelleryBoxTier: one boolean (usePohMountedItems) covers mounted
> Glory, Xeric's Talisman, Digsite pendant AND Mythical cape together, keyed off objectInfo
> strings in teleportation_boxes.tsv. A PR would split it into per-item settings (the varbits
> for which mount is built aren't exposed outside the house, so per-item checkboxes like the
> jewellery box tier dropdown is the practical shape). Interim workaround: leave it unchecked
> and accept losing the digsite mount hint, or keep it and ignore the xeric's leg.



guild master jane note getting highlighted on step to hand in contract for reward, menu swap to "contract" highlight the conversation text to click too
> RESOLVED (all three parts).
> - Jane not outlined: she is THREE NPC ids - 8586/8587 standing in the guild, 8628 as her
>   dialogue chathead - and the hand-in step carried the chathead id while the overlay
>   compared it against world NPCs by exact id, so nobody ever matched and nothing at all was
>   highlighted. highlightNpcById now matches through the Farmers alias table (which already
>   grouped all three); ContractCapture's chathead gate also widened to the id set and given
>   the NPC_CHATHEAD model-type guard ProtectionCapture already had.
> - Menu swap: GuideMenuSwap generalised from item-only to any menu entry; while a
>   HAND_IN_CONTRACT or TAKE_CONTRACT step is current, Jane's "Contract" option becomes her
>   left-click. New toggle "Left-click Jane's Contract option" (on by default). Option string
>   matched case-insensitively since the exact wording isn't pinned - verify in client.
> - Dialog rows: the hand-in conversation's options are now outlined like the pay-farmer
>   dialogue. Denylist (everything except the header and obvious "No/Nothing/I'll come back"
>   declines) because the exact option strings aren't captured anywhere yet - if you note the
>   real wording in client, we can tighten it to a prefix list.

Contracts are getting skipped because seeds are in the bank, we should navigate to get that, but instead it gets unchecked in the run selection
> RESOLVED. The banked seed was innocent - two real causes:
> - "It gets unchecked": the Farming Contract tick is stored under a TYPE-scoped key
>   (CACTUS#contract). Every new contract of a new type created a fresh, unticked line, so
>   the box "came back unchecked" no matter how often you ticked it. The stored key now
>   follows the contract (RunTypeStore.retargetContract, wired to contract changes and plugin
>   load) - tick Farming Contract once and it stays ticked across assignments. Test added.
>   Once ticked, the existing supply machinery already creates the bank leg for a banked
>   contract seed.
> - The misleading note: standing in the guild with the seed in the bank, the panel said
>   "not part of this run - tick Farming contract" even when it WAS ticked. It now says the
>   honest thing ("your contract seed is in your bank - withdraw it") when the box is ticked
>   and the seed is owned but not on you.
> - Also: the contract line no longer disappears from the run selection when its patch
>   type's tab is hidden in settings - the contract is a job, not a patch type.

there seems to be a lot of step/state drift from the items I have in my inventory, how can we prevent that from happening?
> RESOLVED (a full pipeline audit; five concrete drift mechanisms found and the big ones fixed).
> - CarriedItems now re-reads the pack AND worn items from the client once a tick, as a
>   backstop, same pattern as LeprechaunStore. Events alone had no way back from one missed
>   read - worn gear can go a whole session without a container event, so a single skipped
>   startup read meant every worn cape/cloak/ring counted as unowned until you changed gear.
>   This was very likely the bulk of "a lot of drift". Runs before GuideTracker in tick
>   order, so steps always see this tick's pack.
> - FarmingBonusStore (cape/secateurs/outfit bonuses) is PERSISTED and was never relearned -
>   take the cape off while the plugin isn't looking and "+5%" survived across restarts.
>   It now relearns from the client at plugin load alongside the other stores.
> - RunPanel's protection budget counted only unnoted payments while the guide's counted
>   noted too, so the sidebar under-allocated while the guide allocated in full - the same
>   noted/unnoted split fixed once before in the loadout. Now getCountIncludingNoted.
> - Seed box: a lagged SEED_BOX container event arriving just after a Fill/Empty could
>   overwrite the correctly-derived contents with the pre-action snapshot; box reads within
>   2 ticks of a box action are now ignored (any later read is trusted as truth).
> - Known remaining (lower priority): taking seeds out of the box one at a time still leaves
>   the derived count high until the box is next observed; carried-side lookups are exact-id
>   while the bank side canonicalises variants (charged jewellery) - flag it if it bites.

"Farm run
Travel to Kourend.
5 stops left
via Camelot Teleport: Seers'
via Xeric's talisman: 2. Xeric's Glade" - okay but when I tele to house I have mounted items turned off in SP, and its not taking me to Camelot, its having me do the kourend castle tele, and then its having me tele back home once i do that, either way none of this is near Camelot. And I don't have a mounted Xerics like I said. Area mismatch of some sort?
> RESOLVED. Settled after discussion (the talisman was in the BANK, not carried): the two
> "via" lines were one coherent SP plan after all - Camelot Teleport to Seers' -> the bank
> beside the landing -> WITHDRAW the banked Xeric's talisman -> rub to Xeric's Glade -> walk
> to the patches. SP models bank pickups: once a path passes a bank, banked teleport items
> become usable hops, and Xeric's Glade is far closer to the Hosidius patches than Kourend
> Castle, so its cost model preferred tele+bank+tele over the carried Kourend Castle spell
> plus the long walk. It was ALLOWED to consider that on a plain travel leg because our
> includeBankPath override was only sent when TRUE - omitted, SP fell back to the user's own
> "include bank path" setting.
> - THE fix: includeBankPath is now sent explicitly both ways, so travel legs can never plan
>   through a bank. Supply legs keep it on, where bank pickups are the whole point.
> - A walk-edge "phantom hop" filter was added under an earlier (wrong) theory and REMOVED
>   by request once the hop turned out to be real: the router's account of its own route is
>   not something to second-guess.
> - Stale route points surviving a clear: ShortestPathIntegration.clear() reset the
>   transports but NOT the destinations, so the travel naming kept reading a dead route's
>   landing points. Fixed.
> - Transports messages are now ignored unless WE have a route outstanding - SP posts one
>   for every path once postTransports is on, including paths you set yourself, and those
>   were displayed as the farm route.
> - Destination naming now reads the hop landings back-to-front (they're kept in path order
>   now): the LAST landing names the trip, so an intermediate hop's town can't.
> - Remaining/upstream: the teleport-home-then-teleport-back flip-flop while STANDING in the
>   POH is SP re-pathing from your instance position every tick - the plugin-message API's
>   "start" isn't sticky the way SP's own setStart is, so our front-door reroute is honoured
>   for exactly one computation. Needs an upstream PR (make plugin-message start set
>   startPointSet, or extend the walk-edge suppression); re-posting from our side every tick
>   would just fight it.
> - Also upstream: the transports message carries no request id, so a LATE answer to a
>   previous target can still be mistaken for the current leg's for one exchange. Gating on
>   the answer's landing points being near our targets is unsound (a legit route can end
>   with a multi-region walk), so fully closing it means a request token in SP's message
>   payload.

"Farm run
Travel to Kourend.
5 stops left
via Camelot Teleport: Seers'
via Xeric's talisman: 2. Xeric's Glade" - okay but when I tele to house I have mounted items turned off in SP, and its not taking me to Camelot, its having me do the kourend castle tele, and then its having me tele back home once i do that, either way none of this is near Camelot. And I don't have a mounted Xerics like I said. Area mismatch of some sort?

2 book of the deads showing up in teleport list, both placeholders, one with charges, one without.
> RESOLVED (with the Ectophial item below - one root cause). The plugin had no item-identity
> canonicalisation anywhere: charged/uncharged variants, full/empty forms and bank
> placeholders are distinct ids, and every bank-path dedup was keyed on the raw id.
> - listedTeleportIds now keeps ONE row per item NAME (carried form preferred), so the book
>   and its placeholder id can never both become teleport rows.
> - RunLoadout.bankFormsOf now expands through RuneLite's ItemVariationMapping, so the
>   filter, highlight, counts and layout all treat every variant as one item.
> Needs an in-client look: if a faded second book still appears, it is Bank Tags' own
> objvar-placeholder handling and we'll take it from there.

Ectophial showing up at the bottom right corner of the bank list for some reason, withdrawing it snaps a second copy up to the top, looking similar to the book of the dead issue.
> RESOLVED. Full (4251) and empty (4252) Ectophial are two ids for one item, and
> BankLayout.pinFirst matched the route item by EXACT id only - the route resolved one form,
> the layout held the other, so the pin "missed", overwrote slot A1 with a second copy, and
> the displaced item fell out of the layout entirely (Bank Tags then re-appended it as
> overflow in the bottom-right corner, which is the wandering item you saw).
> pinFirst now matches any bank form, and when the item genuinely has no slot the displaced
> occupant moves to the first empty slot instead of being dropped. Tests added.

when visiting a bank noted crops are highlighted in yellow, change it to cyan
> RESOLVED. Deposit highlight (BankHighlightOverlay.DEPOSIT_COLOUR) changed from amber to
> cyan, matching the withdraw-count cyan.

2 copies of the notification list icon at the top of left of the screen? one even when the plugin is off? how?
> RESOLVED. Found the smoking gun in client.log: disabling the plugin while the bank filter
> was open threw "AssertionError: must be called on client thread" - RuneLite stops plugins
> on the Swing EDT, and BankFilter.shutDown -> closeBankTag ends in runScript. The error
> aborted DoogleMapsPlugin.shutDown before removeInfoBox ran, orphaning the infobox (it
> renders off config alone, so it survived the plugin being off), and the next enable added
> a second. close() now runs on the client thread, and bankFilter.shutDown moved to the END
> of the plugin's shutDown so UI teardown can never be skipped by it again. The same abort
> also leaked all 4 overlays, 7 store listeners and the sidebar button - all fixed by the
> same change.

stats page jerking around while getting xp in game
> RESOLVED. Feedback loop: every Farming xp drop writes farmingXp to config, the write posts
> ConfigChanged in OUR group, and onConfigChanged treated any group key as a settings change
> -> full refresh() -> HarvestStatsPanel torn down and rebuilt several times per game tick
> (intended cadence: the 20-second idle refresh). onConfigChanged now ignores keys that are
> not actual @ConfigItem settings; state writes rely on their stores' own change listeners,
> which is what they were already wired to.

not highlighting seedbox to fill again after planting
> RESOLVED. "Fill the seed box before moving on" was structurally unreachable: planting the
> last patch completes the stop, the stop leaves getRemaining() that same tick, stopAt()
> goes null and computeStepsHere returned [] - so every before-moving-on errand (fill box,
> note crops, return buckets) silently vanished the moment it should have become current.
> The leaving errands are now re-attached when standing at the just-finished stop
> (workingRegion), same failure shape as the old walked-off-before-payment bug. Also
> plugged: the third seed box id (ItemID.SEEDBOX) was missing from the step generators.

via Varrock Teleport: GE not highlighting in the nexus, exit portal still flashing highlighted for a split second when teleing into POH
> RESOLVED (both halves).
> - The nexus's Varrock teleport redirected to Grand Exchange makes the row SAY "Grand
>   Exchange", which shares no substring with "Varrock". The furniture match knew the pair;
>   the ROW match did not. Added varrock<->grand exchange to PLACE_ALIASES - a real Varrock
>   row still wins via the direct-beats-aliased preference. Test added.
> - The flash had two windows: (1) on the arrival tick, GuideTracker runs before PlayerHouse
>   (event bus orders subscribers by class name), so "inside house" went true while the
>   route was still the pre-teleport one that matches no furniture -> exit-portal fallback
>   for a full tick. PlayerHouse.justEntered() now suppresses the fallback on that tick.
>   (2) the router answers mid-tick while the overlay judged by the once-a-tick snapshot;
>   the overlay now reads the live transports, same as the tracker itself does.

getting a next step of "skipping prifddinas - no seed" but I do have a seed, its in my seedbox and the plugin knows it. But I went out of order and harvested another patch before doing any planting. as soon as I empty the seedbox its fixed again
> RESOLVED. A seed box Fill/Empty click armed a pending action that was cashed in by the
> NEXT inventory event of any kind. An Empty into a full pack moves nothing and fires no
> event, so the armed EMPTY sat there until the out-of-order harvest changed the inventory -
> and then "confirmed" it, silently zeroing the box's tracked contents (the panel kept
> showing them; the allocator read zero, and Prifddinas is late in the key-sorted patch
> order, so it starved first). Emptying for real moved the seeds into the always-live
> inventory count, which is why that fixed it.
> Three fixes in SeedInventoryStore: a pending action expires after 2 ticks (a no-op click
> can never be redeemed by a later unrelated event); Empty is now derived from the container
> delta exactly like Fill (partial empties into a near-full pack are also right now, and
> emptying at a BANK - where the seeds land in the bank, not the pack - is tracked too);
> and a box change fires the change listeners so the panel can never show one thing while
> the allocator reads another. Four tests added.

self-healing state pass: make decisions deterministic/derived so a missed step or event can't kill a run
> DONE (three package-wide audits: guide/, state/+capture/, route/+bank/+lifecycle). Every
> mutable field was traced for "state that can't heal itself". Fixed this pass:
> - GuideTracker's run-scoped state (skips, contract/errand announcements, destination
>   anchor) now clears when the planner goes inactive - reset() only ran on plugin shutdown,
>   so a skipped "pay the farmer" silently planted unprotected on every later run all session.
> - Contract deadlock: the awaiting-hand-in flag could only be cleared by two tick-sampled
>   dialogue lines; spam-clicking past both left a hand-in step for produce that no longer
>   existed and suppressed TAKE_CONTRACT forever (config-persisted, survives restart). Now
>   reconciled against Time Tracking - a new assignment proves the old hand-in happened.
>   Also: the patch-evidence completion fallback now requires a varbit-observed state, not
>   an elapsed time estimate, before it rewrites config.
> - The load() priming block (relearn pack/seeds/bonuses, item names, prices) was a one-shot
>   behind a LOGGED_IN guard with no retry - enable the plugin at a login screen and the
>   whole session ran unprimed. Now a re-queued clientThread.invoke that runs on the first
>   logged-in frame, guaranteed.
> - Session state now resets at the login screen / profile change: protected-herb unlock
>   memo (account B inherited A's memo and never got its own key written - protected tab
>   absent all session), leprechaun stores, house portal answer, payment-patch selection,
>   pending seed-box action, in-memory inventory seed counts.
> - Seed inventory: per-tick reconcile against the live container (same backstop
>   CarriedItems got), and the pending-action tick math is range-checked so a tick-counter
>   reset can't freeze box reads.
> - Payment-patch selection now expires (60s) - a stale selection from one farmer could
>   record another farmer's payment on the wrong patch of a pair. Refusal over guessing.
> - Compost pending window cut 30s -> 15s, and resolution takes the most recent qualifying
>   click instead of an arbitrary one (paired allotments could swap records).
> - Barbarian Farming's permanent "no dibber needed" latch is now covered by profile reset -
>   it was the one observation the reset button couldn't reach.
> - Time Tracking backfill no longer resurrects spent protection/compost: it only fills
>   while a crop is standing in a state the fact could still be true for.
> - RunPlanner: the deferred supply trip is picked up on BOTH completion paths (the polling
>   path dropped it); completion re-arms (a stop that un-completes when you withdraw the
>   missing seed can announce again); run-end needs the emptiness confirmed against a fresh
>   exemption push (a one-tick over-broad push used to end a live run outright).
> - NEW ESCAPE HATCH: Skip step, pressed during the supply leg, now waives the bank leg for
>   the run - previously a payment you'd decided not to make parked the run at the bank with
>   no way past. Needs in-client verification.
> - Caches that served stale answers within a tick now invalidate on the event: the loadout
>   (withdrawing an item un-highlights it the same tick), the route item (a retarget stops
>   pinning the previous leg's teleport).
> - Bank filtering re-probes for Bank Tags (rate-limited) instead of one shot at startup;
>   awaiting-route times out after 10s so an uninstalled Shortest Path can't hold the
>   overlay in "waiting" forever; bank locations are never learned inside instances (the
>   template tile is a void coordinate, and the store is permanent).
> DEFERRED, deliberately (bigger design changes, flag if they bite):
> - RunPlanner.stops is still a plan-time snapshot: a crop that ripens mid-run can't join
>   the run, and won't until the next Start. Fixing means re-deriving stop membership per
>   tick (union, never remove) - doable but touches completion semantics everywhere.
> - The front-door house reroute is still undone by SP re-pathing from the instance
>   position every tick (upstream: plugin-message start isn't sticky). Latching differently
>   on our side would just fight it per tick.
> - GuideTracker's tick handler runs after the plugin's (class-name event ordering), so
>   reviewProgress reads last tick's exemptions - one-tick lag, benign, but the ordering is
>   implicit; an explicit @Subscribe priority would document it (and changing it would also
>   reorder CarriedItems, so not done casually).
> - DroppedProduce can still read a deliberate drop as overflow for ~1 min (needs a
>   harvest-witness: only accept a spawn when the inventory gained produce that tick).
> - RunStop.serviced is a one-way latch whose only consumer is test-only getRemainingPatches;
>   worth deleting both.

Farm run header on the infobox should always have the patch location listed in parenthesis next to it, some instances I've seen where it doesn't
> RESOLVED. The header's location came from stopAt(), which only knows REMAINING stops - the
> moment a stop completed (or was skipped) the name vanished from "Farm run - Falador" while
> the player was still standing there packing up. Same gap that ate the leaving errands. The
> title now names any stop of the run whose region you are standing in; step generation
> keeps its stricter test.

seed box empty, but still highlighted to empty before planting, but the seed I need is already in my inventory and not highlighted. Protected herb patches, ranarr
> RESOLVED (two layers).
> - The phantom: the box's derived count had accumulated seeds that are not in it (the old
>   pre-fix corruption persists in config until the box is next observed). NEW HEAL: an
>   Empty click that moves nothing while the pack has a free slot is now taken as proof the
>   box is empty, and the count is zeroed - so following the phantom step once clears it,
>   instead of the step surviving the very action it asks for. Opening the box also still
>   corrects the count outright.
> - A regression the heal also guards: the per-tick inventory reconcile added earlier could
>   consume a pending Fill/Empty on the click's own tick against an unchanged container (the
>   game applies the move a beat later), so the real change arrived to a store that had
>   forgotten anything was owed - recreating exactly this phantom. The reconcile now stands
>   down during the two-tick pending window.
> - (The plant step with the ranarr highlight was hiding BEHIND the phantom empty step; both
>   fixes above make it surface.)

second seedbox bug, seedbox can only hold 6 seed types at a time, don't step to fill seed box when its already full
> RESOLVED. "Fill the seed box before moving on" gated on loose seeds alone; it now asks
> whether the box can actually TAKE any of them - a loose kind already boxed always fits
> (stacks), a new kind fits only while fewer than six kinds are boxed. A full box with a
> seventh kind loose raises no step, because the game would refuse the fill.

weird bug with routing: at the GE the panel kept saying "Cast Teleport to House / via Respawn Portal (Prifddinas)" until ~10 tiles from the spirit tree; spirit tree not highlighted; "via 6: Prifddinas" unreadable; Farm run title missing location
> RESOLVED (three of four; the first is SP's cost model, explained below).
> - "via 6: Prifddinas" now reads "via Spirit tree - 6: Prifddinas": SP's message carries
>   objectInfo (the thing you click) beside displayInfo (its menu row) and we were
>   discarding the half that names the object. Item/spell hops unchanged.
> - The GE spirit tree (and any world object the route's first hop goes through) is now
>   outlined - the same treatment house furniture gets, extended to the overworld via the
>   objectInfo name and a per-tick scene scan.
> - The tree's "Spirit Tree Locations" sub-menu rows now highlight: the game moved that
>   list to the new Menu interface (MenuNew) and our row scan only covered the old one.
> - Farm run title now falls back to the DESTINATION between stops ("Farm run -
>   Prifddinas"), so the header never goes blank mid-journey.
> - The stale-looking house route at the GE is Shortest Path re-pathing honestly: its cost
>   model kept preferring tele-to-house -> respawn portal -> Prifddinas until you stood
>   close enough to the tree that walking won. Two SP-side things to check: the "Respawn
>   Portal (Prifddinas)" edge is SP imagining POH portals (its POH portal data has no
>   per-portal requirements; our override can only disable the whole category, and only
>   when your house has NO portal-room portals - if you have any portal built but not that
>   one, turn off "POH teleportation portals" in SP's own settings). SP's teleport
>   wait-time cost setting also moves that crossover point.

"Farm run - Prifddinas / Fill the seedbox with your loose seeds before moving on" correctly highlighted and menu-swapped the seedbox, but the north allotment (stage 1 snape grass) was highlighted too - it came back whenever I emptied the box into my inventory. Take out the seedbox instructions and highlighting; keep the menu swap but make it simple with a toggle: box full -> Empty, box partly full and no seeds in inv -> Empty, box empty -> Fill, box partly full and seeds in inv -> Fill.
> RESOLVED, by removal. The stray outline: the fill step was hung on stop.getPatches().get(0)
> so it would sit at the right stop, and GuideStep.highlightsPatch() is true for it - so the
> stop's first patch got lit by an instruction about an inventory item. Rather than exempt the
> action, both box steps are gone (FILL_SEED_BOX and WITHDRAW_SEEDS removed from GuideAction,
> with GuidePlan's empty-before-planting branch and GuideTracker.appendFillSeedBoxBeforeLeaving).
> What is left is GuideMenuSwap, no longer step-driven: while a run is on and seedBoxLeftClick
> is set, the box's left-click is Empty when it holds seeds and nothing loose in the pack would
> fit in it, and Fill otherwise - which is the four cases asked for, with "full" meaning the
> game would refuse the fill (six kinds boxed, seventh kind loose) rather than a count. Reads
> the box, not the guide: GuideMenuSwap.wantedBoxOption, pinned by GuideMenuSwapTest.

"via Teleport Menu Fancy Jewellery Box 37501 - J: Farming Guild" - remove the item id "37501" from these
> RESOLVED. Shortest Path's objectInfo column is "menuOption menuTarget objectID" in its own
> transport TSVs ("Teleport Menu Fancy Jewellery Box 37501", "Travel Spirit tree 37329",
> "Enter Annakarl Portal 29341") and we were prefixing the hop line with it raw. The trailing
> id is now stripped (ShortestPathIntegration.objectName, pinned by HopWordingTest) for both
> the panel wording and the route-object highlight name. Only the trailing number goes - the
> numbers inside a hop's wording, like a mushtree's menu row, are wording.
>
> STILL OPEN, found while doing this: the route-object highlight cannot match. GuideOverlay
> .scanForObjects compares the objectInfo name to a scene object's name with equalsIgnoreCase,
> but objectInfo carries the MENU OPTION too - "Travel Spirit tree" will never equal an object
> named "Spirit tree", id stripped or not. So the GE-spirit-tree highlight logged as resolved
> earlier in this file has probably never lit anything. The fix is a suffix match, and it wants
> deciding on rather than assuming: a plain suffix rule makes "Enter Annakarl Portal" match any
> object named "Portal", and a POH is full of them.
> RESOLVED (that sub-issue). Confirmed still broken on read: firstTransportObject holds the
> id-stripped objectInfo ("Travel Spirit tree"), scanForObjects demanded equalsIgnoreCase
> against a scene name, so the overworld outline had never lit anything - only the panel's
> WORDING was ever fixed. The rule, decided against SP's own data rather than guessed: every
> objectInfo row in all 24 transport TSVs was read, the column is "menuOption menuTarget
> objectID" with no delimiter, and the longest menu option in the data is TWO words ("Teleport
> Menu", "Look out", "Land's End", "The Pandemonium", "Draynor Village"). So a scene object
> matches when its name is a word-boundary SUFFIX of the hop and the prefix is at most two
> words - with two guards the plain suffix rule lacks:
> - a ONE-word name may only follow a ONE-word option. "Enter Annakarl" is itself two words,
>   so the cap alone does not stop "Portal" claiming that hop and lighting every house exit;
>   this does. Genuine bare names ("Climb-up Ladder") are the whole of a two-word hop.
> - among matches, the LONGEST name wins, so "Ape Atoll Dungeon Portal" beats "Dungeon Portal"
>   when both are standing there.
> An option longer than two words simply fails to match, which is where this started - no
> regression possible. Tests: RouteObjectMatchTest, every fixture a real TSV row.

Bushes aren't getting prompted to be cleared after harvesting them
> RESOLVED. A regrowing crop never empties - picked clean it sits HARVESTABLE with a stock of
> zero - and two layers both read that as "finished": GuidePlan fell through to the
> growing-leave-it-alone branch (no dig step), and RunPlanner.isActionable's regrowing
> exception dropped the stop from the run entirely. Both now special-case the families a
> spade digs straight out (new hand-written data/SpadeClearedCrops: BUSH and CACTUS - fruit
> trees stay "finished when stripped" since the chop-and-stump flow isn't modelled):
> - GuidePlan: a stripped bush/cactus with the chosen seed AT HAND gets a spade fetch plus
>   "Dig up the picked-clean X so a new one can be planted." With no seed at hand it stays
>   silent (clearing a producing bush with nothing to replace it is worse than leaving it),
>   and the usual skip-wording/exemption path lets the stop complete.
> - isActionable: the stop stays in the run when a selected seed for the group is OWNED
>   anywhere (bank counts - the supply leg exists to fetch it), so the loadout and pricing
>   pick the bush seeds up too. Tests added in GuidePlanTest and RunPlannerTest.

theres something really wrong with SP line drawing, and it seems specific to our plugin,
don't have this issue when I just use this on the game map, but drawing seems to only occur
like 10-20 tiles away from the patch area, any further than that and I get nothing.
> RESOLVED (our half) + upstream (the start half). Verified against SP's source:
> - Our half: SP only ever finishes a search by stepping ONTO a target tile (there is no
>   "adjacent counts" - Pathfinder terminates on targets.contains(expandedNode)), and we were
>   handing it the patch object's own tile, which is BLOCKED for bushes/trees. Every
>   computation therefore ran to SP's no-progress cutoff (5 ticks = 3s by default) instead of
>   arriving, and every recalculation (player >recalculateDistance from the drawn path, or a
>   re-post of ours) threw the line away and started the grind over - so the line barely
>   existed mid-journey and only stabilised near the patches, where searches are tiny. Manual
>   map targets don't hit this because a map click naturally lands on walkable ground.
>   FIX: PatchLocationStore.getRouteTargets - the router now gets the ring of tiles one step
>   OUTSIDE the patch footprint (plus the centre), so the first walkable tile beside the
>   patch ends the search instantly, which is also where the player wants to stand.
>   PatchLocationCapture now records the object's footprint (sizeX/sizeY) alongside the
>   position; older 3-value entries fall back to an assumed 3x3. Tests added.
> - Upstream half (the "nothing at all" when legs start at the POH): the plugin-message API
>   defaults a missing start from the RAW getLocalPlayer().getWorldLocation() - garbage
>   inside any instance - where SP's own set-target uses fromLocalInstance. And a garbage
>   start never self-heals: isNearPath() short-circuits to "near" when the path is EMPTY, so
>   the dead pathfinder sits there drawing nothing until our next re-post (often the region
>   change right before the patches - hence "only 10-20 tiles out"). Same family as the
>   already-logged non-sticky start. The PR is ~three lines in ShortestPathPlugin
>   .onPluginMessage: use fromLocalInstance for the defaulted start, and set startPointSet
>   when an explicit start arrives (clearing it on the next start-less message).

seedbox not getting filtered into the gear section of bank filtering - its ending at 7 rows,
if I remove an item it shows up
> RESOLVED. The gear region was full (12 slots in the default map), the seed box was the
> thirteenth gear item, and BankLayout deliberately left region overflow to Bank Tags'
> OPTION_ITEMS_NOT_IN_LAYOUT_AT_BOTTOM - which turns out not to keep its promise. Verified in
> core's LayoutManager: the bottom-append starts after the layout's last item and walks the
> bank widget pool, but BREAKS (not skips) at the first child it cannot use (null, or a
> non-item-height widget), so an unplaced item can simply never draw. Freeing a gear slot
> gave the box a real layout slot again, which is why removing an item brought it back.
> FIX: BankLayout.build now spills what a region cannot hold into the map's own empty slots -
> unclaimed dots first, so overflow cannot be mistaken for part of another letter's region,
> then any still-empty region slots. Only a genuinely full 8x8 grid falls through to Bank
> Tags' append. Tests added (13th gear item lands in the D1 gutter; a 70-item run fills all
> 64 slots).

still to collect isn't working right, none of those seeds are in my inv (run-planned line
showed Toadflax/Snapdragon/Snape grass at inv 0, box 0 with still-to-collect []; Avantoe 169,
Ranarr 27, Watermelon 320, Limpwurt 699 all attributed to the seed box)
> DIAGNOSIS PENDING - needs one more run on the new build; the log itself was the blocker.
> - Given the store's data the decision was consistent: still-to-collect only lists a source
>   when a seed short of at-hand (inv+box) has stock in the BANK or VAULT caches. The three
>   0/0 seeds therefore read as "owned nowhere" - nothing to collect, patch worded as a skip.
>   Whether that is truth or corruption turns entirely on the bank/vault counts, and the
>   run-planned line didn't print them (asked from play: "why isn't bank/vault listed?").
> - FIXED the line: every picked seed now shows all four sources (inv/box/bank/vault), plus a
>   per-source freshness summary ("sources read: inventory 2s ago, bank 3h ago, vault never
>   read...") - a zero from a source read a minute ago and a zero from one never read mean
>   different things and the line now says which.
> - Two suspects for the underlying data, distinguishable from the new line next run:
>   1. Phantom box counts (the 169/320/699 look like bank stacks): a box Fill/Empty delta
>      misattributed at a bank would inflate the box AND leave the bank correct - next line
>      would show the same seed large in both box and bank.
>   2. A zeroed/stale bank cache: bank shows 0 for seeds genuinely owned - next line would
>      show bank 0 with a suspicious "bank last read" age, or a wipe from a spurious empty
>      bank container event.
> - NOT related to the seed-box bank-layout issue above (that was drawing only).
> RESOLVED (suspect 1 confirmed by the new line): Avantoe/Ranarr/Watermelon/Limpwurt all read
> box == bank TO THE SEED (169/169, 27/27, 320/320, 699/699) - the box's contents were
> emptied into the bank and never debited, so the store kept counting them twice: "at hand"
> in the box (no withdraw asked) and again in owned totals (skewing the allocation - which is
> why only Cotton was fetched: phantom-rich seeds soaked up every herb patch).
> - THE CAUSE, in applyPendingSeedBoxAction: the armed Empty was consumed by WHICHEVER of
>   inventory/bank changed first. The bank interface can refresh the pack container with a
>   zero delta in the same window, which ate the click; the box's stacks then landed in the
>   bank container a tick later with nobody owed - bank recorded truth, box kept its phantom.
>   FIX: the pending action is now only consumed by an event that actually moved seeds; a
>   click that never meets its delta still expires on schedule, which also still feeds the
>   Empty-moved-nothing box-proved-empty heal. Test added (PersistenceTest
>   .anUnrelatedRefreshDoesNotStealTheEmptysBankDelta).
> - HEALING THE EXISTING CORRUPTION (it persists in config): click Empty on the seed box once
>   with a free pack slot - the expiry heal zeroes the phantom - or just open the box so its
>   real container overwrites the count. Withdraws and allocation right themselves after.
> - The user's ask "list the steps with this": run start now also logs the full loadout at
>   INFO ("Loadout for [HERB, ...]: Avantoe seed=CARRIED, Cotton seed=WITHDRAW x4 from BANK,
>   ...") - every item's verdict, not just the fetches, beside the Run planned line.
> - STILL OPEN, seen on the same line: "sources read: inventory never read" while logged in.
>   Should be impossible - SeedCapture.onGameTick reconciles the inventory every tick and is
>   registered (the bank read proves the object receives events). Watch whether it persists
>   on the next run; if so, check client.log for a startUp abort at plugin load.
>   20:00 UPDATE: persisted 3h into the session, and it is not cosmetic - the Empty-moved-
>   nothing box heal lives INSIDE relearnInventoryFromClient, so a dead reconcile also means
>   the phantom box cannot heal by the Empty click (which is why the run still only asks for
>   cotton). Ruled out: SeedInventoryStore not being a @Singleton (it is). The method now
>   counts its own progress and the run-planned line prints it ("inventory reconcile: X
>   ticks, Y logged in, Z stored"): 0 ticks = the tick handler never runs (registration /
>   event delivery); ticks but 0 logged in = game-state gate; logged in but 0 stored =
>   getItemContainer(INV) returning null; stored but "never read" = a reading bug. The box
>   can still heal WITHOUT the reconcile by opening the seed box itself (its container event
>   overwrites the counts through record()).

a while back we removed the highlighting on the seedbox when its time to plant a seed thats
in it, lets re-add that, our menu swapping checkbox should be unaffected by that
> RESOLVED. Highlight only, deliberately none of what the old removal removed: no FILL/EMPTY
> steps return, no step is hung on a patch (the stray-outline bug that caused the removal
> cannot recur), and GuideMenuSwap with its seedBoxLeftClick toggle is untouched.
> GuideInventoryOverlay: while the current step is PLANT and the box holds plantable seeds of
> that crop AND the pack has fewer than a patch's worth, all three seed box forms are
> highlighted alongside the seed's own highlight (both light up when a partial stack is
> loose). Tree saplings can't be boxed, so tree steps are quietly unaffected.

I've got a cactus in the farming guild thats set as H/O in my run right now, its currently
"Check-health" and the plan is skipping it now that the rest of the farming guild tasks are
done, but it shouldn't, I should check-health if its available and then harvest, even on H/O
> RESOLVED. RunPlanner.isActionable's harvest-only branch asked only hasProduceToPick(),
> and a grown-unchecked crop is still GROWING - nothing to pick YET - so the run walked
> past the one click that makes the picking (and the crop's real experience) available.
> Now hasProduceToPick() || needsHealthCheck(). The guide side never had the gap:
> GuidePlan's check-health branch sits above its harvest-only gate, so the step and the
> patch highlight appear as soon as the stop stays routable. After the check the cactus
> reads HARVESTABLE with stock and the harvest step follows; picked clean, the stop
> completes exactly as harvest-only always has. Test added (RunPlannerTest
> .aGrownUncheckedCropStillCountsOnHarvestOnly).

lol seedpacks are getting highlighted with that
> RESOLVED - and it unmasked a data trap sitting in four places. gameval ItemID names are
> the cache's INTERNAL names: SEEDBOX (22993) is the Seed pack - the farming contract
> reward - not a seed box form at all (the real pair is SEED_BOX 13639 / SEED_BOX_OPEN
> 24482). An earlier fix adopted it as "the third seed box id / the Farming Guild one", so
> every seed-box treatment applied to seed packs: the new plant-step highlight lit them
> (the report), and in RunLoadout owning a contract reward passed for owning a seed box.
> SEEDBOX removed from all four id sets (SeedCapture, GuideMenuSwap, RunLoadout,
> GuideInventoryOverlay), each now carrying a warning comment. The docs/NOTES entry about
> "the third seed box id" fix is thereby superseded.

standing outside my POH - being asked to do a weiss protected patch run by tele'ing to
house, however once I tele into my house, both the jewelery box and nexus are highlighted.
And the destination changes as soon as the POH loads, prompting me to now go to the
ardougne monistary for a bush H/O run. Thats weird
> RESOLVED (both symptoms, one root). Entering the house made the route recompute from
> garbage: a path request with no explicit start defaults, upstream, to the RAW player
> position - virtual-area coordinates inside an instance - so Shortest Path re-picked the
> "cheapest" stop from nowhere, the destination naming flipped (Weiss -> Ardougne bushes),
> and the furniture highlights followed the garbage route's hops (hence jewellery box AND
> nexus lit at once). Two feeders, both closed:
> - Our own posts: RunPlanner.retarget now refuses to post a START-LESS request while the
>   player's region is an instance's virtual area (x-part >= 100). The front-door reroute
>   still goes through - it says where it starts - and leaving the instance retargets
>   normally. This also stops garbage supply-leg posts from inside the house.
> - SP's own recomputes: it re-paths from the instance-template position every tick it
>   judges the player off-path, posting a transports message per recompute, and we read
>   every one. ShortestPathIntegration now ignores transports while the player is instanced
>   UNLESS we are awaiting an answer we asked for (the door-start reroute still lands).
> Net effect: teleport to the POH and the route display keeps the plan that was computed
> outside - destination stays Weiss, and only the furniture serving that plan's hops lights.
> Test added (RunPlannerTest.noStartlessRouteIsPostedFromInsideAnInstance). The full fix
> for in-house routing remains the upstream PR (sticky start + fromLocalInstance).

lets make drop bucket a non required step, and change its highlighting to a transparent red
> RESOLVED (scoped after a correction: the leprechaun hand-back step and its toggle stay
> exactly as they were). The drop-mode ambient bucket highlight - the one drawn for the
> whole run when "Drop empty buckets" is on - was painted in the guide colour, so the one
> highlight that means "whenever you like" read as the required next click. It now draws in
> its own transparent red (0xC43B3B, the sidebar's shortfall red, over the same FILL_ALPHA
> wash), so drop-when-convenient can't be mistaken for the step. GuideMenuSwap untouched.

Troll Stronghold step via POH is having me take the exit portal instead of stony basalt in
my nexus, however the text on the nexus is highlighted, the nexus itself is not, but the
POH exit portal is. Also while scrolling in the list of teles in the nexus the highlight
box moves around and lags behind a lot
> RESOLVED (both halves).
> - The contradiction: the nexus ROW lights by DESTINATION matching, but the furniture
>   outline judged only by the route's HOPS - and Shortest Path's model has no nexus, so a
>   destination the player's own furniture genuinely reaches (Troll Stronghold via the
>   added Stony basalt) can be absent from every hop. Hops matched nothing, the exit-portal
>   fallback won, and the two matchers disagreed on screen. The destination now gets a say
>   before the way out does: GuideOverlay tries destination-based furniture matching
>   (HouseTeleports.furnitureServesHop with the destination as the text) before falling
>   back to exit portals, and GuideTracker.furnitureServesTheRoute mirrors it so the travel
>   hint cannot contradict the outline.
> - The scroll lag: matchingRows cached the matched ROW RECTANGLES once a tick ("a menu
>   does not reshuffle between frames" - but a scrolling list does), so the box trailed the
>   moving row by up to 600ms. The scan (expensive) stays per tick; the measuring (cheap)
>   is now per frame from the live widgets, clipped to the list as before.

fairy ring step via POH fairy ring to Aldarin is doing something similar, highlighting the
exit portal, instead of the fairy ring. [After rebuild:] before entering it shows the fairy
ring code, its definitely there, however it goes away upon entering the POH, just says
"Travel to Aldarin" and the exit portal gets highlighted
> RESOLVED. Confirmed against SP's fairy_rings.tsv: a fairy hop's display info is the BARE
> CODE ("A J P", "ZANARIS", or a dash-chain like "A I R - D L R - D J Q"), and the
> destination rows' objectInfo column is EMPTY - so the composed hop line never contains
> the words "fairy ring". The garden ring failed furnitureServesHop on all three clauses,
> routeFromTheFrontDoor read the house as serving nothing, and its door-start reroute
> replaced the fairy plan - the walk-only answer cleared the via-lines (hence "Travel to
> Aldarin" with no hops) and handed the overlay to the exit portal. Every observed detail
> follows from that one wording gap.
> FIX: furnitureServesHop gains a fairy clause - furniture named "fairy ring" serves any
> hop that IS a code (regex: triplet of single letters, ZANARIS, or a dash-chain of them).
> With it, served=true, the door reroute stands down, the via-lines keep the code, and the
> garden ring outlines. Tests added (PlaceNameMatchTest.aFairyRingServesItsCodeShapedHops),
> including that no other furniture may claim a code.
> STILL REPRODUCING after that fix, with new evidence: the outside plan's hop is worded
> "Configure Fairy ring - C K Q" - which contains "fairy ring" and so matched clause 1 on
> EVERY build; the code-shaped-hop clause was solving a wording this leg does not use. The
> via-lines vanishing on entry therefore points at either (a) routeFromTheFrontDoor's
> doorIsTheWay override rerouting DESPITE the furniture serving (it deliberately beats
> "served"), or (b) the client running a pre-freeze build. Two changes to settle and fix it:
> - furnitureServesHop split: furnitureNamedByHop (hop contains the furniture's name, or is
>   a fairy code) is the STRONG half - the router explicitly routing THROUGH the furniture.
>   routeFromTheFrontDoor now lets a strong serve beat doorIsTheWay: the override exists for
>   destination-list coincidences (the Prifddinas nexus), not for a plan that names the
>   garden ring. Weak serves still lose to the door, preserving the Prifddinas behaviour.
> - The stay-put branch now logs at INFO once per visit ("House furniture serves the route
>   [...] - keeping the drawn path (explicitly named: X, door is the way: Y)"); the reroute
>   branch always logged. Whichever line appears at the next house entry ends the guessing;
>   NEITHER line appearing while the via-lines still vanish means the transports were
>   cleared by something outside routeFromTheFrontDoor (and points hard at a stale build).
> Tests: PlaceNameMatchTest.namedFurnitureOutranksADestinationCoincidence.
> THE REAL FIX (user's insight: "does SP have any navigation for that in its code?"): it
> does, and we were throwing it away. SP models the POH as a real place - a reserved map
> area (x 1856-2047, y 5696-5767, its own isInsidePoh), one canonical landing tile
> (1923,5709) every house teleport remaps to, and every house-furniture transport (portals,
> jewellery box, mounted items, garden spirit tree and fairy ring) anchored INSIDE it. And
> the transports message carries a per-hop ORIGIN list we never read. So the question every
> wording heuristic was guessing at has a data answer: a hop whose origin is inside that
> area is house furniture, by the router's own word.
> - ShortestPathIntegration now reads the origin list; isPohArea mirrors SP's bounds;
>   routeDepartsPoh is true when any hop departs the house (cleared with the route).
> - routeFromTheFrontDoor: a through-the-house plan counts as served AND strongly served -
>   never rerouted out the front door, even when no furniture NAME matched.
> - The travel hint's furnitureServesTheRoute asks the origins first.
> - GuideOverlay: the exit-portal fallback never fires while the plan departs the house;
>   when the furniture cannot be named, nothing lights and the unmatched hops are logged.
> Name/destination matching remains only for choosing WHICH furniture to outline.
> Tests: RouteOriginsTest (bounds + message flow, both directions).


when you run out of seeds, our seed inventory should keep them listed in the planner, right now it doesn't, and you can't reset the order. But maybe I don't want to reset the order, maybe I want to start back on those other seeds as I gather new ones, so just, if you've seen a seed on this account, ever. List it, but maybe dull it out so its clear by eye that you don't have any
> RESOLVED. The list was ownership-gated (PlantableResolver skipped owned==0), so running out
> delisted the seed - taking its picked state and its place in the fixed level-order grid
> with it, invisibly (the selection store never pruned it; you just couldn't see or untick it).
> - SeedInventoryStore now keeps a persisted "ever seen" set (seedsEverSeen, per profile):
>   any seed id observed with a nonzero count in any source, ever, grown-only. Backfilled on
>   load from the current persisted counts so existing profiles start with what they hold.
>   Cleared by profile reset. hasEverSeen checks seed+sapling forms like getCount.
> - PlantableResolver lists ever-seen seeds at owned 0; they draw dulled (isUsable false),
>   keep their fixed level-order slot, and keep their selected border + priority digit.
> - ZERO CLICKS to rejoin (clarified from play): the selection sticks through zero stock, the
>   allocation rebuilds per tick from selection x owned, so a restocked seed re-enters the
>   run by itself. Also: toggling is now gated on Farming level alone, not stock, so a
>   run-out seed can be picked/unpicked while empty (picking-ahead is planning, and the
>   allocator ignores an empty pick by itself). Tooltip explains the dulled state.
> - Tests: PlantableResolverTest (run-out stays listed dulled, never-held stays hidden),
>   PersistenceTest (ever-seen survives reload; never-held doesn't leak in).
> FOLLOW-UP (reported from play): snape grass was picked at position 1 with none owned and
> none on record, and stayed invisible - the ever-seen memory only reaches back to this
> plugin's own first sighting, so a pre-existing pick predating it had no witness. A picked
> seed now ALWAYS lists: SeedSelectorPanel hands the group's selection to
> PlantableResolver.forPatchType(type, includeUnowned, alwaysListed), and being picked is
> itself proof of belonging. Visible means untickable/reorderable again. Test added.

when having to note an item on lep when your inventory is full, the infobox freaks out and puts the notice under each of the steps. Also before your inventory is full it puts that at the bottom of the list?
> RESOLVED (the freak-out; the bottom-of-list one is intended, see below).
> - The repeat: the full-pack note step is raised per PATCH - every patch at the stop with
>   produce to pick added its own copy, each naming its own crop, so four harvestable patches
>   read as four leprechaun trips. GuideTracker.collapseDuplicateNotes now keeps only the
>   first (the working patch's, naming the crop being harvested right now); the list is
>   re-derived every tick, so if the pack is still full after following it, the next crop's
>   note surfaces by itself. Collapsed BEFORE the skip filter, so skipping the visible note
>   silences the notice instead of promoting the next patch's copy. Test added
>   (LeprechaunErrandOrderTest.aFullPackNotesOnceNotPerPatch).
> - The bottom-of-list notice before the pack is full is a different step doing its job:
>   "note your crops before moving on" is the leaving errand, and the bottom is its place -
>   the stop's checklist runs patch work first, packing up last. It moves up to join the
>   visit only when something else (a compost withdrawal, a full-pack note) is already
>   taking you to him. Flag it if the wording reads too much like the full-pack notice.

okay, I just got a contract for poison ivy, I already had one planted though, and it was already "check-health"'d, and I was directed back to jane after harvesting it. Couldn't turn in the contract, I think you have to plant it after getting the contract on some patches but not others, do some research on this to determine the mechanics and impliment them. Going to be very difficult to test this in game, the odds are low that this happens tbh, but it has, so lets fix it.
> RESOLVED. The mechanics, wiki-confirmed (Farming contracts page): completion is an EVENT,
> not a state - check-health for bush/tree/fruit tree/cactus/celastrus/hespori, fully
> harvested for everything else - and the event must fire while the contract is active.
> Planted-before-assignment is fine ("already been planted but not checked or harvested...
> will still count"); CHECKED-before-assignment is fatal ("they will need to plant a new
> seed and wait for it to grow") - the check fires once per planting, and harvesting what
> regrows changes nothing. So yes: on check-health patches you effectively must (re)plant
> after taking the contract if the standing crop was already checked; on harvest-class
> patches a standing crop always still counts, since its completion event empties the patch.
> - THE BUG: contractToHandIn's patch-evidence fallback read the standing HARVESTABLE
>   poison ivy as a completion nothing had recorded (recordCompleted!), deferred behind the
>   berries, and after the harvest sent you to Jane - who won't take it. For check-health
>   families HARVESTABLE only EXISTS after the check, a mid-contract check announces itself
>   in the chatbox (captured), and the logged-out case the fallback exists for leaves the
>   crop grown-but-UNCHECKED (a check needs a present player) - so a HARVESTABLE check-class
>   crop with nothing awaiting is precisely the pre-checked dud. isGrownInGuild now splits
>   the evidence by family: needsHealthCheck for check-class, HARVESTABLE for harvest-class.
> - THE GUIDANCE: no new steps needed - with the false completion gone, the contract stays
>   assigned, the contract group still claims the patch, the seed allocation names a fresh
>   poison ivy for it (HARVESTABLE patches are already in the plantable set), and the
>   existing bush machinery walks harvest -> dig up -> replant; the planner's
>   wantsReplantClear keeps the stop alive on the contract group's derived selection. What
>   WAS added is the why: a panel note + one-shot chat line ("...was health-checked before
>   the contract was taken, so it can never count for it. Dig it up and plant a fresh...") -
>   without it the dig-up step at a healthy producing bush reads as the guide malfunctioning.
>   (missingContractSeed deliberately stays quiet - it treats crop-in-ground as seed-spent,
>   which is exactly wrong for a dud - so the note's wording asks for the fresh seed.)
> - HEALING EXISTING CORRUPTION (the false recordCompleted persists in config, and this
>   account has it): reconcileAwaitingHandIn - which it turns out was documented into
>   existence by the self-healing pass and NEVER WIRED ANYWHERE, so its deadlock heal had
>   run zero times - is now called at plugin load and on Time Tracking's key changing. It
>   also gained the specific heal: awaiting X while Time Tracking (enabled) STILL has X
>   assigned is a contradiction - the completion message clears that key the moment it
>   fires, and Jane never assigns the same crop twice running - so the awaiting record is
>   cleared as corruption and the assignment survives. Your poison ivy contract will read
>   as assigned-and-ungrown on next load, and the run will replant the patch.
> - Tests: ContractHandInOrderTest (pre-checked dud raises no hand-in and records nothing;
>   grown-unchecked still detected; harvest-class herb still detected; the dud note + one-
>   shot chat line), ContractStateTest (reconcile: differing assignment clears, same-crop
>   contradiction clears but keeps the assignment, disabled Time Tracking's stale key
>   proves nothing).

lets not jerk back the bank filtering on the player after they navigate away from it in the bank, its rude
> RESOLVED. Clicking another tab (or your own Bank Tags tag) already deactivates our tag -
> Bank Tags' own doing - but BankFilter kept believing it was open, so the next bank change
> re-opened it through relayoutIfBankChanged, yanking the view back mid-browse. The tick
> handler now asks Bank Tags whose tag is actually active: not ours means the player
> navigated away, and the filter stands down for the REST OF THAT BANK - no closeBankTag
> (their chosen view stays put), no re-open. Scoped to the bank session: closing and
> reopening the bank starts filtered again, as does the next bank. Tests added
> (BankFilterTest.navigatingAwayStandsTheFilterDownWithoutAFight, theNextBankOpensFilteredAgain).

menu entry swap grimy herbs to 'use' instead of 'clean' when highlighting to give to lep
> RESOLVED. While a NOTE_AT_LEPRECHAUN step is current, Use is promoted to a grimy herb's
> left-click (GuideMenuSwap, same reorder-never-rename machinery as the others). Grimy herbs
> are the one noted item whose left-click fights the step - everything else already defaults
> to Use, and Clean-by-accident hands him a herb he won't note. Applies to ANY grimy herb,
> not just the one the step names: the step points at the biggest stack, the visit notes them
> all, and only the hovered item's own menu is ever reordered. Behind a new toggle
> "Left-click Use grimy herbs at the leprechaun" (on by default); herbs clean normally
> whenever no note step is current. The grimy set lives beside the names table in
> NotableHarvests (isGrimyHerb), pinned by GuideMenuSwapTest. Verify the exact option
> wording ("Use") in client with the rest.

POH fairy ring issue still ongoing, is SP dropping it when you enter the house? it still suggests the ring code in the menu for the fairy ring so I know it knows about it. The infobox is inacurate too when you enter the POH, drops the fairy ring messaging.
> RESOLVED (two real causes, both caught red-handed in client.log - and no, SP is not
> dropping it; our stored route now survives house entry, the log proves it).
> - The outline: at 22:16:10 the new unmatched-hops line fired with the router's exact
>   words - hops [Teleport to House, Configure Fairy ring - C K Q], "None of Shortest
>   Path's hops mapped to the furniture here". The route was intact INSIDE the house; the
>   furniture NAME match failed. CORRECTED after the first write-up guessed a combined
>   build (owner: it's a plain fairy ring): a plain "Fairy ring" name WOULD match that hop
>   by containment, so the scan's name resolution for the ring itself is what failed -
>   the jewellery box's old disease (an impostor-varied object reporting a placeholder
>   name), caught in a second place. All three superior-garden teleport builds are now
>   recognised BY ID with forced names (PlayerHouse.GARDEN_TELEPORT_IDS: POH_SPIRIT_TREE
>   29227 "Spirit tree", POH_FAIRY_RING 29228 "Fairy ring", POH_SPIRIT_RING 29229 "Spirit
>   tree & fairy ring" - wiki-checked ids), so scan and match no longer depend on what the
>   scene calls them. The combined build's forced name splits on "&" in
>   furnitureNamedByHop so each half claims its own hops, and fairy rings also match by
>   kind (name has "fairy ring" + hop says "fairy ring" or is a code). If it STILL fails,
>   the unmatched-hops log now prints the other half of the comparison - every scanned
>   furniture as name#id - so the next occurrence is diagnosable on sight.
>   Tests: PlaceNameMatchTest.aCombinedBuildAnswersForEachOfItsHalves.
> - The infobox going quiet: the log's line 2789 shows a run REPLANNED from region 56001 -
>   an instance's virtual area, i.e. from inside the house. Every retarget starts by wiping
>   the route state, and the instance guard then refused to post the start-less request -
>   so the wipe stood: no destination, no via-lines, nothing outlined, until you walked
>   out. The guard now falls back to the FRONT DOOR as the start when you are standing in
>   your own house (the same compromise routeFromTheFrontDoor makes); other instances stay
>   silent as before. Test: RunPlannerTest.aPlanMadeInsideTheHouseRoutesFromTheFrontDoor.


----------------
Weird catherby step, heres the full text and the situation, when outside the POH

"Farm run
Travel to the next patches.
15 stops left
via Teleport to House
via Enter Catherby Portal - Catherby Portal"

Spell books and POH tele not highlighted

After teleporting anyways text changes to

"Farm Run
Travel to the next patches.
15 steps left."

Nothing is highlighted in the POH
> RESOLVED (three stacked, sharing parts with the fairy entry above).
> - "Travel to the next patches" + nothing lit OUTSIDE: the tell is the missing
>   destination. destinationStop only names a trip when it can do so without guessing, and
>   travelHint(null) returned null OUTRIGHT - so an ambiguous leg travelled with hops on
>   screen ("via Teleport to House") and no hint at all: no "Cast Teleport to House." line,
>   no spellbook outline, no tablet highlight. The hint is now built from the route's hops
>   whether or not the destination has a name; only the destination-keyed extras (nexus
>   row, jewellery category) stay quiet on a nameless leg, and those overlays null-guard.
>   Test: NamelessLegStillHintsTest.
> - Everything vanishing INSIDE: the replan-from-inside-the-house wipe, fixed with the
>   front-door fallback described in the fairy entry above.
> - The Catherby Portal wording is SP imagining POH portals it cannot see (documented in
>   the earlier Catherby entry); with the two fixes above the panel keeps its lines and the
>   destination-based furniture match can still light the nexus/box that really serves it.

----------------


incorrectly highlighting for Al Kharid tasks on the ornate jewellery box, its meant to go to 1: Emir's Arena (what SP says), but instead its highlighting "R: Al Kharid" - clicking on "R: Al Kharid" results in getting routed back to to tele home, creating an infinate loop, because the other al kharid location is too far away from the patch, causing SP to navigate us.
> RESOLVED. The row highlight was keyed on the DESTINATION name alone, and a jewellery box
> can carry the stop's name verbatim on a row the router did not pick - "R: Al Kharid"
> (glory) beside "1: Emir's Arena" (dueling), with the router having planned through the
> arena row because it lands beside the patch. The overlay now asks the route's own words
> first: every hop with a " - " names its row after it ("Teleport Menu Fancy Jewellery Box
> - 1: Emir's Arena"), and those row names - the router's own choices, in path order - are
> tried before the destination, which stays as the fallback (the nexus's model-less
> destinations like Stony basalt are only findable that way). One widget walk per tick
> shared by all the wants, same caching as before. GuideInventoryOverlay.rowNames/scanRows;
> matcher facts pinned in PlaceNameMatchTest.aHopsOwnRowNameFindsExactlyItsRow. Verify the
> right row lights in client - the fix rides on SP's hop wording carrying the row name.
> STILL REPRODUCED after that fix, with the connecting clue reported alongside: the "via
> Teleport Menu Basic Jewellery box - 1: Emir's Arena" line VANISHES on entering the POH -
> and the row fix reads that hop for its row name, so when the hop dies the highlight falls
> back to the stop's name and lights "R: Al Kharid" again. Same root, and it was the NEW
> front-door retarget fallback (the fairy-entry fix) firing too broadly: on every in-house
> retarget it re-routed from the front door, throwing away the through-the-box plan
> mid-teleport. The old outright refusal had been quietly load-bearing for legs whose next
> click is furniture INSIDE the house. Now scoped by the router's own origins: a live plan
> that departs the POH keeps its route on every in-house retarget (routeFromTheFrontDoor
> still owns the door-vs-furniture judgment); the door start serves only walk-out plans and
> the dead no-plan state the fallback was built for. Test: RunPlannerTest
> .aLiveRouteThroughTheHouseKeepsItsRouteInside.

from the distance from the cactus patch and the lep in the farming guild, the lep doesn't highlight to note, but he does 6 tiles closer
> RESOLVED. Not a distance cap on the highlight - the highlight scans the whole scene - but
> the STEP vanishing: stopAt() matched the player's map region to the stop's EXACTLY, and
> the guild's cactus patch sits close enough to its region's edge that standing on the far
> side puts your region id one column over. Every region-keyed question then answered
> "between stops" at once - step list emptied, note step gone, leprechaun dark - until you
> walked ~6 tiles back over an invisible line. New GuideTracker.standingAt: a stop claims
> its own region always, and a TOUCHING region within 10 tiles of one of its patches'
> learned locations. Wired into stopAt (exact matches still win), the leaving-errands gate
> (same hole, one report away: the errands vanished from across the line the moment the
> stop completed) and the panel title's fallback. A patch with no learned location claims
> nothing past its region - nothing to measure from also means no work to stand beside.
> Tests: StopRegionEdgeTest.

run has me trying to harvest a herb patch without a spade - I have them in my bank, and at the lep
> RESOLVED. A wrong assumption in the guide's model, caught by the game itself: herb and
> allotment crops need a SPADE to harvest (wiki: "a spade to harvest herbs"), and the
> harvest branch fetched no tool at all - rake, dibber and the clearing spade all had their
> addToolStep, the pick alone was assumed free-handed. With the spades in the bank and the
> leprechaun's store, the guide said "Harvest the herb" at a patch the game refused to let
> the player touch. New SPADE_HARVESTED set in GuidePlan (HERB + ALLOTMENT - flowers, hops
> and the regrowing families pick by hand and are deliberately absent; extend on evidence):
> the fetch step goes after the full-pack note (noting frees the slots, same trip to him)
> and before the harvest. The bank half was already the loadout's job via ToolNeeds. Tests:
> GuidePlanTest.aSpadelessHarvestFetchesTheSpadeFromTheLeprechaun / aCarriedSpadeLeavesTheHarvestAlone.

al-kharid travel issue still on-going for emir's arena still going to "R: Al Kharid" and not "1: Emir's Arena", info box still incorrect as well, showing "Farm run - Al Kharid\n Travel to Al Kharid.\n 14 stops left"
same for fairy ring, the step changes every time you enter the POH, is this a location bug? because the POH has a set of coordinates, are they re-rendering inside the house? maybe we need to cache that on home teles or something? Pull every text option that could display from this from the SP plugin and turn it into a test in our test suite, another one of these IS working, seems inconsistent. A L S works.

notification should be only showing based on what you have checked for a farm run, not all patches enabled.

seedbox orange highlighting not showing at all, ever
> DIAGNOSIS PENDING - the gate's logic checks out, so the next run answers it.
> - The highlight code is present and correct as far as tests can reach: while the current
>   step is PLANT, the box lights when the step's seed resolves, the box holds plantable
>   stock of it, and the pack holds less than a patch's worth. All four corners of that
>   gate are now pinned by SeedBoxHighlightTest against a real store fed through the same
>   container path the client uses - they pass.
> - So "never" means one of the three inputs is not what it looks like on screen at the
>   moment of planting. The overlay now logs the decision, once per distinct answer
>   ("Seed box highlight at <patch>: ON/OFF - N in the box (plantable), M loose in the
>   pack, patch takes K"), same spirit as the errand-bundle line. Next plant step with
>   seeds boxed, grep client.log for "Seed box highlight" and the failing input names
>   itself.
> - (Also learned while looking: this source file trips grep's binary detection - some
>   byte in it makes ordinary grep silently skip the whole file; use grep -a. Two rounds
>   of "the code is missing!" were that.)

I think we're going back too early on some h/o patches, bushes for example, not sure if this applys to all h/o types (research this) but from my understanding is they re grow in stages as well, we should only do these when they're maxed out on regrowth, otherwise yeild is lower and we're just wasting time
> RESOLVED. Research confirmed the model (wiki, all regrowing families): produce refills
> one unit per fixed tick - bush 4 berries @ ~20min, cactus 3 spines @ 25min, fruit tree
> 6 @ 40min, calquat 6 @ 20min, potato cactus lives @ ~25min full - and NOTHING accumulates
> past the cap, so a full plant is idle while a part-full one is still working. Visiting
> early buys nothing and costs the trip; for bushes the wiki also ties the guaranteed-4 +
> level-scaled yield to a FULLY regrown bush ("the minimum always being four unless
> harvested again before it has regrown all berries"). Celastrus/seaweed do not regrow at
> all (fixed harvest); herbs/allotments/flowers/hops clear on harvest - so the rule is
> exactly the regrowing families, detected by the data (regrowTickrate), not by a list.
> - RunPlanner.heldForRegrowth: on a HARVEST-ONLY run, a regrowing crop with stock but
>   more still coming (hasProduceToPick && isRegrowing - the projection's regrowEstimate
>   is 0 exactly at the cap) is held out of the plan. Full runs are untouched: they come
>   to dig the plant out, and holding that trip hostage to berries the spade is about to
>   destroy would be backwards.
> - Planning-time only, wired beside clusterHeld at all three sites (planStops, the
>   pricing counts, the estimate), NEVER completion: once you are there picking, the
>   falling stock must not complete the stop under your feet at the first berry.
> - The projection already climbs stock back up over elapsed time, so a bush seen
>   half-full an hour ago and full by now is correctly included, and a never-observed
>   patch has nothing to hold back.
> - Test: RunPlannerTest.aRefillingBushWaitsForTheCapOnHarvestOnly (varbit-decoded states,
>   not hard-coded values).

notification should be only showing based on what you have checked for a farm run, not all patches enabled.
> RESOLVED. The ready counter (top-left infobox) counted every AVAILABLE patch - the
> panel's account-wide view - so unticked types inflated a number that exists to say "a
> farm run is worth starting". It now asks the planner the run's own question first:
> RunPlanner.selectedForRuns = ticked for a run (full or harvest-only, same test routing
> uses) AND not held for regrowth - so a bush the run is deliberately letting refill does
> not sit in the count either, and the number on screen agrees with what Start run would
> do. The sidebar's own summary stays account-wide on purpose; it is the overview, the
> infobox is the glanceable "go now" number. Ready/needs-attention lists in the tooltip
> filter the same way. Test: RunPlannerTest.theReadyCounterOnlyCountsWhatARunWouldVisit.

same for fairy ring, the step changes every time you enter the POH, is this a location bug? because the POH has a set of coordinates, are they re-rendering inside the house? maybe we need to cache that on home teles or something? Pull every text option that could display from this from the SP plugin and turn it into a test in our test suite, another one of these IS working, seems inconsistent. A L S works.
> RESOLVED (the caching you suggested is exactly what the jewellery-box fix above built).
> - "Step changes every time you enter": yes, a location bug of our own making - entering
>   the house is an instance, and every in-house retarget used to either re-route from
>   garbage (the old bug) or, after the front-door fallback, re-route from the door -
>   throwing away the plan computed outside and re-answering from new coordinates each
>   entry. The fix IS the cache: a live plan whose origins say it goes THROUGH the house
>   (the router's own word, routeDepartsPoh) is now preserved untouched across entry and
>   every in-house retarget. Computed outside once, kept inside.
> - "Seems inconsistent, A L S works": explained by SP's own data model, and now pinned.
>   fairy_rings.tsv (SP source, ground truth) has two row shapes: destination rows carry
>   ONLY the code as display info ("A L S", "ZANARIS", chains like "A I R - D L R - D J Q
>   - A J S"), origin rows carry ONLY objectInfo ("Configure Fairy ring <objectId>"),
>   which readTransports id-strips and prefixes ("Configure Fairy ring - C K Q"). Which
>   shape a plan produces depends on which edge the router used - both are normal, and the
>   matcher must take every one.
> - The full matrix is now a test: SpFairyWordingTest runs every composed shape from the
>   TSV against furnitureNamedByHop for the plain ring AND the combined build, asserts no
>   other furniture can claim a fairy shape, and pins the one oddball (a ring entered via
>   "Enter Portal", varbit-gated) as correctly NOT claiming the garden ring. All pass
>   against the current matcher - so with the by-id ring naming (POH_FAIRY_RING 29228,
>   which is literally the id in SP's own POH row) and the preserved route, every known
>   wording resolves. HopWordingTest also pins the 29228 id-strip.

seedbox still not highlighting to fill, whats the full logic around that right now, we're missing something
> RESOLVED - the missing something was the whole direction. The box's full logic was:
> (1) the left-click swap (GuideMenuSwap.wantedBoxOption: Fill when the box is empty or
> loose seeds fit, Empty when it holds seeds and nothing loose fits); (2) a highlight
> while the current step is PLANT and the needed seeds are in the box - the TAKE-OUT
> side, which is what all the earlier testing proved correct; and (3) for the fill side:
> NOTHING. The fill steps were removed by request long ago (they hung patch outlines off
> an inventory instruction) and the later re-added highlight covered only the planting
> direction - so "never highlights to fill" was literally true by design, and the earlier
> "not showing at all, ever" report was this, not a broken gate.
> - NEW: an ambient fill highlight, bucket-style. While a run is on and any loose seed
>   would actually go in - GuideMenuSwap.boxCanTakeLooseSeeds, the swap's own six-kinds
>   rule, now shared so the two cannot drift - both box forms light in their own ORANGE
>   (0xE88D1E): "when convenient", distinct from the bucket's red and from the guide
>   colour's "this is the step". No new steps, no patch outlines, swap untouched.
> - The plant-side highlight and its diagnostic log stay as they were; the fill rule is
>   pinned through the swap's existing GuideMenuSwapTest cases (loose-fits, six-kinds-full,
>   sapling-never-counts).

with herb, allotment, flower patches checked, we shouldn't visit the areas with those 3 until all 3 are finished growing, its a waste of teles, we already did a pass at this and its not working.
> DIAGNOSIS PENDING - one real bug found and fixed (the counter), the routing itself not
> reproducible yet. Everything below was settled against live state, not theory.
> RULED OUT, each with evidence:
> - The toggle: holdClustersUntilReady is TRUE in the profile.
> - The build: build-windows/.../RunPlanner.class (what the running client loaded) contains
>   clusterHeld, so the deployed code has the feature.
> - The protection-payment exception, which was the prime suspect: runProtection is
>   ["FRUIT_TREE|PALM","CACTUS|POTATO_CACTUS","CALQUAT|CALQUAT","TREE|MAPLE","TREE|YEW",
>   "TREE|MAGIC"] - no HERB, ALLOTMENT or FLOWER anywhere, so wantsProtectionPayment can never
>   fire for a cluster patch and the exception is unreachable on this account.
> - The rule itself, against this account's own recorded plot: RunPlannerTest
>   .theGuildPlotIsHeldWhileItsHerbAndAllotmentsGrow rebuilds the Farming Guild from the stored
>   varbits verbatim (flower 32 ready, herb 56 avantoe growing, allotments 59/58 growing) with
>   the group and run-option lookups made to behave like the client's rather than the null-
>   returning mocks the other tests get away with. It holds. The paired
>   theGuildPlotIsVisitedOnceNothingIsStillGrowing proves the fixture can produce a visit, so
>   the hold is not passing by accident.
> FIXED (a real inconsistency found while ruling the above out): the ready counter - the
> top-left infobox - applied heldForRegrowth but NOT clusterHeld, so it counted the ready
> flower of a plot Start run would refuse to visit and said "go now" about exactly the teleport
> this setting exists to prevent. selectedForRuns now applies both, scoped to the ticked types
> (new tickedTypes(), since runTypes is empty between runs, which is when the counter is read).
> - The rule was restated from play and matches what the code already does: for the three types
>   that share a plot, hold the location while any patch of a TICKED one of them is still
>   growing - two ticked types wait for each other exactly as three do, and an unticked type is
>   not part of the question. (An empty patch cannot hold: it is only filled by visiting.)
> - So the failure is in one of the three ways a plot legitimately goes free, and which one is
>   not recoverable after the fact - the decision is derived at Start from four stores and the
>   crop has grown on by the time the trip is noticed. The prime suspect is the protection
>   payment: a growing crop that is protection-selected and has no recorded payment deliberately
>   lets the trip through, and with Herb (protected) + ultracompost every herb planted is
>   protection-selected - so ONE missed payment capture leaves the plot free for the whole
>   growth cycle. Second suspect is the tick scoping, e.g. Catherby's herb belonging to the
>   plain Herb group while only Herb (protected) is ticked.
> - NEW LINE at run start (only when the setting is on): "Shared plots: Falador [HELD] [...]",
>   naming every cluster patch, its state, and the verdict that decided it - "growing - HOLDS
>   the plot", "growing, but owed its protection payment - lets the trip through", "not ticked
>   for this run, so it holds nothing", "you are standing here", "empty", "finished (DISEASED)".
>   Next run that routes you somewhere early, grep client.log for "Shared plots" and the cause
>   names itself. RunPlanner.describeClusterHolds.

don't prompt to note logs on lep, thats not a thing, they won't note logs.
> RESOLVED, and wiki-confirmed: the tool leprechaun's page lists "Any type of logs" among what
> he refuses. Every tree family's Produce row carries its LOGS as the item - that is what core's
> enum stores for a tree - so a pack of magic logs read as a pack of notable crop and the guide
> sent you to him with nothing he would take. Not merely a wasted trip: one you cannot complete.
> - New Produce.isLeprechaunNotable, narrower than isNotable, refusing TREE / HARDWOOD_TREE /
>   REDWOOD (logs) plus three false positives of the same shape that were sitting behind it:
>   BELLADONNA (cave nightshade, also on his refusal list), CRYSTAL_TREE (shards, which stack -
>   and a stack of a hundred would have outbid every real crop for the step) and SPIRIT_TREE
>   (a dummy item that is not a drop). Both places that send you to him now ask it:
>   GuideTracker's note-before-leaving scan and GuidePlan's full-pack note.
> - isNotable itself is unchanged and still what the bank's deposit highlight asks - a noted log
>   still belongs in the bank; it is only the walk to the leprechaun that was wrong.
> - ROOTS are unaffected and still noted (wiki: added 2014), so a tree dig-up is still a trip to
>   him - it is the logs half that is not.
> - FOUND WHILE DOING IT, same fault read the other way round: CELASTRUS's Produce item is a
>   BATTLESTAFF (what the bark becomes), so celastrus BARK - the actual harvest - was as
>   invisible to the note step as grimy herbs and roots were before it, and carrying ordinary
>   battlestaves read as a celastrus harvest. Bark (22935, CELASTRUS_WOOD) added to
>   NotableHarvests; the battlestaff id is deliberately left alone in Produce, since the farming
>   contract is stored as that id and must decode back to CELASTRUS.
> Tests: LeprechaunErrandOrderTest.logsAreNeverOfferedForNoting /
> theRootsFromTheSameDigUpAreStillNoted / celastrusBarkIsNotedButABattlestaffIsNot.

not getting prompted to rake a weedy patch, or withdraw rake from lep, goes straight to compost step.
> RESOLVED. One wrong assumption, held in two places: "has auto-weed" was treated as "has no
> weeds". The unlock stops weeds GROWING - it does not rake a patch that is already weedy, and a
> patch that was weedy when it was bought, or one first reached afterwards, still has its
> original weeds. Confirmed against live state before theorising: this account's autoweed varbit
> reads 2 (on), which is why the weedy patch was invisible.
> - GrowthTimer.project zeroed the STAGE of any WEEDS produce whenever auto-weed was on, so
>   every reader was told the patch was clean. GuidePlan's rake branch asks for exactly "WEEDS
>   at a stage above zero", so it never fired and the patch fell through to the compost step -
>   and with no rake step there was no leprechaun fetch either, which is the second half of the
>   report. Now only the tick rate is dropped: the weeds do not count down, the observed stage
>   survives, and no doneEstimate is produced (which still keeps a fully weedy patch from being
>   promoted to HARVESTABLE, the other thing that block did).
> - ToolNeeds.requiredFor left the rake out of the loadout entirely on the strength of the
>   unlock, so the bank leg would not fetch one either. It now asks whether anything in the run
>   is actually weedy - same stage test as the guide, on the same two stores, so the loadout
>   cannot promise a rake the patch does not want or stay quiet about one it does. On a normal
>   account with the unlock and no weeds, nothing changes.
> Tests: GuidePlanTest.aWeedyPatchIsRakedEvenWithAutoweedOn /
> aRakedPatchIsNotRakedAgainWithAutoweedOn (the fix must not invent weeds either),
> RunLoadoutTest.autoweedDropsTheRakeWhileNothingIsWeedy /
> aWeedyPatchStillWantsTheRakeDespiteAutoweed.

If I'm an idiot and deposit my spade I never get prompted to withdraw one mid run, can get from the lep or bank depending on what the user has
> RESOLVED. Two halves, and each was individually defensible, which is how the gap between them
> went unnoticed:
> - Whether a run needs a bank was decided ONCE, at start(), and re-asked only for a contract
>   taken from Jane mid-run. Nothing ever asked again, so a tool that left the pack during the
>   run was invisible to the planner.
> - GuidePlan.addToolStep offers the LEPRECHAUN's copy and is deliberately silent when he has
>   none, on the grounds that a tool in your bank is the loadout's business before setting off.
> Between them, a spadeless run walked patch to patch with nothing to say and nothing it could
> do there.
> - NEW RunPlanner.reviewSupplies, polled beside reviewProgress/reviewContract: re-arms the
>   supply leg when ToolNeeds.anyOnlyInBank goes true, which is exactly "the run needs this, it
>   is not on you, and the leprechaun has not got one either". The loadout already lists the
>   tool as WITHDRAW from BANK and suppliesOutstanding already refuses to close the leg until
>   it is withdrawn, so the rest of the machinery needed nothing.
> - The LEPRECHAUN case needed no change and must not be broken: anyOnlyInBank is false whenever
>   he holds one, so the run never diverts past a leprechaun standing next to you - the patch's
>   own tool step already says "get it from him", which is the cheaper trip. Pinned, because the
>   tempting shape ("the spade is not in your pack, go to a bank") would do exactly that.
> - An UNREAD store cannot cause a divert either: sourceOf returns UNKNOWN, not BANK, until both
>   the bank and the leprechaun have been read (his fills in within 600ms of login).
> - A bank leg the player waived with Skip step stays waived - the escape hatch outranks this,
>   same rule pickUpDeferredSupplies already follows.
> Tests: RunPlannerTest.aToolLostMidRunSendsTheRunBackForIt /
> aToolTheLeprechaunHoldsNeverDivertsTheRun / aWaivedBankLegIsNotReArmedByALostTool.
>
> STILL FELL THROUGH after that, and the log named the reason on sight: the run-planned line
> read "Spade=AT_LEPRECHAUN", not BANK - so the divert above was right to stay out of it, and
> the failure was the PATCH step that should have offered his copy. Cause found by the owner:
> "maybe because its a sapling? spade is needed to plant those".
> - Confirmed on the wiki, same sentence on the tree patch AND fruit tree patch articles:
>   "players must have a spade in their inventory when planting and removing the stump of a
>   tree". Only the stump half was ever modelled. GuidePlan's sow branch fetched a DIBBER for a
>   seed and NOTHING for a sapling, on a comment that said "saplings go in by hand" - a wrong
>   fact, sitting there as the justification for the silence. So a run whose spade was in the
>   leprechaun's store reached a tree patch, said "plant the yew sapling", and the game refused;
>   nothing in the guide had a reason to speak.
> - A sapling now takes the spade through the same addToolStep the rake, dibber and clearing
>   spade always used, so it fetches from the leprechaun when he has one and the loadout's
>   always-required spade covers the bank before setting off.
> - Barbarian Farming does NOT excuse it - the unlock removes the seed DIBBER requirement, so it
>   stays consulted only on the seed branch. Pinned, because reading it as "no tool needed to
>   plant" puts the tree patch straight back here.
> Tests: GuidePlanTest.plantingASaplingFetchesTheSpadeFromTheLeprechaun /
> aCarriedSpadeLeavesTheSaplingPlantingAlone / barbarianFarmingDoesNotExcuseTheSaplingsSpade.

SP is trying to have me use the ardougne cloak 3 to go to ardougne farm right now and I'm out of my daily teles there - can we dynamically exclude that use from the run when its out of charges
> RESOLVED as far as it can be locally; the routing half is upstream's and is stated below.
> Researched first, because the numbers matter: the Ardougne cloak's MONASTERY teleport is
> unlimited on every tier, and it is the FARM PATCH teleport that is rationed - 3/day on the
> cloak 2, 5/day on the cloak 3 (raised from 1/day in Nov 2017), unlimited on the cloak 4 and
> the max cape. Explorer's ring 2 is 3/day to the cabbage patch; rings 3 and 4 are unlimited.
> - Shortest Path ALREADY tries to gate this: teleportation_items.tsv gates the cloak rows on
>   "6069<3" and "6069<5". The gate can never fire. Varbit 6069 is ARDOUGNE_CLOAK_LOWBITS -
>   bit 18 of varplayer 635, ONE bit, so its value is 0 or 1 and always under three. Bits 19+
>   of that varp are unrelated varbits (ARDOUGNE_ROL_RESPAWN_POINT, ARDOUGNE_FREE_ESSENCE,
>   YANILLE_*, BERT_DAILYHINT), so the rest of the counter is not in the varp at all and the
>   server does not send it - SP's own data comment says "varbit 6069 is only lowbits, highbits
>   is not transmitted". The count is genuinely unreadable client-side.
> - The explorer's ring needs nothing: varbit 4552 (LUMBRIDGE_CABBAGE_TELEPORT) is bits 25-26
>   of the same varp, a real 0-3 counter that does arrive, so SP's gate on the ring works.
> - We cannot tell SP to drop one transport. Its plugin-message config override only accepts
>   ^(avoidWilderness|includeBankPath|currencyThreshold|use\w+|cost\w+)$ - whole categories.
>   The nearest lever, useTeleportationItems=*_NON_CONSUMABLE, would also drop the Skills
>   necklace and every teleport tab (the TSV marks those Consumable=T as well). So SP goes on
>   DRAWING the cloak hop, and only what we say about it changes.
> - What we say now comes from the game itself: "You have already used all of your available
>   teleports for today. Try again tomorrow when the cape has recharged." DailyTeleports
>   latches that per RS profile against the UTC date (the game's own reset), and
>   TeleportChargeCapture credits it to the item the click named, or to what is worn if the
>   click cannot be read. RouteItem skips a spent hop, travelHint's TeleportItems fallback
>   skips a spent item - between them that was the "use your Ardougne cloak" instruction - and
>   the step overlay lists the hop as "... - out of teleports today" in grey instead of as a
>   "via" line. Clearable from the profile reset, in case a refusal is ever credited wrongly.
> - Deliberately NOT counting successful teleports to predict the last one. A count that drifts
>   high withholds a teleport the player really has, with no way to say otherwise until the day
>   rolls over; the message cannot produce a false positive. The honest cost is one wasted click
>   per day, on the first cloak teleport that fails.
> - Upstream, if it is ever worth a PR: either SP tracks the uses itself (it already intends to
>   gate them) or it accepts a per-request transport exclusion, at which point this store is
>   already the thing that would feed it.
> Tests: DailyTeleportsTest, TeleportChargeCaptureTest, SpentTeleportIsNotTheHintTest,
> RouteItemTest.aTeleportTheGameHasRefusedTodayIsSkipped.

"No seed picked for compost bin" - when any of the items are selected
> RESOLVED. The run panel's warning line walks every ticked type and asks the SEED selection
> what will go in it (RunPanel.nothingToPlantIn -> SeedSelectionStore). A compost bin has no
> seeds at all - Seed.forPatchType(COMPOST) is empty by design, which is also why the bin tab
> shows a fill selector instead of a seed list - so the answer was always "none" and the line
> fired however full the tab above it was. The fill lives in CompostRunStore, which nothing
> here was asking.
> - Bins are now skipped in that loop and asked separately, in their own words: "No fill
>   picked for the compost bins." It is a second sentence rather than a rewrite, so a run with
>   both an unpicked seed and an unpicked fill says both.
> - NOT simply silenced, deliberately: a bin the run could fill with nothing chosen to fill it
>   with is worth saying, because an empty bin still routes (owner's call) and the guide then
>   has nothing to offer there.
> - But gated on there actually BEING a fillable bin. Emptying finished compost is a complete
>   bin run on its own - buckets from the leprechaun, no produce involved - so someone whose
>   bins are all sitting full would otherwise be permanently nagged about a fill that trip
>   never wanted, which is how a warning line stops being read.
> - Both sizes count under the one tick, the same fold RunTypeStore.getSelected makes: the
>   checkboxes only ever carry the normal bin's type, so a fillable big bin alone still earns
>   the line.
> Tests: CompostBinWarningTest (all four cases).

scheduled a run for compost bins only, and only enabled the farming guild, and its telling me
there isn't anything in this run, while standing at the farming guild (at the bank)
> RESOLVED. The Farming Guild has NO ordinary compost bin - its only one is the BIG bin - and
> the fold that turns the single "Compost bin" tick into both implementations existed in one
> place only, RunTypeStore.getSelected. RunPanel builds its own type set straight from the
> checkboxes (getSelectedTypes), so the preview, the destination list AND planner.start all
> planned over COMPOST alone, which matches no patch anywhere in the guild. Everything read as
> an empty run while the bin was standing in front of the player.
> - The fold now lives on the data as CompostBin.coveredByTheBinTick, and both the store and
>   the panel call it. Two copies of a rule like that drift, and the failure when they do is
>   exactly this silent one.
> - RunPanel.binsWantAFill (the "no fill picked" warning) used a third hand-rolled copy; it
>   uses the shared one now too.
> Test: RunPlannerTest.aGuildOnlyBinRunFindsTheBigBin pins both halves - the raw tick matching
> nothing, and the widened set finding the bin.

we're going to need to be a little smarter with our compost runs, we only have 28 inventory
slots, they can't be noted
> RESOLVED. Researched first, and one of the two assumptions was wrong: volcanic ash IS
> stackable, so 25 or 50 of it is one slot and was never the problem. The fill produce is the
> problem - a bin refuses notes and nothing on the supercompostable list stacks, so the item
> count IS the slot count.
> - Consequence worth writing down: THE BIG BIN CANNOT BE FILLED IN ONE TRIP. Thirty un-noted
>   items against twenty-eight slots. (The wiki's one-trip trick, six baskets of apples, makes
>   ORDINARY compost, so it does not help a supercompost fill.) CompostBin.fitsOneInventory
>   states it and a test pins it.
> - The loadout asked for the run's whole total - nine empty bins is 150 pineapples, an
>   instruction nobody could follow, and it drove the bank highlight's withdraw count too. It
>   now asks for one pack-load (the inventory less a slot for the ash when the upgrade is on)
>   and says so: "As many as one trip holds ... your N bins want X items and a pack holds Y.
>   The rest needs another load."
> - The buckets are deliberately NOT reserved out of that budget. Emptying and depositing
>   happen in turns against whatever room is going - CompostBinPlan already takes as many
>   buckets as there are free slots and hands the compost straight back to the leprechaun - so
>   the produce never has to share the pack with them.
> - The guide's fill step no longer says "fill the bin" to someone holding ten: it says what
>   can go in and how far short that leaves them, since a part-filled bin is a fine place to
>   stop and the count is what says you are coming back.
> Tests: CompostBinPlanTest.aShortFillSaysHowShortItIs / theBigBinCannotBeFilledInOneTrip,
> RunLoadoutTest.theFillNeverExceedsOnePackLoad.

those colapsable lists buttons themselves are not rendering right, they're like, locked to the
middle, left aligned, they should be full width
> RESOLVED. The two section headings were stacked in a vertical BoxLayout, which lays its
> children out at their MAXIMUM width and centres them on their alignmentX - both defaulting to
> "as wide as I asked for, in the middle". So the buttons rendered at their text width, floating
> mid-panel, exactly as described.
> - Rebuilt as nested BorderLayouts, which is the shape SeedSelectorPanel already uses: NORTH
>   and CENTER are stretched to the container's full width with no alignmentX or maximum-size
>   fiddling to keep in step as the grids' contents change. The footer (tomato note + ash box)
>   had the same latent problem and was converted with it.
> - The codebase's other idiom for this is setAlignmentX(LEFT_ALIGNMENT) plus a maximumSize of
>   (Integer.MAX_VALUE, preferredHeight) - see PatchTypePanel.row and PageLayout.cap. That works
>   but has to be re-applied whenever the preferred height moves, which for a grid that gains and
>   loses rows on every refresh is a standing liability. BorderLayout has no such upkeep.
> Test: CompostBinPanelLayoutTest lays the panel out at the sidebar's real 225px and asserts each
> heading spans it and starts at x=0. Verified it FAILS against the old BoxLayout version
> ("Supercompost (0) is only 109px of 225"), because a layout test that was never seen to fail
> proves nothing - and nothing else in the suite paints this panel.

the seed style ordering for multiple selections also doesn't work here, it should, and does
volcanic ash apply to regular compost?
> BOTH ANSWERED.
> ASH: no. Wiki-checked - ultracompost is made by using volcanic ash ON SUPERCOMPOST, in the
> bucket or in the bin, and there is no ash route from ordinary compost at all. The way across
> that gap is a compost potion, one dose of which turns a finished bin of ordinary compost into
> supercompost. The model was already right (the ash step is gated on the bin's decoded produce
> being SUPERCOMPOST); what was missing was saying so, which the ash checkbox's tooltip now
> does when a picked fill makes ordinary compost.
> ORDERING: implemented, mirroring the seed grid exactly - insertion-order queue, the 1/2/3
> digit drawn top-right in the game's own font, re-picking sends an item to the back.
> - CompostRunStore holds an ordered list now, stored comma-separated. A single stored id is
>   what the one-pick version wrote and still parses as a queue of one, so an existing profile
>   keeps its choice.
> - The spill is PER BIN, not within one, and that is the one place this differs from seeds. A
>   bin filled with a mixture that is not entirely supercompostable makes ORDINARY compost, so
>   running out of pineapples half way through a bin and topping up with potatoes would quietly
>   cost the tier. CompostBinPlan therefore fills each bin from the first queued item it is
>   actually carrying, and RunLoadout budgets in whole bins of one item for the same reason.
> - A queue that mixes the tiers is allowed and explained rather than prevented: each bin comes
>   out at its own item's tier, which is a reasonable thing to want and a nasty surprise to
>   discover at a bin.
> Tests: CompostRunStoreTest (queue order, lone pick wears no digit, re-pick goes to the back,
> single-id profile still loads, mixed queue is not all-supercompost), CompostBinPlanTest
> (spills to the next fill when the first is not carried, first carried fill wins).

also compostable items were showing up in the gear section in bank filtering, they should show
in seeds now, but I think some of them are also used for protection, so it needs to be listed in
seeds when its for a compost bin, and in protection list when its for protection, both if the
user has both in their plan.
> RESOLVED, both halves.
> - The fill rows were LoadoutItem.Category.COMPOST, and BankLayout groups COMPOST under 'G'
>   with the tools and the buckets - which is right for a bucket of ultracompost coming out of
>   a bin and wrong for fifteen pineapples going into one. New Category.BIN_FILL, grouped with
>   SEED under 'S': bulk produce the trip is for, in a quantity, exactly like a seed. Added to
>   CANNOT_PROCEED_WITHOUT too, which it previously inherited from COMPOST.
> - BOTH LISTINGS: an item serving two purposes now appears in both regions. I first
>   "corrected" this the wrong way, deduplicating across regions on the strength of a comment
>   already in itemsIn claiming "the same id in two slots is not a layout Bank Tags can
>   honour". THE COMMENT IS WRONG and the owner said so from experience. Checked against the
>   client sources: Bank Tags has a DUPLICATE_ITEM menu option for exactly this, Layout.count
>   (id) exists to find ids placed more than once, TabInterface gates a menu entry on
>   count(id) > 1, and LayoutManager.drawItem fills every slot from the bank's own count -
>   which is how a combat layout shows the same food in several places. The dedup was removed
>   and the comment rewritten with that evidence.
> - Within a region it is still one slot per id: two payment rows naming the same berry are
>   one lot of berries.
> KNOWN, and worth watching in play: the withdraw NUMBER on a slot is summed per item across
> every loadout row (BankHighlightOverlay.refreshCounts, which does that deliberately for
> payments shared by two crops). So a shared item shows the same combined total in both of its
> slots rather than its per-purpose share. Splitting that needs slot-keyed counts rather than
> item-keyed; say the word if it reads as double-counting at a real bank.
> Tests: BankLayoutTest.aBinFillLaysOutWithTheSeeds /
> anItemServingTwoPurposesAppearsInBothBlocks / oneItemTwiceForOnePurposeStillTakesOneSlot.

"skipping fishbowl helmet, diving apparatus - you have none." I think they're on my boat
actually? one of them at least? research that
> RESOLVED - the owner was right, and the wiki is explicit. A boat's CARGO HOLD stores diving
> gear "taking no space at all", and once stowed it is "accessible from all boats when stored in
> any of them". A December 2025 update even records "the fishbowl helmet and diving apparatus
> are no longer deleted from the bank when deposited into the cargo hold" - stowing them is an
> ordinary supported thing to do, and a hold is exactly where a sailing player leaves them.
> The loadout was looking in the bank and the pack only, so it announced "you have none" about
> the one place those two items are least likely to be.
> - The holds ARE readable: InventoryID has SAILING_BOAT_1_CARGOHOLD .. _5_CARGOHOLD (963-967),
>   five because a player can own five boats. New BoatHolds store reads all five off
>   ItemContainerChanged and persists per profile like BankContents does, because a hold is only
>   sent to the client when it is opened - without a memory the answer would be "not looked" on
>   every login and the false alarm would return.
> - Recording is ADDITIVE across the five, unlike the bank's wholesale replace: the game sends
>   one hold at a time, so replacing would have opening boat two forget boat one. Nothing here
>   is a count, so the only staleness is remembering gear since removed, which re-reading that
>   hold corrects.
> - New LoadoutItem.Need.ON_BOAT, the sea-going counterpart of AT_LEPRECHAUN: not WITHDRAW (no
>   bank highlight, no withdraw line) and not MISSING (no false alarm). Both diving pieces and
>   the medallion go through one divingNeed(), which asks carried -> hold -> bank and only says
>   MISSING once at least one of the two stores has actually been read.
> Tests: RunLoadoutTest.divingGearInACargoHoldIsNotReportedMissing / divingGearNowhereIsStill
> Missing / carriedDivingGearOutranksTheHold.

also at the seaweed patches, seaweed spores spawn in the area periodically, we should chatbox
notify that one has spawned and highlight it on the ground so it catches the players eye while
they're there, this is one of the few ways to get these and its best to just pickup what you
planted on the run so you never run out
> BUILT. Researched first: spores "may randomly appear when diving in shallow water within the
> underwater area beneath Fossil Island. 1-3 seaweed spores can spawn, and they will despawn in
> 30 seconds", they appear individually per player like marks of grace, and they "instantly
> disappear upon logging out". Thirty seconds with your eyes on a patch object is exactly the
> window a player misses, which is what makes the callout worth making.
> - SeaweedSpores watches ItemSpawned/ItemDespawned for the spore (gameval SEAWEED_SEED - the
>   cache's name for it; Seed.SEAWEED already plants the same id) and speaks once per spawn.
> - NOT a run step, deliberately. A spore is an interruption with a half-minute clock, not work
>   to queue behind raking and planting - so the guide keeps saying whatever it was saying and
>   this speaks over the top and marks the tile.
> - Region-gated to the underwater area (15008) so a spore on a bank floor cannot set it off.
>   Ownership is deliberately NOT filtered, which is the opposite of DroppedProduce: spores are
>   spawned per player and the wiki notes "all players whose spores spawn on that tick will
>   spawn at the same tile", so ownership flags are not something to lean on. Item id plus
>   region is tight enough - nothing else down there is a spore.
> - The tile is marked in the game's own ground-item green rather than the guide's highlight
>   colour, so it reads as a chance passing by rather than as the current step. Drawn before
>   the step and regardless of whether there is one.
> - Both halves answer to one switch AND to guided mode: the mark is drawn by GuideOverlay,
>   which renders nothing with guided mode off, so speaking anyway would be half a feature and
>   a line of chat from a plugin told to be quiet. New setting "Call out seaweed spores",
>   default on.
> Tests: SeaweedSporesTest x8 (announced and remembered, wrong region ignored, wrong item
> ignored, despawn forgets by handle, scene reload clears, batch, switch off, guided mode off).

full inventory at the farming guild for a compost only run, I'm not getting prompted to go to
the bin because I don't have all of the items (you physically can't for the big bin). Also
Compost should take precidence at the farming guild when checked, since this is not a
traditional patch and can't be assinged as a contract, also to clear space for the rest of the
runs and restock compost. Contract precidence at the farming guild should not take place if
farming contract is not selected for the run.
> RESOLVED - three faults, and the first two both had to go before the bin appeared at all.
> 1. THE GUILD WAS BEING HELD CLEAR FOR A CONTRACT NOBODY ASKED FOR. contractComesFirst gates
>    on the setting and the region but never on whether the contract is IN THE RUN, and with
>    none assigned its "nothing claimed" branch returns an empty list - so every guild patch,
>    bin included, was withheld to keep the ground free for a contract Jane had not given out.
>    It now also requires the contract's own run line to be ticked (contractIsInTheRun), which
>    is the owner's rule and covers both "no contract" and "assigned and being ignored".
> 2. AND IT WITHHELD THE BIN. A compost bin can never be a contract's patch - Jane cannot
>    assign one - so it competes for no ground and has no reason to wait. Bins are now exempt
>    from the hold-back on every branch.
> 3. THE BIG BIN COULD NEVER BE SUPPLIED. addFills rounds the ask down to whole bins so no bin
>    ends up half one item and half another, but the big bin's thirty un-noted items exceed a
>    28-slot pack, so (held / 30) * 30 was zero every time: the row went MISSING, the supply
>    leg it gates never completed, and the run sat at the bank being told to fetch what it
>    physically cannot hold. Rounding is now skipped for any bin bigger than a pack - the
>    rounding is meaningless there anyway - and the row says it fills over two trips.
> PRECEDENCE: binsFirst sorts a bin to the very front of its stop, applied AFTER contractFirst
> so it outranks even the contract. The reasoning is inventory rather than importance: emptying
> the bin frees the pack and restocks the compost every other patch at that stop is about to
> want, and doing it last means arriving at the herbs with fifteen slots of produce in hand and
> no ultracompost. It is also free to be wrong about - a bin shares no tool, seed or payment
> with anything else at the stop, so putting it first takes no click away from another patch.
> Tests: GuildBinPrecedenceTest x4 (guild open without the contract, unticked contract does not
> hold, ticked contract still holds but not the bin, bin sorts first),
> RunLoadoutTest.theBigBinIsSuppliableEvenThoughAPackCannotHoldAFullFill.

still having me try to do my farming contract when its not checked for the run / check the logs
> RESOLVED, and the log named it outright: "Contract needs collecting for; routing to a supply
> point" on a run logged as "1 stops [Farming Guild=4922] ... seeds picked for this run: none" -
> a compost-only run being sent to a bank to collect a poison ivy contract's seed.
> The earlier fix gated GuideTracker's guild hold-back on the contract's run line. This is a
> SECOND path, in RunPlanner, and it is the one that was actually doing it:
> reviewContract() adopts a contract's patch into a live run, widens the run's covered types to
> match (runTypes.add), and calls collectForTheContract() to route to a supply point. It gates
> on "active" and "a contract crop exists" and nothing else - it deliberately BYPASSES inTheRun,
> because its whole job is to adopt a patch the plan does not have, and it tests the patch's
> group and actionability, neither of which knows what the player asked for.
> - It now asks contractIsInTheRun(wanted) first, the same question GuideTracker asks. The
>   contract's line only exists while one is assigned and its patch belongs to the account, so
>   unticked covers both "no contract" and "assigned and being ignored".
> - Five existing RunPlannerTest cases were exercising reviewContract without the tick and
>   rightly started failing; they now tick the line, which is a precondition of the behaviour
>   they are testing.
> Test: RunPlannerTest.anUntickedContractIsNotAdoptedMidRun asserts both halves - the patch is
> not pulled into the stop, and the run's covered types are not widened.

our seedbox orange highlighting logic, should work literally all of the time, right now I have
seaweed spores and elkhorn frag in my inv and its not prompting during a navigation step on a run
> NOT REPRODUCED FROM HERE - instrumented instead, plus one real bug found in the owner's data.
> What was checked and RULED OUT:
> - The gate is not the navigation step. The orange fill-box highlight is drawn BEFORE the
>   step==null return in GuideInventoryOverlay.render, so travel legs already reach it.
> - Not the item ids. SEED_BOX_IDS matches SeedCapture.isSeedBox (closed and open forms).
> - Not saplings. Seaweed spores and coral frags have saplingItemID -1, so isSapling() is false
>   and neither is excluded.
> - Not the six-kinds rule for THESE two items. Read from the owner's own profile store: the
>   seed box record holds seaweed spore x81 (21490) and elkhorn frag x60 (31511), so the
>   "kind already boxed always fits" branch should pass for both.
> REAL BUG FOUND, though not the one reported: that same record holds NINE kinds, and the wiki
> is clear the box holds six. The box model has drifted - stale kinds linger until an Empty
> proves otherwise (see the boxProvedEmpty heal in relearnInventoryFromClient). Any count above
> six there is the model, not the game, and while it stands no genuinely NEW kind can ever
> light, because kindsBoxed >= SEED_BOX_KINDS is permanently true.
> WHAT IS LEFT is the live inventory count, which nothing persists, so it cannot be read after
> the fact. GuideInventoryOverlay now logs the fill-side decision once per distinct answer -
> the run, which loose seed carried it, the box's recorded kinds, and every loose seed in the
> pack - exactly as the take-seeds-out side already did for the same "it never lights" report.
> NEXT TIME: reproduce and the log line names it, e.g.
>   "Seed box fill highlight: OFF - box holds 9 kinds (limit 6), loose seeds in the pack: none"
> which would say the store never saw the spores, against
>   "... loose seeds in the pack: SEAWEEDx3" which would say the six-kinds rule is at fault.

the coral nursery I'm being routed to is giving "desination unreachable" in SP, I think we
actually want to navigate to and highlight this object on land, which is a set of stairs right
to the nursery, diving gear needs to be put on first, flippers have no effect here either it
seems. Steps (ID: 57904 X: 3272 Y: 2463)
> RESOLVED, and unrelated to the seed box report above.
> Shortest Path was right and the plugin was asking the wrong question. It routes over a
> collision map of places you can stand; a coral nursery is on the seabed, so no path to it
> exists and "destination unreachable" is the honest answer. getRouteTargets was handing it the
> patch's own tile ring.
> - New UnderwaterApproach: for CORAL the run asks for the LANDWARD approach instead - the
>   steps at (3272, 2463) - which is a real walkable tile. The router takes the player to the
>   shore and the last two actions, gear on and click the steps, are theirs. Same division of
>   labour the plugin already uses for a fairy ring or a spirit tree.
> - GuideOverlay outlines the steps whenever the run has an underwater stop left, BY OBJECT ID
>   (57904) rather than by menu wording - the id was read off the client at the spot, so there
>   is nothing to parse and nothing to get wrong. Drawn independently of the step, because a
>   travel leg has no step and this click is the end of one.
> - Coordinates and id are from play rather than a wiki page, which is worth recording: they
>   describe the object actually clicked.
> DELIBERATELY ONE ENTRY. The seaweed patches are underwater too but are reached differently -
> a rowboat out to sea and a Dive option, no steps - and nobody has reported the router refusing
> them, so no approach is invented for them. A wrong approach point is worse than none: it would
> route somebody confidently to the wrong shore.
> NOT MODELLED: flippers, which the owner reports have no effect there. Nothing in the plugin
> mentions them, so there was nothing to correct - noted so the next person does not add them.
> Tests: PatchLocationStoreTest.aCoralNurseryRoutesToItsLandwardSteps /
> anOrdinaryPatchStillRoutesToItself.
> FOLLOW-UP, owner clarifying the mechanic: "you're on the dock when you click that, it's steps
> leading underwater, you're intended to put on your diving gear before going under water."
> So the routing half above was right and a second instruction was missing entirely. WEARING the
> gear is what counts, and nothing said so: the loadout's job is to get the suit out of the bank
> and it is satisfied the moment the pieces are in the pack - which is exactly the state the game
> punishes, since taking the steps down holding them drowns you or turns you back.
> - UnderwaterApproach.gearToWear() lists the medallion FIRST, because it replaces the pair -
>   naming all three would tell a medallion owner to wear a fishbowl helmet they replaced.
> - GuideInventoryOverlay lights whichever piece is loose in the pack while the run has an
>   underwater stop, in the GUIDE colour rather than an ambient one: unlike the bucket and the
>   box this is not "when convenient", it is the next thing to do before the steps. It goes out
>   the moment the piece is equipped, because getInventoryCount counts the pack alone.
> - GuideStepOverlay adds "Wear your diving gear before the steps down." to the travel panel,
>   not as a step - the router stops at the dock and the last two actions, wear then descend,
>   are the end of the travelling. The order only works one way round, so the line says it.
> Tests: PatchLocationStoreTest.theDivingGearListLeadsWithTheMedallion / onlyCoralHasALandward
> Approach (pinning that seaweed is deliberately absent - rowboat and Dive, not steps).
> FOLLOW-UP 2: "when we click that we start trying to navigate back to it via fairy rings, need
> to stop that. And patches aren't getting highlighted to do the run, Patches are (ID: 58711,
> and ID:58710)"
> Two faults, both from the divert being half-finished.
> 1. THE ROUTE CLIMBED BACK OUT. The approach point is on the dock, and it was substituted
>    unconditionally - so after the dive the router did exactly as asked and plotted a course
>    back UP to it, fairy rings and all. The substitution now also asks WHERE THE PLAYER IS:
>    UnderwaterApproach.stillWanted is true on the dock's own region (derived from the approach
>    point, so the two cannot disagree) and while the position is unknown, and false once they
>    are below. Standing at the nursery needs no route at all.
>    The divert moved from PatchLocationStore to RunPlanner.routeTargetsFor to do this - the
>    store has no idea where the player is, and pretending otherwise is what made it
>    unconditional. The store is back to answering with the patch's own tiles for every patch.
> 2. THE NURSERY PATCHES NEVER HIGHLIGHTED. Everything the outline machinery does keys on the
>    patch varbit, and nothing on the seabed carries one - so scanForPatchObjects found nothing
>    and the tile fallback needs a learned position that is captured the same varbit way.
>    GuideOverlay now falls back to scanning by OBJECT ID (58710, 58711, from the client at the
>    spot) before giving up. The two nurseries cannot be told apart from the objects alone, so
>    both are marked - two adjacent nurseries read as one place to go, the same call the
>    dropped-crop highlight already makes for stacks on neighbouring squares.
> Tests: RunPlannerTest.theCoralDivertAppliesOnTheDockAndNotBelowIt / theNurseryObjectsAreKnown
> ById, PatchLocationStoreTest.theStoreAlwaysAnswersWithThePatchesOwnTiles.
> FOLLOW-UP 3: "rowboat to seaweed is doing the same thing, ID: 30919 X: 3761 Y: 3898"
> The seaweed rowboat is now an approach too, with the same evidence the steps had - object id
> and coordinates read off the client at the spot. The earlier note claiming seaweed was
> deliberately absent "because nobody has reported the router refusing them" was true when
> written and stopped being true here; the restraint it describes is still the rule, it just no
> longer applies to this patch.
> - stillWanted was comparing against a single hardcoded CORAL_DOCK_REGION. Each Approach now
>   derives its OWN region from its own point, so the coral dock and the Fossil Island shore are
>   judged separately - standing on one is not standing at the other, and with one constant it
>   would have been.
> - Nothing else needed changing: the divert, the object outline, the "wear your diving gear"
>   line and its inventory highlight are all keyed on "this run has an underwater stop", so
>   seaweed picked them all up for free.
> - Seaweed patches were NOT given object ids. Its patch objects carry the farming varbit like
>   any ordinary patch (core decodes "Seaweed patch[Rake,Inspect,Guide]" through the varbit
>   states), so the normal outline scan already finds them - and the owner reported only the
>   routing this time, not a missing highlight.
> Tests: PatchLocationStoreTest.bothUnderwaterFamiliesHaveALandwardApproach /
> eachApproachIsJudgedAgainstItsOwnShore.

when you run out of ultracompost fall back to supercompost and then regular compost and so on,
but notify in the chatbox and info box that you've been downgraded, do your compost runs.
(follow-up: "make it a setting actually")
> BUILT, behind a setting - "Fall back to weaker compost", OFF by default. Off is the right
> default even though the behaviour is wanted: which bucket gets spent is a decision made on the
> tab, and quietly spending a different one is the plugin overriding the player without saying
> so. The switch is what makes the substitution consented to.
> - CompostTier.weaker() / bestAvailableAtOrBelow() walk the enum's own order, so the ladder is
>   the ranking rather than ordinal arithmetic scattered over three call sites.
> - Only DOWNWARD. Treating with something stronger than was asked for spends a scarcer bucket
>   than the player chose, which is their call.
> - "In reach" means carried or in the leprechaun's store, because at a patch those are the same
>   thing - he is standing there and the withdrawal is one click. A tier that exists only in a
>   bank is not reachable from a patch and does not count.
> - THE IMPORTANT GUARD, and a regression I introduced and caught: with NOTHING in reach the
>   chosen tier stands. The first version returned NONE, which silenced the compost step
>   entirely - so "withdraw ultracompost from the leprechaun", exactly right when his store has
>   not been read yet, became nothing at all. Only a tier actually seen to be there displaces
>   the one that was asked for. Five existing GuidePlanTest cases caught it.
> - Notified once per tier per group, not per patch: a herb run treats a dozen patches from one
>   stack and a dozen identical lines is a spam bug. Chat says it when it happens; the infobox
>   carries it standing, above the ready and withdraw lists, for anyone not watching chat. Both
>   name the fix - "Worth a compost bin run" - rather than only the symptom.
> Tests: GuidePlanTest.runningOutOfUltraFallsBackToTheSuperYouAreHolding /
> withOnlyOrdinaryCompostItFallsAllTheWayDown / aStrongerTierIsNeverSubstitutedIn /
> nothingInReachLeavesTheChosenTierAlone.

the run list is getting long, we should alphabetize it / and dim the text on unchecked patch types
> BOTH DONE, and they are the same complaint: the list had outgrown being scanned.
> ALPHABETICAL: runOptions used the enum's order with the harvest-only pairs grouped at the end.
> That grouping was a LAYOUT argument - a pair has to sit side by side on one row - and it was
> already hedged, because RunPanel.buildTypeBoxes pads a fresh row for every pair precisely so
> the layout could not depend on the order. So sorting was free; it costs the odd blank cell and
> nothing else.
> - A pair still moves as one: the full run and its harvest-only counterpart stay adjacent
>   wherever the name lands them, since two lines about the same patches reading as unrelated
>   entries is what the grouping existed to prevent.
> - The compost bins JOINED the alphabet - "do my bins" is the same kind of standing choice as
>   "do my herbs". The CONTRACT stays pinned last; it is a job rather than a patch type and its
>   own note explains why it must not sort in with them.
> DIMMING: unticked lines are drawn in a dimmer grey, re-applied on every toggle as well as at
> build time so a line lightens the moment it is ticked. Dimmed rather than hidden, deliberately:
> an unticked line is the thing you click to change the run.
> Tests: RunOptionCoverageTest.theListIsAlphabeticalApartFromTheContract /
> aHarvestOnlyLineSitsWithItsFullRun / theContractStaysAtTheEnd (replacing the old
> typesWithAHarvestOnlyVariantComeLast, which pinned the ordering being replaced),
> RunListAppearanceTest.untickedLinesAreDimmerThanTickedOnes / anUntickedLineStaysLegible -
> asserted as a contrast relationship rather than against hex values, so a palette change stays
> free and a change that flattens the two back together does not.
> FOLLOW-UP: "weird spacing on the right column, go top down on the sorting on the left column
> then the right column please" (with a screenshot).
> The screenshot showed both halves of it: the alphabet ran ACROSS the rows - Allotment | Anima,
> Belladonna | (blank), Bush | Bush (H/O) - so the names went sideways while the eye went down,
> and the blank beside Belladonna was a filler cell.
> - buildTypeBoxes now places the components TRANSPOSED: the left column holds the first half of
>   the list top to bottom, the right column the second, so an alphabetical list is read the way
>   an alphabetical list is read. GridLayout still fills row by row; the ORDER handed to it is
>   what changed.
> - The gaps are gone with it. They came from starting a fresh row for every full-and-harvest-
>   only pair so the two halves sat side by side; down a column a pair is naturally adjacent,
>   one directly under the other, so no padding is needed. At most one cell is blank now - the
>   foot of the right column on an odd count.
> - ONE REAL BUG the tests caught rather than inspection: the split can land BETWEEN a pair,
>   putting the full run at the foot of the left column and its harvest-only line at the head of
>   the right - as far apart as the grid can put them, which is exactly what the pairing exists
>   to prevent. With bushes and cactus hidden the boundary fell between the fruit trees. The
>   split now nudges down a row when it would divide a pair.
> Tests: ProtectedTabTest.harvestOnlyLinesSitBesideTheirFullRun and pairsStayAlignedWhateverIs
> SwitchedOff rewritten for vertical adjacency (two cells apart, not one), plus a new
> atMostOneCellIsBlankAndItIsTheLast pinning that no gap can reappear mid-list.

Bush and Cactus always stay bright when unchecked
> RESOLVED, and the report named the mechanism without meaning to: Bush, Cactus and Fruit tree
> are the only PAIRED types, and untick() is only ever called on a pair's counterpart.
> Ticking a full run unticks its harvest-only line (and vice versa) through
> JCheckBox.setSelected - which does NOT fire an ActionListener. Only the box the player
> actually clicked ran the handler that recolours it, so the counterpart was left unticked and
> still drawn bright. untick() now applies the colour itself.
> Verified the test fails without the fix rather than assuming it: stubbed the recolour back out
> and watched aCounterpartUntickedByThePanelDimsToo fail on the brightness assertion.
> The test drives untick() directly rather than the checkbox's listener, which would run a whole
> panel refresh and need run state the fixture has no reason to invent.
> Test: RunListAppearanceTest.aCounterpartUntickedByThePanelDimsToo.

We had a test for this before but its not working again, when I get a contract from jane, sometimes it will be something I haven't check-healthed on yet, however, if its a tree I need to go get log basket/forestry basket and an axe from the bank first so I can clear it

> Fixed, and the earlier test was not the one that broke - the loadout was right all along.
> `RunLoadoutTest.aTreeContractOnAnUnclearedPatchAsksForTheAxeAndTheBasket` pins that a tree
> contract on a patch holding a grown, unchecked tree lists both the axe and the forestry
> basket. It passes, and passed before any of this.
>
> What failed was the *trip back*. Two causes, both about a mid-run contract:
>
> 1. `collectForTheContract` gated everything on `needsSupplyTrip()`, which reads the
>    `withdrawOutstanding` flag pushed once a tick from the withdraw list. At the moment it is
>    called - a few lines after `reviewContract` widened the run's types - that flag still
>    describes the run as it was BEFORE the contract existed, so it cannot know about the axe.
>    It answered no and returned without even setting `supplyOwed`, so the deferred pickup had
>    nothing to find either. Confirmed from client.log: "Contract needs collecting for" appears
>    zero times in a session where TREE demonstrably joined the run's types (20:20 without,
>    20:25 with). The debt is now recorded before the stale question is asked.
> 2. `reviewSupplies` - the only per-tick re-arm - asked `ToolNeeds.anyOnlyInBank`, and the axe
>    has never been a ToolNeeds tool; it is a RunLoadout row, which that class's own javadoc
>    says. So nothing could see it. It now also reads `toolsLeftToWithdraw`, pushed like the
>    other flags. Being a poll rather than a one-shot is what makes it right where (1) was
>    wrong: a stale flag is fixed by asking again a tick later.
>
> Deliberately narrowed to TOOL rows. A seed or a payment must not yank the player to a bank
> mid-stop - the run can press on and skip that patch, and "standing on work beats going
> shopping" is a settled decision. A tool is different: without an axe the tree cannot be
> chopped, and no amount of standing there changes it.
>
> The forestry basket needs no separate work - it is a STORAGE row already offered for a tree
> run, so it is on the list and highlighted on the trip the axe now earns.
>
> Tests: RunPlannerTest.aToolTheWithdrawListWantsMidRunDivertsToABank,
> anOrdinaryWithdrawalDoesNotDivertMidRun, aWaivedLegIsNotReopenedByAMissingTool.

not prompted to put watermelons in the big bin at farming guild after harvesting while big bin is empty, also at adrougne (at least) I'm getting prompted to note my watermelon instead of using it on the bin

> Two causes, and the log pinned the first.
>
> 1. **The bins were only ever put into stops at Start.** `addOpportunisticBins` runs inside
>    `planStops`, which runs in `start()` and never again - so ticking "fill bins from your
>    harvest" after pressing Start did nothing for the rest of the trip, silently, and a bin that
>    finished composting mid-run could never join either. client.log 20:40:17 has
>    "Harvest: Watermelon x19 from Ardougne South" with `10548.4775` sitting EMPTY, watermelon
>    (5982) on the fodder list and the minimum only 5 - every condition met and no step, because
>    the bin was not in the stop to be asked. Now a per-tick `RunPlanner.reviewBins()`, the same
>    shape as `reviewContract` and using the same `adopt`. The rule is unchanged: a bin joins a
>    stop the run is **already** making and never creates one.
> 2. **The leaving note ate the fodder.** `GuidePlan`'s mid-harvest note already stood down for a
>    crop a bin wants; `appendNoteBeforeLeaving` did not, and it is the one that says "note your
>    watermelons before moving on". A bin refuses noted items, so the two instructions cannot both
>    be followed and following the note makes the bin impossible. Being later in the step list is
>    no protection when the crop is gone by the time the bin's step is reached. It now skips only
>    the crop a bin at that stop is waiting for; everything else is tidied up exactly as before.
>
> Not a bug: the Farming Guild's big bin reads `BIG_SUPERCOMPOST / GROWING` in the profile - closed
> and composting - so silence there is correct. If it was empty at the time and still said nothing,
> that is cause 1 again, since the guild bin joins at Start via the Compost tick rather than the
> poll. Worth re-checking now the poll exists.
>
> Tests: RunPlannerTest.aBinJoinsAStopWhenFodderIsSwitchedOnMidRun,
> thePollStillDoesNotLetABinEarnAStop, LeprechaunErrandOrderTest.theLeavingNoteLeavesTheBinsFodderAlone,
> anotherCropIsStillNoted, aBinWithNoRoomDoesNotHoldTheNoteBack.

for some reason the catherby teleport step always drops out of the infobox and nexus list when entering the POH

> The house is not the cause, and the log is what rules it out:
>
>     20:47:09  None of Shortest Path's hops for "(unnamed leg)" mapped to the furniture here
>               hops: [Teleport to House, Enter Catherby Portal - Catherby Portal]
>     20:47:14  RegionUpdateEvent{currentRegionId=41385, previousRegionId=12083}   <- entered the POH
>
> The leg was already **"(unnamed leg)"** five seconds before the house was entered. Entering it
> is just when you notice, because that is when the nexus row is meant to light.
>
> The destination name is inferred from Shortest Path's reply, and the reply is briefly
> unavailable far more often than it looks: `setTargets` wipes `currentDestinations` on every
> re-route, `onPluginMessage` discards unsolicited replies outright while the player is instanced,
> and a reply can carry hops with no landing worth matching. That was harmless while nothing
> depended on the name - but the portal nexus row and the jewellery box line are matched **by
> destination**, so a nameless leg silently stops highlighting the thing you are meant to click.
>
> `destinationStop` now asks the run first. Since the destination latch landed, the planner
> *knows* which stop it committed to and does not have to infer it; `RunPlanner.committedStop()`
> is that answer, and the reply-derived logic stays as the fallback for a leg not yet committed.
> No new guessing: the commitment is only ever set from an unambiguous reply in the first place,
> and it clears the moment the stop leaves `getRemaining()`.
>
> Also returns null on the supply leg, whose destination is a bank rather than a stop.
>
> Tests: RunPlannerTest.aCommittedLegIsStillNameableWithNoRouteReply,
> aFinishedStopIsNoLongerTheCommittedOne, theSupplyLegIsNotNamedFromTheCommitment.

2 things, I think we're going back to cactuses too soon, I just got sent back to the farm guild one mid run, after already harvesting it 20 minutes prior, only now receiving 1 cactus spine, I think they take longer to fully regrow. Second issue is that hold shared plots until all are ready isnt' working, check the logs, just got routed to ardougne farm patch again after only limpwurts were ready

> One cause, two symptoms. The data was already right - a cactus regrows a spine every twenty
> minutes and holds four (`Produce.CACTUS` regrowTickrate 20, harvestStages 4), and
> `heldForRegrowth` already says to wait for the fourth. Nothing needed re-timing.
>
> **The two hold rules gated stop *creation* and had no say in stop *revival*.** `clusterHeld` and
> `heldForRegrowth` were consulted by `planStops` and `actionableByGroup` only, while `isComplete`
> - which decides whether a stop is still in `getRemaining()` - asked `isActionable` alone. A stop
> is completed after you service it and comes back the instant one of its patches turns actionable
> again, which is exactly the moment both rules exist to ignore:
>
> - Ardougne: you replant, the limpwurt ripens in twenty minutes and the herb beside it takes
>   eighty. The flower alone un-completed the stop and fetched you back.
> - The guild cactus: picked clean, one spine back twenty minutes later, and that single spine
>   un-completed the stop.
>
> `isComplete` now applies both holds. Note this also means a wholly-held stop reads complete, so
> a run whose only remaining work is a plot it is waiting on will end rather than sit open for
> eighty minutes - which is the right answer, and is what "hold until ready" asks for.
>
> The `Shared plots:` diagnostic is logged only at `start()`, which is why the log shows Ardougne
> "[free to visit]" at 20:18 with everything HARVESTABLE and nothing at all about the later
> revival. Worth knowing when reading it: it describes the plan, not the run.
>
> Tests, each checked to actually fail with the fix removed rather than assumed to work:
> RunPlannerTest.aHeldPlotStaysHeldWhenOnlyOnePatchRipens, aHeldPlotReturnsWhenTheWholePlotIsReady,
> aPartlyRegrownCactusDoesNotFetchYouBack.

great conch is still routing us to that random table and not the stairs / try to move the coodinates 1 tile west / oh and its trying to have me rake weeds there, weeds can't exist in the coral nursery / the coords for the great conch calquat patch are at X/Y: 3128,2405

> Two causes, and the tile-west guess was right about the first one.
>
> **The steps are not a tile you can stand on.** Shortest Path only ever finishes a search by
> stepping ONTO a target tile, and the one tile written down was the object's own - a set of
> steps leading into the sea is no more standable-on than a tree. This is the identical fault
> `PatchLocationStore.getRouteTargets` rings a bush for, so the approach is ringed the same way:
> all eight tiles, west included.
>
> **The calquat was bidding against it with a made-up tile.** `getLocation` never returns null -
> an unvisited patch falls back to its region's centre, which for the Conch is (3168, 2400), the
> table. Honest as a rough guess when it is the only target; fatal in a set beside an approach
> read off the client, because the router takes the cheapest of what it is handed and cannot
> tell which entries were invented. The deck is nearer than the steps, so the guess won.
>
> Two changes: `routeTargetsFor` only offers a dry patch whose location is *known* (learned or
> pinned) rather than assumed, and the calquat now has a pin - (3128, 2405), given from play.
>
> Worth writing down: that tile is in region **12325** while the patch is filed under **12581**.
> The Conch is thirteen regions of ship with every varbit on one of them, so
> `everySeededCoordinateLandsInTheRegionItBelongsTo` rejected a coordinate that is simply
> correct. It now asks `FarmingWorldData.getRegionsForLocation`, which is the same evidence and
> is a check the multi-region cases can pass.
>
> The rake step was **not** a bug. CORAL is already excluded from raking; that step was the
> calquat's, and the calquat is real. What made it read as absurd was being offered while
> standing at the nursery, which is the same 12581-vs-13194 disagreement in another costume.
>
> Tests: RunPlannerTest.theStepsAreOfferedWithTheTilesAroundThem,
> theConchCalquatIsRoutedToItsPinnedTile, aLearnedDeckPatchIsRoutedToAlongsideTheSteps;
> PatchLocationStoreTest.aPinIsKnownWhereARegionCentreIsNot. The first two were checked to fail
> with the fix removed, and the failure printed WorldPoint(x=3168, y=2398) - the ring around the
> table.


right now I'm out of enough giant seaweed to protect the east patch and its promtping me to pay "Pay the farmer 5 elkhorn to protect the elkhorn" That isn't even a thing

> `GuidePlan.addProtectionStep` built the sentence from `payment.getProduce()`, which is the crop
> being *protected*. Every payment on that branch bought a thing with itself - "2 potato to
> protect the potato", "10 watermelon to protect the watermelon". Wrong everywhere, visible only
> here, because the planting sequence words its own payment step and this branch fires only when
> you come back to something already in the ground and still unpaid.
>
> Fixed with `ItemNames`, which exists for exactly this and already has every ProtectionPayment
> item id read into it at login. Its own class comment makes the case: half these payments are
> baskets and sacks, where the constant reads "basket tomato 5" and the game says "Basket of
> tomatoes".
>
> Test: GuidePlanTest.thePaymentStepNamesThePaymentAndNotTheCrop.


still not getting prompted to put watermelons on the regular compost bins with "fill bins from your harvest" checked and watermelons selected, its actually trying to have me still harvest more like my inventory isn't full

> The two halves are one dead end. A bin waiting to be fed is ordered **last** at its stop, which
> is right - you cannot fill it from a harvest that has not happened. But the pack fills up
> *during* that harvest, and at that moment three things are true at once: the harvest cannot
> continue, the leprechaun's note is suppressed precisely because a bin here wants the crop, and
> the bin's own step is queued behind the harvest that is stuck.
>
> `binRank` now sends a hungry bin to the **front** the moment the pack is full and it will take
> what is filling it. Everywhere else it still goes last.
>
> Second, smaller hole in the same place: "a bin here wants this crop" was an open bin plus a
> crop on the fodder list, which is not the same statement as "the bin will take this". Below a
> third of the bin `CompostBinPlan` declines to offer a token fill - so three watermelons and a
> full pack answered yes to the suppression while the bin produced no step at all. It now asks
> `CompostBinPlan.fodderFor`, so there is one rule about what a bin will take, in the class that
> acts on it.
>
> Tests: GuildBinPrecedenceTest.aFullPackSendsYouToTheBinYouCanFeed (verified to fail without
> the fix), aBinStillWaitsWhileThereIsRoomToHarvest,
> aFullPackOfSomethingTheBinRefusesDoesNotMoveIt.


the seedbox issue with counts is still a problem, seedbox full, highlighted orange

> Found where the drift comes from rather than only how to heal it.
>
> **The Fill derivation credited the box with anything that left the inventory inside its
> two-tick window.** Banking the rest of your seeds a moment after filling the box looks
> identical to a Fill, and so does planting one. The account this was found on has a single
> whiteberry seed in its modelled box and 140 in the bank - which is the shape of a bush seed
> going in the ground, not in a box.
>
> Two rules the box cannot break, both hard rather than heuristic: a Fill takes the **whole**
> loose stack of a kind it accepts (there is no per-kind limit), so leftovers in the pack mean
> something else moved them; and it can never make a seventh kind.
>
> **And why it stuck.** Six kinds is legal, so `boxCannotBeRight` never fires on a full box, and
> the old suppression window discarded nearly every real read the game offered - a container
> event fires when the container changes, and a Fill or Empty is what changes it. A lagged read
> is *by definition* the contents from before the click, so that is now the test rather than a
> two-tick guess. When a read is taken, the pending action is disarmed so its delta is not added
> on top.
>
> Two existing tests asserted the old window and were rewritten rather than deleted: their
> fixtures fed a "stale" copy that was a completely different box (one cabbage where the model
> held potato and onion), which nothing in the game produces. Pinning the time window instead of
> the thing the window is for is what let an informative read be thrown away with it.
>
> Tests: SeedBoxDriftTest.aPartialDepartureIsNotCreditedToTheBox, aFillCannotAddASeventhKind,
> aReadThatDiffersFromThePreClickBoxIsTakenAsTruth, plus the two rewritten ones.


ope, got sent to a calquat patch without turning into a sapling first, seeds for trees need to be turned into saplings before planting

> `seedAtHand` was right to say no - only the sapling is plantable - but the whole planting
> branch then returned in silence, so the player stood in front of the patch holding the thing
> they came for and was told nothing. That is the failure shape this codebase keeps hitting:
> arrived, and nothing said.
>
> New `POT_SEED` step, emitted only when the raw seed is in the pack and the sapling is not. It
> is a diagnosis rather than a click - a plant pot of soil is a bank errand and the sapling then
> needs watering, so there is nothing to do at the patch. `RunLoadout` already carries "needs
> potting into a sapling first" as a reason on its bank row, which is where it is cheapest to
> act on; this is what gets said when that was missed.
>
> Tests: GuidePlanTest.anUnpottedTreeSeedSaysSoAtThePatch, aPottedSaplingStillPlantsNormally.


this doesn't make sense to me, why do I have to visit it to navigate to it if we have coordinates?

> Fair challenge, and we mostly did not have coordinates. **32 of 43 farm regions were pinned.**
> The other eleven routed to a region centre, which is `(regionId >> 8) * 64 + 32` - arithmetic
> on an id, with no knowledge of where anything stands.
>
> The comment justifying the gap said the one-off patches "have a single location each, so the
> region centre is already a fine approximation". It is not an approximation of anything. For
> the Great Conch it landed on a table; for the seaweed patches it lands at y 10272, which
> nothing can reach - and rather than fix the pin, the code worked around that with an approach
> and wrote the unreachable tile into a comment.
>
> The eleven were: Al Kharid (cactus), Anglers' Retreat / Locus Oasis / Fossil Island (the three
> hardwood groves), Draynor Manor (belladonna), the seabed (seaweed), Hosidius Vinery (grapes),
> Canifis (mushroom), Port Sarim (spirit tree), Tai Bwo Wannai (calquat) - which is the Karamja
> fairy ring - and the hespori.
>
> Ten are now pinned from the wiki's own `{{Map|...}}` templates on *Special patches/Patches*,
> the same source and the same provenance as the original 31. That page independently gives
> **3128,2405** for The Summer Shore calquat, which is exactly the tile given from play - so the
> two sources agree on the one coordinate we could check them against.
>
> The hespori is left out knowingly: its pin sits on `mapID=33`, an instanced cave beneath the
> guild, so its coordinate is not on the same map as the rest and would fail the region check
> for a reason that has nothing to do with being wrong.
>
> `PatchLocationStoreTest.everyFarmRegionIsPinned` makes this an invariant with one named
> exemption, so adding a farm region without a pin fails a test instead of being discovered from
> in front of the client. All 42 pins pass the existing land-in-the-right-region check,
> derived rectangle and polygon centres included.


just pin all of them so we don't keep having this issue

> Done, in both directions.
>
> **Every region.** Ten of the eleven unpinned regions now have a pin, from the wiki's own
> `{{Map}}` templates. `everyFarmRegionIsPinned` keeps it that way, with one named exemption:
> the hespori, whose pin sits on `mapID=33` - an instanced cave beneath the guild - so its
> coordinate is not on the same map as the rest and cannot be checked the way the others are.
>
> **And every patch that a region pin was lying about.** Pins were per *location*, which is
> honest at most of them: Falador's two allotments, its flower and its herb are one little farm
> and the wiki gives them one pin because they are one place. The **Farming Guild** is not that.
> Fourteen patches from the redwood at x 1229 to the cactus at x 1265, and from the anima at
> y 3723 to the fruit tree at y 3759 - forty tiles across, every one of them routed to the herb
> patch. Seven other patches are outliers in the same way: a spirit tree at the far end of
> Brimhaven, Etceteria and Hosidius, the Gnome Stronghold's fruit tree away from its tree, and
> Kastori's three.
>
> `WikiPatchLocations` now has a per-patch table keyed by `FarmPatch.getKey()` - the same
> `region.varbit` string the learned table uses - so the tiers read the same way throughout:
> learned, this patch's pin, its location's pin. 42 region pins and 22 patch pins, all of them
> checked against `FarmingWorldData.getRegionsForLocation` rather than against region-id
> equality, which is what lets the genuinely multi-region places pass.
>
> The flower patch and the compost bin have no map template anywhere on the wiki, and that is
> consistent rather than missing - they stand inside the allotment enclosure at every location
> that has them, the guild included, so they take the allotment pin.
>
> One thing the check caught that is worth recording: the Hosidius spirit tree at 1693,3542 is
> in region **6711** while the plugin files it under 6967, and the Great Conch calquat at
> 3128,2405 is in **12325** while its patch is filed under 12581. Both passed, because a
> `FarmRegion` can claim more than one region id and the test asks the world data instead of
> doing the arithmetic itself. An id-equality check would have rejected two coordinates that are
> simply correct.
>
> Tests: PatchLocationStoreTest.everyFarmRegionIsPinned, everyPerPatchPinLandsWhereItBelongs,
> theGuildsPatchesAreToldApart, plus the existing land-in-the-right-region check now covering
> all 64 pins.


I don't get prompted to note (at the coral)

> `RunStop.claimsRegion` exists so every caller answers "is the player at this stop" the same
> way - the Great Conch is filed under region 12581 while two of its patches are on the seabed
> at 13194 - and its own comment says so, naming its three callers. A **fourth** was added
> afterwards and compared the bare region id instead: `GuideTracker.standingAt`.
>
> What that cost: planting or paying for the last nursery completes the stop, at which point
> `appendLeavingErrandsAtFinishedStop` asks `standingAt` before re-attaching the errands that
> belong to the stop rather than to a patch. Underwater it got no, so the note that turns a pack
> of coral into one stack was never offered, and neither was the bucket return.
>
> Test: StopRegionEdgeTest.aStopIsStoodInWhereverItClaimsToReach, verified to fail without the
> fix. The existing harness needed its `RunStop` mock taught to answer `claimsRegion` for its own
> region, which is worth noting: a mock that answers false to everything made the stop's own
> ground read as somewhere else.


the menu swaping on paying chet isn't quite right

> **Not a bug, as far as the code goes.** `ChetPaySwapTest` now pins his exact menu in his own
> order - Pay (East) / Talk-to / Pay (West) / Trade - with his runtime id 15063
> (`TORTUGAN_CORAL_FARMER_UNLOCKED`, a variant of the plain id the world data records, which is
> what `FarmerVariants` is for) and the West payment is promoted correctly.
>
> Three things had to be right and all three are: the entry array runs backwards so the
> left-click is last, the id is matched through the variant group, and the option is matched on
> the patch's own name rather than on wording copied out of one NPC's menu.
>
> What is most likely happening: **the swap follows the current step.** If the guide is on the
> East patch and you go to pay West, it will promote East, correctly. Pinned as a test rather
> than left as a guess, so if it recurs the next report can start from "the step said X".


for some reason the catherby teleport step always drops out of the infobox and nexus list when entering the POH

> The nexus row and the jewellery box line are matched **by destination**, so a nameless leg
> silently stops highlighting the thing you are meant to click. There is a latch for this - keep
> the last name while its stop is still outstanding - and it was gated on being inside the
> house.
>
> That gate was too narrow for the case it was written for. The log shows the leg was already
> nameless five seconds **before** the house was entered, which is what ruled the house out as
> the cause in the first place and should have ruled out the gate with it.
>
> The distinction that actually matters is not where you are standing, it is whether the router
> said anything. An answer that arrived and matched no stop might genuinely be a new
> destination, and still returns null. **No answer at all is no news** - the route is wiped
> whenever a fresh one is asked for, discarded outright while instanced, and empty for a second
> or two around every teleport.
>
> Test: NamelessLegStillHintsTest.aQuietRouteKeepsTheLegsName - names the leg, wipes the route
> and keeps the name, then sends an unrelated route and loses it. Verified to fail without the
> fix.


seedbox issue still ongoing, its supposed to only be lit orange with the menu set to "fill" if there are seeds in the players inventory and free space in the seed box, else the menu should be "empty" and unlit

> The highlight already worked that way. The **menu did not**, and the two were separate
> expressions rather than one question asked twice.
>
> `GuideInventoryOverlay` lights the box on `looseSeedTheBoxWouldTake` - a loose seed in the
> pack that the box would actually accept, which is the stated rule including the six-kinds
> limit. `wantedBoxOption` instead read `anyBoxedSeeds && !boxCanTakeLooseSeeds`, on the
> reasoning that an empty box has "nothing to tip out" so Fill is the safe default.
>
> They agreed everywhere except the state that default created: **an empty box and an empty
> pack**, where the box sat unlit with Fill under the left click. A free click that does nothing,
> on an item the overlay is saying nothing about.
>
> Both now call the same method. Empty on an empty box is also a click that does nothing, which
> is the honest cost - and the smaller one, because it matches what the box looks like.
>
> If it still lights with no seeds in the pack, the cause is the inventory side rather than this
> rule, and the overlay already logs which seed carried the decision: *"Seed box fill highlight:
> ON - POTATO loose in the pack, box holds N kinds"*. That line names it.
>
> Tests: GuideMenuSwapTest.anEmptyBoxAndAnEmptyPackEmpties (the flipped case, with the reasoning
> for the flip), theMenuAgreesWithTheHighlightInEveryState, aStockedBoxWithAnEmptyPackIsUnlitAndEmpties.
>
> Unrelated but found while reading: `GuideInventoryOverlay.java` had a **raw NUL byte** in it -
> deliberate, as the separator in the row-scan cache key, and correct for the job since a NUL
> cannot occur in a widget name. But with the byte written literally, `file` reports the class as
> `data` and plain `grep` treats it as binary and prints nothing. Every search of that file this
> session came back empty and looked like an answer. Now written as `"\u0000"` in a named
> constant: identical at runtime, and the file is text again.


its staying orange still... it should only be orange when there are seeds available to put in it and its not full / any seeds that can go in the seedbox should be accounted for

> Found it, and it was in the counting rather than in the rule.
>
> Both `kindsInTheBox` and `looseSeedTheBoxWouldTake` skipped every crop whose
> `Seed.isSapling()` was true, with the note: *"the box will not take one, and for tree crops
> the count cannot tell an acorn from the sapling it became."* Those are two different things
> and conflating them cost a whole slot:
>
> - the **sapling** — the plant pot — is what the box refuses;
> - the **seed** — a calquat seed, an acorn, a magic seed — goes in like any other and takes one
>   of the six.
>
> `isSapling()` is a fact about the **crop**, not about the item in front of you, so it can never
> make that distinction. Using it meant a box holding five ordinary seeds and a calquat seed
> counted as **five** kinds, read as having a slot free, and lit to fill with a seventh kind
> loose in the pack that it would have refused.
>
> Found by resolving the live box contents from the profile rather than by reasoning about it:
> `{5298, 5282, 21490, 31511, 31545, 5290}` — and 5290 is a calquat seed. Five of the six
> resolved to ordinary crops and the sixth was invisible to the counter.
>
> New `SeedInventoryStore.getSeedCount`, which counts the seed item and never the sapling. That
> is the third question the store now answers, alongside `getCount` (both forms — "do I own
> this crop") and `getPlantable` (the form that goes in the ground). The box's question was
> always the third one and there was no method for it.
>
> The rule itself is unchanged and was already right: a loose seed of a kind already boxed
> always fits, since the stacks have no practical limit (the wiki's figure is 2,147,483,647); a
> new kind fits only while one of the six is free.
>
> Tests: GuideMenuSwapTest.aBoxedTreeSeedCountsTowardsTheSixKinds (verified to fail without the
> fix), aLooseTreeSeedFillsAndItsSaplingDoesNot, plus a `stockSeedItem` helper — the existing
> `stock` records `getPlantedItemID()`, which for a tree crop is the sapling, so a test could
> not previously put a tree seed in a container at all.


this is still a problem, can you look at the logs? / coral seeds?

> The log settled it, and it showed a second bug underneath the first.
>
> **The box was being counted two different ways, and they disagreed.** At 23:04:30 the overlay
> logged *"box holds 5 kinds (limit 6)"*; at 23:05:43 the store's Fill guard logged *"a box
> already holding 6 kinds"*. Same box, seventy seconds apart, two answers - because there were
> two implementations: `applyPendingSeedBoxAction` counted raw map entries with `box.size()`,
> and `GuideMenuSwap.kindsInTheBox` walked the `Seed` table. The calquat seed is what separated
> them, which is the same fault as before wearing different clothes.
>
> One of those counts gates whether a Fill is believed and the other gates whether the box
> lights, so a disagreement means two features acting on different beliefs about one container.
> The count now lives in `SeedInventoryStore.kindsInTheSeedBox` and both call it.
>
> **The remaining ON is the rule working, not failing.** The last lines read *"ON -
> ELKHORN_CORAL loose in the pack, box holds 6 kinds"*. Elkhorn is one of those six, and a kind
> already boxed always fits - the stacks have no practical limit - so boxing the loose frags
> saves a slot and the indicator says so. Checked against the wiki rather than assumed: the seed
> box page says outright that *"frags and mushroom spores are not taxonomically seeds, [but] they
> can still be stored in the seed box"*, and notes a separate bug about frags not auto-storing
> from sailing chests.
>
> So there are two possibilities left and they need one look in game: either elkhorn frags really
> are in the box, in which case the highlight is right; or they are not, and the model has
> drifted again. **Open question for the owner:** even where the game allows it, should frags and
> spores be left out of the box logic - carried loose because they are about to be planted?
>
> Also visible in the log and working as intended: the Fill guard rejecting ranarr, limpwurt,
> snape grass and watermelon seeds that left the pack during a Fill window while the box was at
> six kinds. Those went to the bank or the vault, not the box, and before today they would have
> been credited to it.
>
> Tests: SeedBoxDriftTest.theBoxIsCountedTheSameWayEverywhere, aSaplingIsNotOneOfTheSixKinds.


jfc... Seedbox: "Ranarr seed"x12, "Toadflax seed"x338, "Irit seed"x281, "Snapdragon seed"x70, "Cadantine seed"x15, "Torstol seed"x53. In my Inventory: "Avantoe seed"x153. Its a bug, stop telling me its not.

> It is a bug, and the owner was right to force the point. Six ordinary herb seeds in the box, a
> seventh kind loose, and it lights: **no rule about kinds can produce that.** Only a model that
> disagrees with the box can. Every fix before this one was to a rule, and each of them was
> arguing about the wrong thing.
>
> **The box was never read.** It was derived - from a Fill or Empty plus the inventory delta -
> and its own container event was discarded by the action window, which in practice is the only
> time that event fires. Meanwhile `relearnInventoryFromClient` has reconciled the *pack* against
> the live container every tick for ages, with a comment saying why the box was left out:
>
>     "The bank, vault and box stay event-and-derivation driven; the inventory is the one
>      container that is always available to check."
>
> True of the bank and the vault, which the client only holds while they are open. **False of the
> box.** It is a container you carry; `getItemContainer` answers for it exactly as it does for
> the pack. Nothing about it ever needed inferring except for the couple of ticks around a click.
>
> That one wrong premise is the whole of this container's history of bugs. A model that is only
> ever inferred has no way back once an inference goes wrong - which is why the notes above keep
> describing correct fixes to real defects that did not make the symptom go away. They could not.
> They were all downstream.
>
> The box is now reconciled from the client every tick, alongside the pack. The derivation stays
> for the click itself, where the client's copy really does lag, and the existing window keeps
> the reconcile out of the way while it does. A null container is treated as "no news" rather
> than "nothing in it", so a box the client has never been told about does not wipe what is
> remembered. Any disagreement is logged with both sides written out, so the next one has a
> timestamp instead of being a state nobody can reconstruct.
>
> Tests: SeedBoxDriftTest.theBoxIsReadBackFromTheClientEachTick (the herb-seed case verbatim,
> verified to fail without the fix), aBoxTheClientCannotSeeLeavesTheModelAlone,
> theReconcileWaitsForAClickToLand.
>
> Also fixed on the way, and worth keeping: the box's kind count had **two implementations** that
> the log caught disagreeing about the same box in the same second - `box.size()` in the store's
> Fill guard said six while `GuideMenuSwap` said five. One gates whether a Fill is believed, the
> other whether the box lights. It now lives in `SeedInventoryStore.kindsInTheSeedBox` and both
> call it.


I've been trying to get these side category's highlighted for the seeds you need to withdrawl from the seed vault, but its never worked

> The widget inspector's own tree is what made this attemptable. `BankHighlightOverlay.highlightVault`
> already recorded the history: *"The category half never worked. Two attempts at finding the
> label failed to highlight anything, and neither could be verified from outside the client."*
> Both were looking at the wrong widget.
>
> The rows are **`SeedVault.CATEGORY_LIST` - group 631, child 8**, one dynamic child per row, and
> the inspector confirms `631.8[3]` is the "Flowers" row. That constant exists in
> `InterfaceID.SeedVault` and nothing was using it.
>
> **No crop-to-category table.** The obvious implementation is a map from each crop to its
> category name, and it would be a fourth thing to keep in step with the game - wrong the day a
> category is renamed or a seed moves, and wrong silently. The vault already knows:
> `CATEGORY_HEADERS` labels the sections of the item list and `OBJ_LIST` lays the items out
> beneath them, so a seed's category is the nearest header above it. That is the same thing the
> player reads off the screen, and it needs no maintenance.
>
> Compared on the y each widget was **laid out** at rather than its canvas position: headers and
> items are separate lists over one scroll, so scrolling moves both together and must not change
> the answer.
>
> It also degrades honestly, which matters given this has failed twice. An unrecognised layout
> matches nothing and draws nothing - no invented outline on the wrong row - and a log line names
> the categories derived and every row title on screen, so a third failure would arrive as data
> rather than as another guess.
>
> Tests: SeedVaultCategoryTest - the nearest-header rule, several sections at once, the
> off-by-one where a header below an item must not claim it, an item above every header, tagged
> header text, and the no-headers case drawing nothing.


" - Your inventory is full - note the irit with the tool leprechaun." - I don't have any irits, I'm mid compost bin emptying

> The step named the crop growing in the patch in front of you rather than anything in your
> pack. So a full pack at a ripe irit said "note the irit" with the irit still in the ground -
> an instruction with nothing to perform it on. Mid compost-bin emptying the pack was full of
> buckets and the herb patch beside the bin supplied the wording.
>
> It now requires you to be holding some, un-noted. Silence when you hold none is the right
> answer: the pack being full is real, but this branch's job is to name what to hand over, and
> the leaving errands name the biggest stack you are genuinely carrying once the patch work is
> done.
>
> Two existing tests broke, and they were pinning the defect. `carryingFullPack()` fills the
> pack with twenty-eight dummy ids and no potatoes, and the assertion still expected "note the
> potato" - it passed only because the branch named the crop in the ground. Both now use a new
> `carryingFullPackOf(itemId)`, which is also the more honest picture: mid harvest the pack
> really is full of the crop.
>
> Tests: GuidePlanTest.aFullPackDoesNotOfferToNoteACropStillInTheGround,
> aFullPackOfTheCropStillOffersTheNote.


after getting a new contract from jane and I need to get the seed from vault/bank, neither get highlighted [the object to walk to, as on a bank run]

> `GuideOverlay.highlightSupplyPoints` outlines the bank booths and the seed vault from
> `GuideStatus.getSupplySources`, and that set was populated **only on the bank leg**:
>
>     planner.isActive() && planner.isAtBankLeg() ? planner.getSupplySources() : emptySet()
>
> A contract is taken from Jane in the middle of a run, nowhere near the bank leg. So
> `contractSeedFetch` correctly produced "Withdraw your irit seed from the seed vault here" and
> nothing in the room lit up - the instruction without the thing to walk to.
>
> `contractSeedStorageHere` already worked out which container it is, and threw the answer away
> as a string. It now returns the `SeedSource` and derives the wording from it, so the sentence
> and the outline cannot name different containers.
>
> The outline is keyed off the **step on the list** rather than off the contract state, because
> the step is what carries every other condition - the run's own types, an empty patch to plant
> in, the seed not already in hand - and re-deciding those in a second place is how the two come
> to disagree.
>
> Tests: ContractSeedFetchStepTest.theFetchLightsTheContainerItNames, aVaultedFetchLightsTheVault,
> nothingIsLitWithoutAFetchStep, nothingIsLitWhenTheSeedIsNotStoredHere.


my inventory is full, mid watermelon harvest, and instead of promtping me to put them into the compost bin, its trying to get me to keep harvesting

> The data was all correct this time - the guild's big bin reads EMPTY, fodder is on, watermelon
> is the allowed crop. What blocked it was the **minimum fill**.
>
> `FODDER_MINIMUM_DIVISOR` declines to offer a token fill: below a third of the bin the step is
> not worth the click, and the bin keeps its contents between runs so there is no urgency. That
> reasoning is sound and it is about **a pack with room in it**. With no room the bin is not a
> tidy-up, it is the only way to carry on - four watermelons into it is four slots back and the
> harvest continues.
>
> The big bin makes this sharper than the seven small ones: a third of thirty is **ten**, and one
> allotment rarely gives you ten of anything loose after the rest of the stop has filled your
> pack. So the bin that is best placed to help was the hardest one to trigger.
>
> A full pack now waives the minimum. Everywhere else the floor stands, so a stray two
> watermelons still produce nothing.
>
> Note for the tests: `CompostBinPlanTest` mocks `CarriedItems`, and a mock answers **zero** free
> slots - which is now the waiving case. Left alone that would have silently switched the floor
> off in the very tests that exist to pin it, so the harness now sets a pack with room by
> default and the two new tests opt into fullness explicitly.
>
> Tests: CompostBinPlanTest.aFullPackIsOfferedTheBinHoweverLittleItHolds (verified to fail
> without the fix), oneFreeSlotIsNotAFullPack.
>
> Also added: a bin decision log line. This has now been reported three times in different
> clothes - never offered, offered too late, and asking for another watermelon with nowhere to
> put it - and each time the answer was live counts that had moved on before anyone could look.
> The line carries all of them: fodder on or off, the allowed crops and how many are held, the
> room in the bin, the free slots, and the verdict.


I have mushroom spores in my box but the bank is prompting me to withdraw mushroom seeds for the run... doesn't complain when the 1 is in my inventory with zero in my box, but as soon as I "fill" the bank run step comes back

> The arithmetic is right and the model is wrong. The loadout counts the box - `inPack` is
> INVENTORY plus SEED_BOX - and the stored model reads:
>
>     SEED_BOX: toadflax 676, irit 281, snapdragon 70, cadantine 15, torstol 53, ranarr 25
>     BANK:     mushroom spore 94
>
> Six herb kinds and **no spore**. Given that model the WITHDRAW row is correct, which is why
> nothing in the loadout needed changing.
>
> **Where the spore went.** The seventh-kind guard exists so a seed banked during a Fill window
> is not credited to the box. When it fires the seed has already left the inventory, so declining
> to add it to the box removes it from the model altogether - and if the model's six kinds are
> not the box's six, that is a disappearance rather than a correction. The spore left the pack,
> the guard refused it, and nothing held it. Exactly "as soon as I Fill, the bank run step comes
> back".
>
> The guard now says so about itself: a seed the game accepted that the model cannot place is
> evidence the model is wrong, so `boxSuspect` is set and the next read of the box is taken over
> the derivation instead of being discarded by the action window.
>
> **And the log says the root fix is not in the build being tested.** There are zero "Seed box
> reconciled from the client" lines, so the tick reconcile - the change that stops the box model
> going stale at all - is not in it. With that in, this model would have been overwritten by the
> real box within a tick and the spore would never have gone missing.
>
> Tests: SeedBoxDriftTest.aFillItCannotAccountForMakesTheNextReadWin,
> anUnremarkableFillLeavesTheWindowAlone.


also the vault/bank object highlight (not item highlighting inside the interfaces, the literal game world object) is still not working after getting a new contract

> Two gates, and the earlier fix only opened one of them.
>
> Populating `GuideStatus.getSupplySources` for a contract fetch was necessary and, on its own,
> useless: `GuideOverlay.render` only calls `highlightSupplyPoints` inside the **"no current
> step"** branch, and a contract fetch *is* a step. The answer was available to a branch that was
> never reached.
>
> The reasoning behind the old placement was that the supply leg is the stop with no patch work.
> True of the bank leg, not true of the other way a run sends you to a container.
>
> The named container is now marked wherever the run has named one, outside that branch, guarded
> on the set being non-empty - which matters, because `marks()` reads an **empty** set as "a
> bank, we are not sure what for" and would otherwise light every booth in the room. The bank
> leg keeps that fallback for itself.
>
> Test: ContractSeedFetchStepTest.aNamedContainerIsNotTheSameAsTheBankLegsFallback, which pins
> the two cases apart in words rather than leaving them to a reading of the render method.


I literally just launched this client/run, it is in. keep looking. [re: no "Seed box reconciled" lines]

> Correct, and my inference was bad. That line only fires when the read **differs** from the
> model, so its absence means either "the client had no box to read" or "the box matched" - and
> I read it as "the code is not there". Same mistake as reading an empty `grep` as an answer
> earlier in the session, and it cost a round.
>
> The reconcile counters only ever counted the inventory, so the box half was invisible. It now
> counts separately - reads, ticks with no container at all, and corrections made - and says once
> when the client has no seed box container to reconcile against, naming the id it asked for
> (573). The run-planned line carries all three.
>
> That distinguishes the two remaining possibilities, which want opposite fixes: if the container
> is null the whole reconcile path is dead and the box is still only ever derived; if it is read
> and matching, the client's own copy is what disagrees with the box on screen.


and the issue with watermellon harvesting when inventory is full is still happening, not getting prompted to put them in the big compost bin

> The `Stop ... offers, in order:` line ended it in one reading, which is what it was added for.
>
> The bin was in the stop, and `binRank` was putting it first, and the bin decision line read
> **"room for 30, fodder on (allowed [5982x14]), 0 free slots -> fill with 5982"**. Everything
> upstream was right. What the panel showed was the bin's *first* step:
>
>     Stop Farming Guild offers, in order: Farming Guild=DEPOSIT_COMPOST; ...
>
> **`addDepositStep` runs before `addFillStep`**, and it fires whenever you are carrying a single
> bucket of compost. At the Farming Guild you always are - it is what you are treating the
> patches with. So the bin's instruction was permanently "store your buckets with the tool
> leprechaun", pointing at the leprechaun, and "put these in the bin" sat behind it and never
> became current.
>
> The stated reason for that order was that "the fill needs the slots they are sitting in". It
> does not: filling a bin takes items **out** of the pack and needs no room at all. The order is
> right for a **banked** fill for a different reason - you arrived carrying both and the buckets
> are clutter from the bank trip - so that case keeps it. A **fodder** fill now goes first, with
> the deposit right behind it.
>
> Worth recording as a pattern: three rounds of this were spent fixing real defects upstream -
> the bin not in the run, the ordering, the minimum fill - each of which was genuinely broken and
> none of which was the reported symptom. The thing that resolved it was logging **what the stop
> actually offered**, rather than what each part of the decision concluded.
>
> Tests: CompostBinPlanTest.aFodderFillComesBeforeTheDeposit,
> fullBucketsAreDepositedBeforeABankedRefill (the old test, re-aimed at the case its reasoning
> actually holds for).



and now the seedbox bug is back [the highlighting], stop using python to inject code and actually fucking read it please

> Fair, and the two are connected. Nearly every edit this session was a string replacement into
> a file I had not read end to end, which is a way of changing code while only ever seeing the
> few lines either side of the change. This regression is exactly what that produces.

> The reconcile added for the staleness bug wrote the client's copy over the model
> unconditionally:
>
>     Map<Integer, Integer> now = countSeeds(box);
>     ...
>     record(SeedSource.SEED_BOX.getContainerId(), box);
>
> `null` was guarded as "no news". **Empty was not.** So a tick where the client's copy has not
> been populated wiped the box to nothing, which reads downstream as no kinds in it - and the
> fill highlight lit for every loose seed in the pack. The highlighting bug, reintroduced by the
> fix for the staleness bug, in the same file, four lines apart.
>
> The distinction that was missing is what each source **is**. A container event is the game
> telling us something. The reconcile is a poll of whatever the client happens to be holding, so
> it is a backstop: it may correct the model and it may not empty it. A real Empty still empties
> the box, because that arrives as the box's own container event and goes through `record` as
> before.
>
> Counted and said once, like the null case, and carried on the run-planned line as
> "N empty copies ignored" - so if the client's copy really is the box and really is empty,
> that shows up as a count climbing rather than as silence.
>
> Tests: SeedBoxDriftTest.anEmptyCopyFromTheClientDoesNotWipeTheBox,
> arealEmptyStillEmptiesTheBox.


we still, for like the 7th time, haven't fixed the composting issue for fodder patches at the farming guild

> Found it, and every earlier fix was being silently overwritten a few lines after it ran.
>
> `sortedByDistance` applies `contractFirst` and then `binsFirst`, which is where six rounds of
> work went: getting the bin into the run, getting it ranked to the front when the pack is full,
> waiving the fill minimum, putting FILL_BIN ahead of DEPOSIT_COMPOST. All of it correct, and all
> of it discarded by the next thing `computeStepsHere` does:
>
>     List<FarmPatch> offered = contractComesFirst(stop, ordered);
>     offered = flowersAfterAllotments(offered, stop);
>     FarmPatch first = chooseWorkingPatch(offered, stop);   // <- hoists the worked patch back
>
> `chooseWorkingPatch` holds you to the patch you are part way through as long as it has any
> outstanding step. Mid harvest that is the allotment, its step is HARVEST, and it goes to the
> top of the list whatever `binsFirst` decided.
>
> The latch is right in general - it stops the guide hopping between patches mid-job. It is
> wrong when the job cannot be done, and a full pack with a harvest next is exactly that: there
> is no next click, and the thing that would unblock it is the bin the latch is hiding. It now
> lets go for that one case, and for nothing else.
>
> **And the diagnostic I added to find this was measuring the wrong list.** `noteStopOrder` was
> handed `ordered` - before `chooseWorkingPatch` ran - so it logged "bin first" while the panel
> showed "harvest", and it confirmed a fix that never reached the player. It takes the finished
> step list now, after the working patch is chosen, after skips are dropped, after errands are
> woven in, keyed by patch rather than by display name (six patches all called "Farming Guild"
> named nothing).
>
> The lesson worth keeping: a diagnostic that measures something *adjacent* to the question is
> worse than none, because it produces confident wrong conclusions. Both times this session that
> an absence or a near-miss got read as an answer, it cost a round.
>
> Tests: GuildBinPrecedenceTest.afullPackReleasesTheWorkingPatch (verified to fail without the
> fix), afullPackKeepsTheLatchForNonHarvestWork.


something is seriously fucked with the seedbox fill highlighting logic, why does this keep rearing its head? ... all seeds (not saplings) are storable, we probably need to dedupe some seed logic

> Right on both counts, and the second is the answer to the first. There was no single place
> that answered **"can this item go in a seed box"**, so four places worked it out separately:
>
> - `countSeeds` accepted anything in the `Seed` table - which includes **sapling ids**, because
>   `Seed.forItemId` maps both forms to the crop;
> - the kind counter and the fill highlight used `Seed.isSapling()`, a fact about the **crop**
>   and not about the item in front of you;
> - the Fill guard counted raw map entries while the overlay walked the seed table, and the log
>   caught the two disagreeing about the same box in the same second;
> - and the capacity lived in **three** files, each reading from the next.
>
> New `data/SeedBox`: `KINDS`, `accepts(itemId)`, `kindsIn(ids)`, `onlyWhatItHolds(contents)`.
> The accepted set is derived from the `Seed` table - every seed item, never a sapling item - so
> it cannot fall behind it, and that is what makes frags and mushroom spores work with no special
> case.
>
> Applied at the point the box's contents are **recorded**, so a sapling can never enter the
> model at all and every consumer downstream can treat "in the box" as meaning something the box
> can hold. The three capacity constants are deleted rather than aliased; aliasing is how three
> of them happened.
>
> Tests: SeedBoxTest (every seed accepted, every sapling refused, walked over the whole table),
> SeedBoxDriftTest.aSaplingCannotEnterTheBoxsModel, aTreeSeedIsAcceptedLikeAnyOther.


I'm being routed to a different patch mid compost bin fill up, mellons aren't harvested fully yet

> Caused by the previous fix, one release later, and the two are the same event a second apart.
>
> Waiving the fodder minimum for a full pack got the bin offered. Then **putting the first melon
> in frees a slot** - which re-imposed the minimum, took the fill step away mid fill, and let the
> working-patch latch grab the allotment again with the bin standing open.
>
> The minimum is about whether a fill is worth *starting*. A part-filled bin is not that
> question: stopping halfway is worse than a token fill ever was. So it is waived for a bin that
> is already under way as well as for a full pack. "Part filled" is `wanted < capacity`, which is
> what `addFillingSteps` already passes and what an empty bin never does, so the caller had
> already answered it.
>
> Worth noting how this one differs from the six before it: it was not a new place the answer got
> lost, it was a boundary condition on a rule I had just changed. The 0/1 free-slot edge is
> crossed by the very action the rule exists to encourage, which is the kind of thing worth
> looking for whenever a condition keys off a resource the recommended action consumes.
>
> Test: CompostBinPlanTest.aFillAlreadyUnderWayIsNotAbandoned, verified to fail without the fix.


I'm standing at the falador patches with allotment and flower harvested and I'm being prompted to tele to another location instead of finishing these patches / I have watermelon seeds in my seedbox, so that info is wrong / nothing to do with the seed pack - there shouldn't be any plugin logic around seed packs, at all

> Three things, and the diagnostic added last round answered the first two.
>
> **1. The reconcile is dead.** The log says it outright:
>
>     The client has no seed box container to reconcile against (id 573).
>     The box stays derived until it sends one.
>
> `getItemContainer(573)` returns null - the client holds no copy of the seed box until it has
> been opened. So the "read the box every tick like the pack" fix does nothing, and the box is
> still only ever derived. Its contents are also **persisted**, so the model can be a session or
> more old. That is why Falador looked seedless: `seedAtHand` counts the box, the box model held
> six herb seeds from some earlier session, and the watermelons were not in it.
>
> **2. Being sent away without a word.** `announceSkips` exists to say exactly this - "skipping
> the allotment, your watermelon seeds are in your bank" - and it could never reach the moment it
> is for. Picking the last crop empties the patch on the same tick; an empty patch with no seed
> at hand produces no steps; no steps anywhere completes the stop; a completed stop leaves
> `getRemaining()`, so `stopAt` answers null and `computeStepsHere` returns before the
> announcement is built. Same shape as the leaving errands, same repair: re-attached at the stop
> that finished under your feet, anchored to `workingRegion`.
>
> And the wording now separates the two things it could not before: "not in your inventory" when
> the box has been seen this session, and "and your seed box has not been opened this session, so
> what I have for it is from last time" when it has not. The plugin was wrong at Falador, and
> worse, it was wrong confidently.
>
> **3. Seed packs.** There is no plugin logic around them and there should not be - checked, and
> every mention in the source is a comment about the contract reward. `SeedCapture` already
> excludes them from the box highlight deliberately. What went wrong was **wording**: "pack" was
> being used for the inventory in player-facing text, and a seed pack is a real item, so "not in
> your pack" reads as a statement about that. Fixed in the three places a player sees it - the
> skip wording, the seed box config description, and the dropped-crop step - and noted where the
> new wording is, since the internal comments use "pack" for the inventory throughout.
>
> Still open and now precisely stated: **the seed box model cannot be trusted until the box has
> been opened once in a session.** The plugin has no way to read it before that. Worth deciding
> whether the run should ask for that up front the way it asks for tools.

if we know our farming contract is dead, don't have me walk all the way over to clear it, before getting the seed from the bank/vault
> RESOLVED, in the half that was actually broken. Two halves were checked and only one was wrong.
>
> **The supply leg was already right, and this is worth recording so it is not "fixed" twice.**
> A dead patch is actionable (`RunPlanner.isActionable`, `case DEAD`), so it reaches
> `actionableByGroup`; `plantsNothing` is false for a contract group whose crop is not ripe; so
> `selection.getSelectedFor(contractGroup)` hands the contract seed to `seedsWantedThisRun`, and
> `getSupplySources` puts BANK or SEED_VAULT on the list. A dead contract with a banked seed has
> always opened the run at a bank.
>
> **What went wrong was inside the guild stop.** `contractSeedFetch` — the step that says
> "withdraw your seed from the bank here" — stood down for a dead crop, and did so three times
> over: the patch has outstanding clicks (the clear), its produce is the contract's, and it is
> not empty ground. All three tests read a dead crop as "not ready for a seed yet". So the run
> sent the player across the guild to clear the patch, and only once the ground was bare did the
> seed in the bank they had walked past become a step. Two crossings of the guild for one patch,
> which is the complaint exactly.
>
> The fix is one exemption, not a fourth state: dead ground is ground the run is already going to
> clear, which is the same argument the spade-cleared allowance already makes for a stripped
> bush. The seed is wanted on this trip either way, so the fetch goes in front of the walk. It
> is still front-of-queue, so the order is now: withdraw the seed, walk over, clear, plant.
>
> **A second bug fell out of the same reading.** `contractIsInTheGround` counted a dead crop as
> the contract planted, so `missingContractSeed` treated the seed as already spent and said
> nothing — silence at the one moment the player owns no seed and is about to walk to a patch
> they cannot refill. Same "in the ground means spent" error `contractDudPatch` exists to correct
> one state over. A dead crop is now not in the ground.
>
> Tests: `ContractSeedFetchStepTest.aDeadContractCropIsFetchedForBeforeTheWalkOver`,
> `aDeadCropOfAnotherProduceIsAlsoClearedForThisTrip`, `aDeadContractWithNoSeedOwnedStillSaysSo`.
> All three were confirmed to fail against the unfixed code before being kept.
>
> Unverified in the client: the whole path needs a dead guild contract with its seed banked.
> Fail signature — if the fetch step still appears only after the clear, the exemption is not
> reaching `contractSeedFetch`; if it appears but names the wrong container,
> `contractSeedStorageHere` is the place to look, not this change.

when paying for protection we shouldn't compost (on that specific seed/sapling). Also that section should collapse within the "Select seed" drop down.
> RESOLVED, both halves.
>
> **Compost.** A protection payment is immunity outright — `DiseaseRisk.survivalChance` returns 1
> for a protected patch, not a better number — so the disease half of what compost buys is already
> paid for. On a seed with no lives mechanic there is no other half, and the bucket buys nothing.
>
> Scoped **per seed**, which is what "on that specific seed/sapling" asks for and is also the only
> correct scope: compost still raises the yield on herbs, allotments, hops and giant seaweed
> through the lives mechanic, so a protected ranarr wants its ultracompost as much as ever — there
> the payment buys survival and the bucket buys herbs, and they are different purchases.
> Suppressing on "is this protected" alone would have quietly cost a harvest on every protected
> herb and allotment run. `CropYieldModel.respondsToCompost` is the existing single home for
> "does compost move this seed's yield", so the new `compostWastedOnProtected` is built on it
> rather than on a second answer that could drift from it.
>
> Three sites, one predicate: `GuidePlan`'s pre-plant branch (asked of the seed going in), its
> just-planted stage-0 branch (asked of the crop standing there), and `RunLoadout.addCompost`
> (per group, so it only drops the bucket when *every* seed picked for that group agrees — a tab
> holding a protected sapling beside a ranarr still banks compost for the ranarr).
>
> Two things worth knowing about the edges. `GuidePlan` gained a `paidToProtect` parameter rather
> than reusing `protecting`, because that one means "a payment is still owed" and goes false the
> moment the farmer takes it — which is exactly when the crop is most protected, and would have
> let the after-planting branch re-offer compost on a tree that had just been paid for. And the
> suppression happens *before* `noteCompostDowngrade`, so a withheld bucket is not announced as
> "using compost instead of ultracompost"; nothing is missing, so there is nothing to apologise
> for.
>
> **The panel.** `protectPanel` moved inside `seedBody`, so it collapses with "Select seed". The
> rows are one checkbox per seed — the same seeds as the grid directly above them — so a
> collapsed seed section that still showed a column of seed names was putting away the one thing
> it was meant to. The compost picker and its note stay outside deliberately: that is one choice
> for the whole group rather than one per seed. Say so if it should collapse too — it is a
> one-line move.
>
> Tests: `GuidePlanTest.aProtectedSaplingIsNotComposted` (and its counter-case
> `aProtectedCropWithLivesIsStillComposted`, which is a guard rail rather than a regression —
> it passes either way and exists so a later blanket rule fails here),
> `RunLoadoutTest.aFullyProtectedTreeRunBanksNoCompost` and `anUnprotectedTreeRunStillBanksItsCompost`.
> The two behaviour tests were confirmed to fail against the unfixed code.
>
> Unverified in the client: a tree or fruit tree run with protection ticked, checking that no
> compost is asked for at the patch and none is banked for on the way out.

I'm not being prompted to chop down my palm tree on a fruit tree run - after harvesting
> RESOLVED. `PatchProjection.isChoppable` listed trees, hardwoods, the crystal tree and the
> celastrus, and no fruit tree — so the chop step did not exist. `RunPlanner.wantsReplantClear`
> was gated on `SpadeClearedCrops` ({BUSH, CACTUS}), so once the fruit was picked the patch also
> stopped being *actionable* and the run walked away from it. Both halves had to move.
>
> **The order is the careful part.** A tree is check, chop, dig. A fruit tree is check,
> **harvest**, chop, dig — the owner's correction, and the client sources agree: the grown palm's
> menu is recorded as `Palm tree[Chop-down,Inspect,Guide,Pick-coconut]`, both options at once.
> So the chop is gated on there being no fruit left, which lets the harvest branch run itself out
> first. Regular trees are untouched.
>
> **And it will not fell a tree it cannot replace.** A farmed tree never comes back, so chopping
> one is simply how the patch finishes. A fruit tree regrows fruit forever, so the chop is offered
> only where the run has a sapling at hand for that patch — the same test the picked-clean bush
> already makes.
>
> **The stump.** A fruit tree's harvestable stage IS its fruit count, so a stripped tree and its
> stump both decode HARVESTABLE stage 0 and are one value apart. What separates them is what sits
> before: every fruit's block runs growing, harvestable, diseased, dead, then the stump. Confirmed
> against the client sources, which name each one — value 225 is
> `// Palm tree stump[Clear,Inspect,Guide] 8104`. The eight values (33, 60, 97, 124, 161, 188,
> 225, 252) are pinned by `TreeStumpTest.fruitTreeStumpsAreExactlyTheEightUpstreamNames`, because
> getting this wrong does not go quiet — it makes the guide say "dig up the palm stump" at a
> standing tree forever.
>
> Deliberately NOT added to `SpadeClearedCrops`: that set answers "does a spade take this straight
> out", which `contractSeedFetch` relies on, and a fruit tree wants an axe first.
>
> Tests: `GuidePlanTest.aFruitTreeIsHarvestedBeforeItIsChopped`, `aStrippedFruitTreeIsChopped`,
> `aStrippedFruitTreeIsLeftStandingWithNothingToReplant`, `aFruitTreeStumpIsDugUp`, plus the two
> `TreeStumpTest` pins. Confirmed to fail against the unfixed code.
>
> Unverified in the client: a fruit tree run with a sapling banked, through harvest, chop, dig and
> replant. Fail signature — if the chop appears while fruit is still on the tree, `hasProduceToPick`
> is not reading the fruit count; if "dig up the stump" appears at a standing tree, the stump rule
> has picked the wrong value and `TreeStumpTest` is where to look.

compost always shows up as a notification I can't turn off in the infobox
> RESOLVED, and it was not the bin grouping. `GuideTracker.downgradedTo` — the field
> `ReadyInfoBox` reads for its "Fallen back to <tier> / Worth a compost bin run" line — was set
> when a downgrade happened and cleared only by `noteCompostDowngrade` being reached again with
> nothing to report. A run that ended while downgraded left the line standing for the rest of the
> session, and turning "Fall back to weaker compost" off does not clear a line already up, which
> is exactly "I can't turn it off".
>
> Now cleared in `runEnded()` alongside its siblings. Note it is a *different* field from
> `announcedDowngrade`, which stops the chat line repeating within a run; the two had drifted
> apart because only one of them was ever at the run boundary. Same shape as the bug that
> boundary's own note describes.

the whole compost tab should have one outer collapse for all compost items - supercompost, compost, compost these, and the two checkboxes
> RESOLVED. The tab had three collapses and no outer one, so a player who had finished
> configuring compost still gave up most of the sidebar to it and the run planner and patch
> statuses were pushed off the bottom. The owner's reasoning, and it is the same one the seed
> selector's "Select seed" collapse was built on: this is configured rarely and then folded away.
>
> Everything now sits inside one "Compost (N picked)" heading — both fill lists, the volcanic ash
> checkbox, the fodder checkbox, its note and the whole "Compost these" group. The three inner
> sections still fold on their own inside it, and the state persists through `PanelLayoutStore`
> under `binAll` like every other section.
>
> The "Farming Guild bin" label stayed, but moved inside: it names the two fill lists, which are
> the guild bin's alone, and the outer heading cannot carry that name now that it covers the seven
> allotment bins too. The outer count sums fills and fodder crops, because folded away they are
> one question — "is compost set up".
>
> One thing that shaped the layout rather than being a choice: `refreshFodder` does
> `fodderHeading.setVisible(on)`, so the fodder section's own collapse toggle only exists while
> its checkbox is ticked. That checkbox therefore cannot live inside the fodder section — it would
> become unreachable once switched off — which is why it sits above that group rather than in it.
>
> Tests: `CompostBinPanelLayoutTest.thereAreFourOfThem` and `theOuterCollapseFoldsTheEntireTab`;
> the two existing counts were updated rather than loosened.

presets for the run checkboxes - a dropdown with save/delete that remembers the tickbox positions, nameable, so you can quickly do a herb run or tree run without reconfiguring each time
> BUILT, 2026-08-18. Spec in `docs/run-presets-spec.md`, which was written first and is kept as
> the record of the decisions.
>
> `RunPresetStore` (a `ProfileJsonStore` under key `runPresets`) holds name to `RunOption` **key**
> list. Keys rather than resolved options for the reason `RunTypeStore` already stores them that
> way: a line can be a planting group or a mode, not merely a patch type — and it means a preset
> survives the offered list changing under it. A key for a line currently hidden stays in the
> preset and simply is not ticked, so switching the type back on brings it back.
>
> The control is an editable dropdown plus Save and Delete, above the tickboxes. Selecting a name
> applies it at once; typing a name and pressing Save stores the current ticks; pressing Save with
> a saved name selected overwrites it. Delete forgets the preset and leaves the ticks alone —
> deleting a bookmark should not change where you are standing. The box shows `(unsaved)` whenever
> the ticks match no preset, so it never claims a name you have edited away from.
>
> Applying goes through `RunTypeStore.setSelected(ticked, offered)` rather than writing the blob,
> because that method carries the offered-list rule and the contract retargeting.
>
> **Run types only**, deliberately, per the spec: not seeds (`RunTypeStore`'s own note — "seeds
> change constantly as stock runs out", so a preset would be stale within a week), and never
> availability, which is a global invariant with one home.
>
> Tests: `RunPresetStoreTest` — save, overwrite, order, order-insensitive matching, trimming,
> deletion, the config round trip, and a malformed blob being dropped entry by entry.
>
> Unverified in the client: making a herb preset and a tree preset and switching between them.

the run presets broke the checkbox highlighting - random items are highlighted unrelated to the saved profile
> RESOLVED, and it was a regression from the presets rather than anything they revealed.
>
> The tick colour is the plugin's own (`RunPanel.dimIfUnticked` sets the foreground from the tick
> state), so it only changes where something asks for it. The checkbox's own ActionListener asked.
> **`refresh()` did not** — it synced `box.setSelected(...)` from the store and left the colour
> alone. For as long as clicking was the only way to move a box that was invisible, because the
> click path always recoloured.
>
> Applying a preset moves every box at once from inside `refresh()`, so each one kept the colour
> of the selection before it: boxes lit with nothing ticked, ticked boxes reading as off. Which is
> exactly "random items highlighted unrelated to the saved profile".
>
> `refresh()` now recolours unconditionally — not only when it changed the tick, because the
> colour can be stale while the tick is already right, and it is one field assignment either way.
>
> **This is the second bug of this exact shape**, which is worth recording. `setSelected` does not
> fire an ActionListener, so every path that moves a box in code has to recolour it by hand: the
> first was the paired full/harvest-only lines ("Bush and Cactus always stay bright when
> unchecked"), fixed in `untick` and covered by
> `RunListAppearanceTest.aCounterpartUntickedByThePanelDimsToo`. A third path will do it again.
> If it recurs, the fix is to stop scattering the call — wrap the tick and the colour in one
> method that is the only way this panel moves a box.
>
> Test: `RunListAppearanceTest.aTickChangedWithoutAClickIsRecoloured`, driven through the store
> rather than through a click, because a test that clicked would have gone on passing. Confirmed
> to fail against the unfixed code.

in the infobox plan I have withdraw compost listed multiple times
> RESOLVED. The step list emitted one WITHDRAW_COMPOST per patch. From the run's own log, one
> Falador stop:
>
>     4773=WITHDRAW_COMPOST; 4773=APPLY_COMPOST;
>     4771=WITHDRAW_COMPOST; 4771=APPLY_COMPOST;
>     4772=WITHDRAW_COMPOST; 4772=APPLY_COMPOST;
>
> The treating is per patch and that is right. The withdrawal is not: `GuidePlan.addCompostSteps`
> already takes `patchesToTreat` and words the step as enough for all of them, so those were three
> copies of one instruction, each asking for the full amount.
>
> `collapseDuplicateWithdrawals` now keeps the first per tier, beside the existing
> `collapseDuplicateNotes` and for the same reason ("one visit, said once"). **Keyed on the tier
> rather than the action**, unlike the note collapse: a split herb type can want ultracompost on
> the protected patches and supercompost on the rest, and collapsing by action alone would drop
> one and send the run out short of a bucket it had been told to bring.
>
> Tests: `LeprechaunErrandOrderTest.oneWithdrawalCoversEveryPatchAtTheStop` and
> `twoDifferentTiersAreTwoWithdrawals`.

remove the "withdraw them at any patch later" note, and shorten the noting steps to just "Note your strawberries"
> DONE, all three places.
>
> - `CompostBinPlan`: the deposit step drops "- withdraw them at any patch later".
> - `GuidePlan`: the full-pack note was "Your inventory is full - note the strawberry with the
>   tool leprechaun."; now "Note your strawberries."
> - `GuideTracker`: the leaving errand was "Note your X with the leprechaun before moving on.";
>   now "Note your Xs."
>
> One thing the shortening needed that the old wording hid: `plural` lived in `GuideTracker` and
> every caller passed a **patch type** name — "herb", "allotment" — none of which ends in a
> consonant and a y, so adding an s was always right. These steps pass **produce** names, and the
> first one anyone reads it on is the strawberry. The rule moved to `GuidePlan` (the direction the
> dependency already runs) and gained the -y case, so it is "strawberries" and not "strawberrys".
> Pinned by `GuidePlanTest.cropNamesArePluralisedTheWayEnglishDoes`.

weird timing bug - finished at Ardougne farm, prompted to teleport home via the home tab; by the time I clicked, it had swapped to my Ardougne cloak (Kandarin monastery). Now I'm in my POH and the nexus and jewellery box are highlighted with no sub-menu highlighting
> RESOLVED, 2026-08-18.
>
> Two failures, and the second is a consequence of the first.
>
> **The highlight moved between the eye and the click.** The run re-decided its destination while
> the player was reaching for the item the previous decision had named. `GuideTracker.onGameTick`
> resolves `destinationStop` every tick and pushes it to `RunPlanner.commitDestination`; the
> latch added for the mid-journey swing (`open-issues.txt`, "the destination is chosen once and
> kept") holds a stop once it is named, but this happened at the moment a stop **completed** —
> Ardougne finishing is exactly when it leaves `getRemaining()` and the next leg is chosen
> greedily from wherever the player is. So the latch was not wrong, it simply had nothing to hold.
>
> **Then the POH highlight had no sub-target.** Arriving in the house with the destination now
> Kandarin monastery, the nexus and the jewellery box both lit with nothing lit inside them. The
> route item highlight and the sub-menu highlight are resolved separately — see
> `GuideInventoryOverlay.highlightTravelItem` and the jewellery-box handling — so a destination
> that changed after the item was chosen leaves the outer highlight pointing at furniture and the
> inner one with no answer.
>
> **The owner's read, and it is the right shape:** do not highlight a travel item until the
> destination is known *and latched*. An item highlighted against a destination that can still
> move is an instruction that can be wrong by the time it is followed — and the POH case shows
> the failure is not merely cosmetic, because the sub-menu highlight cannot be derived at all
> once the two disagree.
>
> **The fix, as asked for: one tick of agreement before anything is pointed at.**
> `GuideTracker.destinationSettled` compares this tick's destination with the last, and the
> `TravelHint` is only built when they match. Every travel highlight hangs off that hint — the
> teleport rows, the spell, the nexus, the jewellery sub-menu — so one gate covers all of them,
> including the POH case that could not be derived at all.
>
> A destination that has just changed therefore shows **nothing** for a tick rather than showing
> the old answer or a half-resolved new one. Nothing is lost while it settles: the run still says
> where it is going in words, and the words never went wrong. Cleared in `runEnded()` so the first
> leg of the next run settles from scratch. Nameless legs are unaffected — null agrees with null,
> so the vehicle-from-the-hops case settles immediately as before.
>
> Tests: `NamelessLegStillHintsTest.aDestinationIsNotPointedAtUntilItHasHeldForATick` and
> `aNamelessLegSettlesToo`.
>
> Unverified in the client: finishing a stop and watching the teleport highlight hold blank for a
> beat rather than swapping under the cursor.
>
> Worth checking together with the Gnome Stronghold ordering item: both are the run re-deciding
> something the player is already acting on.

theres a bit of a game stutter every time I fill/empty my seedbox
> RESOLVED. The box states its movement in the chat box **once per seed kind** — "Stored 6 x
> Ranarr seed in your seed box." — so a box holding six kinds arrives as six messages inside one
> tick. `SeedCapture.onChatMessage` turned each into a `moveInSeedBox`, and each of those wrote
> config and fired every change listener. Six config writes and six full sidebar rebuilds, per
> fill.
>
> Both halves were already known to be expensive here, and both have caused a reported stutter
> before. A config write posts `ConfigChanged` **synchronously** into every subscriber in the
> client, so six writes are six passes through every other installed plugin — the same shape as
> the per-patch write burst on region entry that `PatchStateStore.asOneWrite` exists to stop. And
> `store()`'s own note already recorded the other half: *"writing config on each one is what made
> banking feel like it stuttered."*
>
> **Coalesced to the tick rather than batched round a block.** `asOneWrite` takes a `Runnable`
> because the patch scan is one loop with a beginning and an end; a chat burst has neither — the
> messages are separate events and nothing knows how many are coming. So `moveInSeedBox` now holds
> the write, and `SeedCapture.onGameTick` calls `flushSeedBoxWrites()` first, before it relearns
> the pack. One write and one rebuild per tick however many kinds moved. The counts still change
> in memory immediately; only the write and the announcement wait, so nothing that reads the store
> sees a delay.
>
> **A second write path was found and deliberately left alone.** `rememberSeen` writes its own
> `seedsEverSeen` key, once per seed kind the account has never held before. Not batched, because
> it fires once in an account's life per crop and never again — it is not what anybody feels. The
> test names the `seeds` key so it stays about the burst it was written for.
>
> Tests: `SeedBoxDriftTest.aFillOfManyKindsWritesOnce`, `everySeedInTheFillSurvivesTheFlush`,
> `anIdleFlushIsFree`.
>
> Unverified in the client: filling and emptying a box of several kinds and watching for the
> stutter. Fail signature — if it still stutters, the remaining cost is the rebuild rather than
> the write, and `DoogleMapsPlugin.refresh` (a full panel refresh per change) is where to look.

cross-group seed double-count in the bank list
> RESOLVED. Found by reading, while chasing the herb-withdraw report that turned out not to be a
> bug. `RunLoadout.allocate` runs once per planting group and each call seeded its stock map from
> the **full** `seeds.getOwned(seed)`, with no ledger between them. So a seed picked in two groups
> was planned into both: two ranarrs covering two protected patches *and* two ordinary ones, out
> of a stock of two. The protected split makes it reachable in ordinary play, because protected
> herbs and ordinary herbs are two groups over one stock.
>
> Both loops that allocate now spend a shared ledger down as they go — `addSeeds` and
> `addPayments`, each with its own, walking the same groups in the same order over the same stock
> so they cannot disagree about what is being planted. A group whose seed is used up simply
> allocates its next choice, which is the spill a single group has always done.
>
> **Untested, deliberately and worth knowing.** A test needs two real groups over one stock, and
> `RunLoadoutTest` mocks `PlantingGroups`, so the fixture produces exactly one group and the
> obvious test passes against the broken code. One was written, checked against a neutralised
> build, found to assert nothing, and removed rather than left as a false guarantee. Whoever next
> needs to touch this should build the two-group fixture first — stubbing `groups.groupFor` per
> patch is not enough on its own; the seed rows came out empty and the reason was not chased down.
>
> Open question the fix does not answer: **which group gets first claim**. It is currently
> whichever `actionableByGroup` yields first. Arguably the contract group should, since its seed
> is the one whose absence costs a reward.

Harvest XP mismatch warnings for Strawberry
> NOT A BUG in CropXp, and the warning is doing its job. Diagnosed from `harvests.csv`, which the
> plugin writes for exactly this.
>
> Most strawberry rows agree within about one XP — 561.7/561.0, 740.1/739.0, 591.4/591.0,
> 710.3/709.0, 532.0/531.0 — so the 29.0 harvest constant is right, and the sub-2.0 differences
> are rounding that `xpMismatchTolerance` already absorbs without warning.
>
> Three rows do not, and they have a shape:
>
>     24 picks  predicted  740.1  actual  803.0   Falador North West   +62.9
>     41 picks  predicted 1245.4  actual 1308.0   Ardougne North       +62.6
>     42 picks  predicted 1248.4  actual 1274.0   Kourend South West   +25.6 (completed=false)
>
> **The cause, from the log rather than from arithmetic — the owner's read, and it is right.** A
> first guess of "the twin allotment's picks leak in" was wrong. The free-slot trace across the
> Falador harvest settles it:
>
>     10:28:31  16 free   picking starts
>       ...     one slot per pick, steadily
>     10:28:54   3 free   pack nearly full
>     10:29:15  17 free   <- noted at the leprechaun and came back
>     10:29:18  16 free   picking resumes
>       ...
>     10:29:34   7 free   record written, 24 picks
>
> A twenty-one second gap, and the slots jump 3 -> 17. **The harvest record stays open across the
> leprechaun trip and collects whatever experience arrives in the meantime.** That also explains
> why the excess is not a clean multiple of one pick's 29.725: it is whatever landed inside an
> interrupted window, not a tidy number of strawberries.
>
> It fits the rest of the data. The rows that match — 17, 18, 19, 23 picks — all fit in one pack,
> so no trip, no gap, no leak. The two big outliers are 24 and 41 picks, both more than a pack.
>
> One detail worth carrying into the fix: at 10:29:15 the step list had **three** Falador patches
> queued for HARVEST at once (4771, 4774, 4773). So during the gap this patch's record was open
> while the guide was pointing at others, which is likely how the foreign experience gets in
> rather than the elapsed time by itself.
>
> Not a data bug, and `CropXp` is correct — see the matching rows. It costs the validation data and
> the stats tab rather than anything said during a run, which is why it is logged rather than fixed
> in the same pass: the attribution window is delicate.
>
> Where to start: `HarvestRecord.addXp` and whatever decides which record an XP drop belongs to.
> The control group is already in the CSV — every one-pack harvest lands within a point.
>
> **Ruled out, so it is not re-suspected:** the Farmer's outfit. `FarmingOutfit` models it per
> piece (0.4/0.8/0.6/0.2) with the +0.5% set bonus for 2.5%, explicitly *not* the flat 0.4% most
> skilling outfits use, and the predicted figures carry it — 24 picks is
> (24 x 29.0 + 26.0) x 1.025 = 740.05, the exact number in the CSV. The five rows that agree with
> the game within one XP are the proof it is right, since a half-percent error would put every one
> of them outside the tolerance.

jewelery box highlighting not working for farmers guild in POH, sent there via teleport to house tablet
> RESOLVED. Diagnosed from the log rather than by reading, and the log had both halves of the
> match sitting there unmatched:
>
>     hops: [Home Portal, Teleport to House, Teleport Menu Fancy Jewellery Box - J: Farming Guild]
>     furniture seen: [... Portal Nexus#33410 ... Ornate Jewellery Box#29156]
>
> The box was in the room, the hop named it, and `furnitureServesHop` agrees the two go together —
> so the name matching was never the problem. The veto was the **spirit tree guard**.
>
> `HouseTeleports.spiritTreePatchFor` finds its place by looking for a known spirit-tree name
> **anywhere in the hop string**, and a hop names its vehicle as well as its destination. So
> "Fancy Jewellery Box - J: Farming Guild" contains "farming guild", resolved to the guild's
> spirit tree patch, and — that patch being empty — answered "no tree grown". `GuideOverlay` ANDed
> that answer onto **every** piece of furniture rather than only onto spirit trees, so the
> jewellery box went dark with the route pointing straight at it.
>
> It bites at exactly the destinations that are also spirit-tree stops — Farming Guild, Hosidius,
> Etceteria, Brimhaven, Port Sarim — and nowhere else, which is why it looked like a jewellery box
> problem rather than a guard problem.
>
> Fixed by scoping the guard: `HouseTeleports.isSpiritTree(name)` gates it, in both places the
> overlay applies it (the hop loop and the destination fallback). The guard itself is unchanged and
> still does its original job — the guild's tree is not outlined for a hop through it while its
> patch sits at weeds.
>
> Tests: `SpiritTreeDestinationTest.aJewelleryBoxIsNotSubjectToTheGrownTreeGuard`, which asserts
> the hop *does* still resolve to a patch (that trap is unchanged) while the box is not a spirit
> tree and does serve the hop; and `aSpiritTreeIsStillRecognisedAsOne`.
>
> Note this report predates the current build — it is from before the day's fixes, so nothing
> landed today is implicated.
>
> The looser half is left alone and worth knowing about: matching a spirit-tree place name against
> the whole hop string is what made a jewellery box row look like a spirit tree question in the
> first place. Scoping the guard makes that harmless rather than correct.

the run presets broke the profile selector, and it should remember the previously set profile
> RESOLVED, both. The first was a regression in the presets themselves.
>
> **The selector was unusable because it was being rebuilt several times a second.**
> `refreshPresets` is called from `RunPanel.refresh`, which runs on every tick and every state
> change, and it unconditionally cleared the combo box, re-added every item and re-set the
> selection. An editable `JComboBox` does not survive that: the text being typed into its editor
> was wiped before Save could read it, and an open popup closed under the mouse before anything
> could be picked from it. So neither making a preset nor choosing one worked, which is the whole
> feature.
>
> Now nothing happens unless it has to. The item list is rebuilt only when the names actually
> change, the selection is set only when it differs from what is shown, and **neither happens while
> the popup is open or the editor has focus** — both are a decision in progress, and the ticks the
> label would be re-derived from are the ones the player is in the middle of changing.
>
> **Remembering the preset** is `RunPresetStore.lastUsed`, set on apply and on save, stored in the
> same blob under a reserved key so one write persists both and they cannot load out of step.
> Forgotten when that preset is deleted, and refused when it names nothing stored.
>
> It is a **label, not an authority**, and that distinction is the whole design: the ticks already
> persist through `RunTypeStore`, so a restart brings back the right run either way. What this
> fixes is the control re-deriving its name from whichever saved preset happened to match first,
> so two presets covering the same lines made the name flip between sessions for no reason the
> player did. It is used only while it still matches the ticks; edit them and the box says
> `(unsaved)` exactly as before.
>
> Tests: `RunPresetStoreTest.theLastUsedPresetIsRemembered`, `anUnknownNameIsNotRemembered`,
> `deletingThePresetForgetsThatItWasTheLastUsed`.
>
> **Untested: the combo-thrashing fix itself.** It is Swing timing — typing into an editor while a
> background refresh fires — and the existing panel tests construct the panel without driving
> repeated refreshes against a focused editor. The guard is three conditions and each is readable,
> but it is the sort of thing that only really answers to being used.

navigated to the bank at the start of a run, and every single patch in the farming guild highlighted
> RESOLVED for the highlight. **The bank leg was correct**, and the run's own diagnostic says so:
>
>     still to collect: [BANK, SEED_VAULT] ... -> starting at a bank
>     Toadflax (bank 341), Snapdragon (bank 65), Snape grass (vault 7), Belladonna (vault 166)
>
> Four picked seeds sat in the bank or the vault and none in the pack, so the trip was earned.
>
> **The patch highlighting was pre-existing** — `highlightPatchesAhead` outlines every patch of the
> stop being travelled to, by design, and nothing in this batch touched the status path that feeds
> it. What made it look new was that it became the *only* thing lit.
>
> That part was mine, from the travel-hint gate added earlier the same day, and it was wrong twice:
>
> 1. **Short-circuited.** `destinationSettled` sat third in an `&&`, so on any tick with a step at
>    the stop it never ran and never recorded. "The previous tick's destination" could be minutes
>    old, and the comparison answered nonsense in both directions. It now records every tick,
>    whatever else is true.
> 2. **Applied on the supply leg**, where it cannot ever pass. There the destination is a bank,
>    re-derived from the router's reply every tick because `commitDestination` deliberately ignores
>    everything while `atBankLeg` — so it never agreed with itself two ticks running and the hint
>    was held off for the whole leg. No teleport marked, no route object marked, and the
>    patches-ahead outline left as the only thing on screen. The gate is now skipped on the bank
>    leg, which is not where the bug it exists for happens: that one is a stop *completing*.
>
> Test: `NamelessLegStillHintsTest.theGateComparesAgainstTheTickBeforeAndNotWheneverItLastRan`.
>
> Still open, and worth deciding separately: whether the patches-ahead outline should show at all
> while the leg is going to a **bank** rather than to those patches. It is honest — they are where
> the run goes next — but "you are being sent to the bank" and "these ten patches are lit" is a
> confusing pair, and it is what made this look like a bug.

compost bins: inventory space is not reserved before a bin step
> OPEN, watching. Raised 2026-08-18 from play at the Farming Guild — prompted to bring watermelons
> for the big bin before applying ash and emptying it, which cannot be carried together.
>
> **What is actually wrong is the loadout, not the step order.** `CompostBinPlan` already emits
> ash -> empty -> fill. `RunLoadout.addCompostBinSupplies` packs all three at once, because
> `binWork` deliberately counts a ready bin as also fillable — "A ready bin is also a fillable one,
> and missing that ended runs early". That decision predates `divertForSupplies` being able to make
> a second trip, so it may simply be stale.
>
> **The arithmetic, against a 28 slot pack:**
>
>     Guild big bin   empty 30 + ash 1 + fill 30  = 61
>     Normal bin      empty 15 + ash 1 + fill 15  = 31
>
> Neither fits. The big bin is worse but the small ones are over too — the owner's correction, and
> it is right. They only look narrower because their fill is harvest-fed and never banked for, so
> the squeeze happens at the bin instead of at the bank.
>
> **What rescues the small bins when they are rescued:** the leprechaun stands beside every one, and
> `DEPOSIT_COMPOST` hands the buckets over, freeing the fifteen back. Empty -> deposit -> fill fits.
> The failure mode is emptying being offered while the pack cannot hold what comes out.
>
> **A bottomless compost bucket changes everything and this account does not appear to have one** —
> no mention in any log. It empties a whole bin into one slot, which takes the normal bin to 17 and
> the big one to 32. Worth knowing before reading any test result: someone who owns one will not
> reproduce this.
>
> **Watch for, while testing:** a partial empty (fewer buckets out than the bin held) followed by the
> empty step re-offering itself; or a fill step naming more items than there are free slots. Either
> is this. A clean empty -> deposit -> fill at a small bin is the case working.
>
> Options were sketched with the owner: (1) stop banking a fill for a bin that is still full, (2) a
> "leave N slots free" line on the loadout, (3) the per-leg loadout that `docs/compost-redesign-spec.md`
> already identifies as the core change. Nothing decided; (3) is the real answer and (1) is the cheap
> one that makes the current behaviour possible rather than impossible.

you don't "hand in" your harvest for a contract - you can note it at the lep and keep it, you just talk to Jane
> RESOLVED, and it was presentation only — the logic never required the produce.
>
> `owesYouSomething` asks the **patch** whether it still owes a health check or a pick, not the pack
> whether it holds the crop, so a noted harvest was never going to block the claim. What was wrong
> was everything the player could see:
>
> - the action read "Hand in the contract";
> - the step read "Hand your cactus to Guildmaster Jane for the contract reward";
> - and the step carried the produce's **item id**, which outlined the crop in the pack.
>
> Together those say the crop is the price of the reward. It is not: the contract completes when
> the crop is harvested — the chat line `ContractCapture` already watches — and Jane simply pays
> out afterwards. Now "Claim the contract reward" and "Talk to Guildmaster Jane to claim the
> contract reward.", with no item, so nothing of the player's is outlined for something they do
> not give up. Jane and the dialogue rows are still highlighted; those are the clicks.
>
> Test: `ContractHandInOrderTest.theRewardIsClaimedWithoutGivingTheProduceUp`. The item assertion is
> the load-bearing half — wording can be reworded by anyone, but an outlined crop beside a hand-in
> step is the part that actively misleads.

infobox says patches are ready that were just harvested - all fruit trees, Etceteria and Rimmington berry patches
> RESOLVED. Reported on a harvest-only run, and every patch named is a fruit tree or a bush — both
> regrowing families, which is the whole of it.
>
> `ReadyInfoBox` counted `PatchProjection.isReady()`, which answers **"has it finished growing"**.
> For a crop that regrows that stays true forever once it has: picking a fruit tree does not
> un-grow it. So a stripped bush and a bare fruit tree read as ready for the rest of their lives,
> and a counter whose whole job is to say "a farm run is worth starting" was saying yes to a farm
> of bare trees.
>
> `hasProduceToPick` is the question that was wanted and it already existed —
> `RunPlanner.isActionable` has asked it of exactly this family since the picked-clean bushes were
> fixed, and its **harvest-only branch is word for word** what the infobox now uses. So this was
> the planner and the counter disagreeing about the same patch, with the planner right.
>
> Scoped to regrowing crops rather than replacing the readiness test: a crop that does not regrow
> cannot be in this state, since picking a herb patch leaves it empty and empty is already skipped.
> A tree waiting to be chopped and a stump waiting for a spade keep their place, and a health check
> counts because it is the click the patch is waiting for.
>
> Pre-existing rather than introduced by the day's work — nothing in this batch touched `isReady`,
> the infobox, or bushes. Worth noting it would have been reported sooner or later regardless; a
> harvest-only run is just where it is most obviously wrong, because there picking is the only
> reason to go at all.
>
> Tests: `ReadyCountTest`, four cases, confirmed to fail against the unfixed code.

changing the run profile should update the ready count too, and the profile select box does not match the UI theme
> RESOLVED, both, and the first was wider than the presets.
>
> **`RunTypeStore` had no change listeners at all.** Every other store the panel writes to
> announces itself — the seed stores, the compost choice, the protection split — and the plugin
> turns that into one refresh of the sidebar and the infobox. This one never did, so changing which
> patch types a run covers updated the boxes and nothing else: the in-game ready count went on
> filtering by the **previous** selection until the twenty-second idle refresh happened along.
>
> Not a preset bug. Ticking a box by hand had the same staleness and nobody noticed, because the
> count moves by one patch type and a stale number looks merely slow. Applying a preset changes six
> or eight lines at once, which is what made it visible. Listeners added, fired from `replace`
> outside the monitor like every other store, registered and deregistered with the rest in the
> plugin.
>
> Test: `RunChoicesTest.changingTheRunLinesTellsWhoeverIsListening`, which also pins that setting
> the *same* lines says nothing — `replace` returns early on an equal set, and a listener firing on
> no change is a refresh loop.
>
> **The styling** was simply not applied: `Controls.styleComboBox` already exists and the compost
> picker uses it. Two things had to be undone afterwards, both because that helper assumes a fixed
> list you can only choose from: it switches focus off, which an editable box cannot have, and it
> styles the renderer rather than the editor — so the typed text arrived light-on-light from the
> look and feel. The editor is now painted to match, caret included, which was otherwise very
> nearly invisible against the sidebar.

teleport crystal: told to withdraw the (5) and the (1) while carrying the (2), and the withdraw list shows outside a run
> RESOLVED, both.
>
> **The charges.** A teleport with charges is several items and the game names each for its count,
> so "Teleport crystal" on the list resolves to every one of them. `addListedTeleports` then made a
> **row per id**, and `carried.has(itemId)` is a different id for every charge — so the (5) and the
> (1) in the bank were each "withdraw", and the (2) in the pack satisfied neither.
>
> Rows are now grouped by the name with its charge stripped, and a group counts as HAVE if **any**
> of its charges is carried, which is the question the player is actually asking. When one is
> carried the row names *that* charge, so it reads as the item in front of them.
>
> **The id set is deliberately left alone** and this is the part worth not undoing later: it still
> holds every charge, because `isOnTeleportList` answers from it and the bank has to light all of
> them. The first attempt collapsed the ids and would have broken that — the existing test caught
> it. Matching every charge is what a wildcard is *for*; showing eight rows was never the point,
> and `RunLoadoutTest.aWildcardMatchesEveryChargeOfAnItem` now says so in as many words rather than
> asserting the old row-per-charge behaviour.
>
> **The withdraw list outside a run.** `ReadyInfoBox.toWithdraw` asked the loadout, which answers
> from the ticked types whether or not anything is under way — so it was a shopping list for a trip
> that did not exist. Gated on `planner.isActive()`. The bank leg keeps it, which is the case it
> was built for: `isActive()` is true from the moment Start run is pressed and the supply leg is
> the first thing that happens.
>
> **And the list is a convenience, not a requirement** — the owner's framing, and the loadout
> already agreed with it: `TELEPORT` is deliberately absent from `CANNOT_PROCEED_WITHOUT`, so a
> missing teleport has never held the supply leg and the run has always been happy to walk. What
> still said otherwise was the infobox, which prints its rows under "To withdraw:" — read as a set
> of orders, on the one surface glanced at mid-game. Teleports no longer appear there. The
> sidebar's bank list still carries them with their need, which is where the detail belongs.
>
> Tests: `aWildcardMatchesEveryChargeOfAnItem` (rewritten), `aCarriedChargeSatisfiesTheWholeTeleport`.

at the start of my run in the farming guild, every single patch is highlighted
> RESOLVED. The supply leg was being drawn as a travel leg.
>
> Patches ahead are outlined on the way in so the last stretch of a journey is not walked with the
> whole farm dark. That highlight lives in the overlay's "no current step" branch — and the supply
> leg has no steps either (`computeStepsHere` returns nothing while `isAtBankLeg`), so banking was
> drawn as arriving. Invisible everywhere the bank sits a region away from the patches, which is
> everywhere but one: **the Farming Guild's bank chest is inside region 4922**, the patch region
> itself, so `destinationStop` matched the chest's region to the guild stop and every patch in the
> guild lit up while the run had not started on them.
>
> `GuideStatus.patchesAhead` is now empty on the supply leg and empty once you have arrived —
> which is what its own javadoc always claimed. Gated in `GuideTracker.patchesAhead` rather than in
> the overlay, for the reason `getSupplySources` records: the planner knows which leg it is on, and
> an overlay that re-decides that can disagree with the route being drawn.
>
> The travel-leg behaviour is deliberately untouched — walking into a farm still lights what is
> waiting there. Tests: `GuildBankLegDoesNotLightPatchesTest`.

are we checking this literally every tick? [the "Ready bin ..." debug line, twice a second]
> RESOLVED — the check is per tick by design; the log line was not.
>
> The chain: `DoogleMapsPlugin.onGameTick` asks `anythingLeftToWithdraw` every tick while a run is
> active, which builds the loadout, which asks `RunPlanner.binWork`, which decodes every bin. That
> rebuild is deliberate and already shared — `RunLoadout.forRun` caches one build per tick because
> four callers wanted the list every tick at the bank — and the decode itself is arithmetic over a
> snapshot, so the cost was never the check. It was the unconditional `log.debug` inside it: one
> line per ready bin per tick, on the client thread, for the length of a run. In the live log
> **14,980 of ~15,020 plugin lines were this one sentence**, which is the whole session's forensics
> buried under it.
>
> It now prints only when a bin's decode changes (`RunPlanner.logBinDecode`, keyed on
> varbit/state/stage/buckets/ash), and the entry is dropped when the bin stops being ready so
> coming back to ready prints again. A repeat carried nothing the first line did not — what the
> line was added to catch is the decode being *wrong*, and a decode that changes still prints.
> Guarded on `isDebugEnabled`, and the map is only ever touched under `binWork`'s monitor.

getting prompted to take watermelons out for the big bin before emptying it first; can't carry
them when the emptying needs the pack for buckets, so I have to bank them again at the guild
> RESOLVED (the sequencing bug), and folded into the compost redesign spec as review §9.
> RunPlanner.count() counted a ready bin's fill "at the same stop, on the same visit", so the
> supply leg withdrew fill for a bin that cannot take a single item until its compost is out —
> and the fill occupied the very slots the emptying batches against. A ready bank-fed bin now
> counts as fillable (pick-a-fill warning and routing unchanged) but contributes no fill items
> until its varbit reads EMPTY; then the row goes WITHDRAW and divertForSupplies makes the
> collection the run's next leg — at the guild that is a walk to the in-region chest.
> The old dead end the up-front fill was fixing (all-ready run packs no produce, run ends with
> the bin open) stays fixed by divertForSupplies, which postdates it. The one coarseness left:
> on a multi-stop run the divert fires at run end, so the guild fill is a return trip rather
> than immediately post-empty — spec §9.2 records the in-region mid-stop re-entry (the contract
> path's canBankHere shape) as the redesign's answer. Tests:
> aReadyBigBinsFillWaitsForTheEmptying, anEmptiedBigBinAsksForItsFill, and
> aReadyBinBanksNoFillUntilItIsEmptied (rewritten — it pinned the old behaviour, and now
> carries both dead ends' stories).

the swapping at the lep never worked for this anyways [withdraw-amount swap]
> RESOLVED, second attempt. The first fix taught itemIdOf to ask the widget when the entry
> answered nothing — and pinned it with a mocked widget that knew the id, which is how it held
> in the suite and never in play: at his store NEITHER the entry nor the widget carries an item
> id, so identification failed and the swap stood down silently at the one interface the
> feature was built for. What every item op does carry is the item's printed NAME in the entry
> target ("<col=ff9040>Empty bucket</col>") — the text the menu shows — and the loadout's rows
> carry the same names. The swap now sizes by name when there is no id to size by
> (GuideMenuSwap.itemNameOf -> RunLoadout.stillWantedNow(String)), ids still preferred where
> they exist. And the silence is fixed alongside the failure: logStandDown writes one debug
> line naming the guard that stood the swap down, once per distinct reason, so the next "it
> doesn't work at X" is answerable from client.log instead of by guessing.
>
> Fixed in the same pass, from review round d (docs/code-review-2026-08d.md):
> - Deposit menus are never sized: the scan matched by amount alone, and the bank's inventory
>   side offers Deposit-1/-5/-10 on every hovered item — so the swap promoted Deposit-10 on the
>   very buckets the run wanted withdrawn. Verb whitelist (Withdraw/Remove) closes it.
> - An unidentifiable entry stands the whole swap down instead of being absorbed.
> - The infobox withdraw list rides GuideStatus.toWithdraw now, built once a tick on the client
>   thread, instead of ReadyInfoBox walking the loadout and planner from the EDT on every store
>   change; ReadyInfoBox no longer depends on RunLoadout at all.
> Tests: aDepositMenuIsNeverSized, anUnidentifiableEntryStandsTheWholeSwapDown,
> hisStoreIsSizedByNameWhenNothingCarriesTheItemId, bucketsFromHisStoreAreSizedByNameToo.

leprechaun empty buckets menu swapping logic still not working, its "Remove-1", "Remove-5", "Remove-All"
> RESOLVED (third attempt), and this time the failure mode is cornered from the log rather than
> guessed at. The live session WAS running the fixed build (the change-only bin logging proves
> it) with debug on — and client.log carried not one stand-down line. The only silent paths were
> "no candidates" and "wanted came back zero", and the way wanted comes back zero with a bucket
> row plainly outstanding is an item id that is POSITIVE BUT WRONG: an interface handing back
> junk instead of -1 sails past the "no id" test, sizes a row that does not exist, and stands
> the swap down with nothing logged — indistinguishable from working as intended.
> Two changes:
> - The id is only trusted as far as it finds a row: when it sizes to nothing and the entry
>   carries a printed name, the name is asked too. Safe by construction — a genuine id and the
>   name find the SAME row, so a real "nothing wanted" stays zero by either path; only a junk id
>   leaves the name to find the right row. Test: aJunkItemIdFallsBackToTheName.
> - The decision now logs at debug, once per distinct answer (GuideMenuSwap.logDecision): which
>   item was seen (id and name), what the run said it wanted, what was offered and what was
>   chosen. Two play sessions have been spent guessing at this feature's silence; if there is a
>   fourth failure mode, client.log will name it.

somewhere the math on the number of free spaces got off by 1, being asked to withdraw 1 more bucket than I can fit
> RESOLVED. The maths was right and the bucket fetch's floor was wrong.
> CompostBinPlan.addBucketFetch asked for max(1, min(remaining - held, freeSlots)) — and the
> min already answers "1 bucket" for one free slot, so the floor only ever changed the answer
> at ZERO free slots, where it asked for a bucket with nowhere to put it. The session log shows
> exactly that state ("0 free slots" at 19:55:20). Reachable because the zero-slot deposit
> escape only fires when the pack holds compost to hand over; full of harvest instead, the
> fetch fell through to its floor. Now: at zero free slots with nothing to deposit, the fetch
> raises no step at all — the stop's other steps (the note step for a full harvest, the fill
> step for an open bin) are what make room, and the fetch comes back the tick a slot does.
> Test: aFullPackOfSomethingElseIsNotAskedForABucket; roomForOneBucketFetchesOne still pins the
> one-slot case the floor claimed to serve.

remove menu still not swapping [round four - and the decision log caught it]
> RESOLVED. The 20:09 session log answered on the first grep, which is what the logging was for:
>   Withdraw-amount swap: item id=-1 name='Bucket' wanted=0 offered=[1, 5] -> nothing to promote
> The name fallback was working; the name COMPARISON was wrong. A menu prints the GAME's name
> for an item, and the game calls item 1925 plain "Bucket" - while the loadout row is labelled
> "Empty bucket" so the bank list reads unambiguously. Matching menu names against row labels
> compared the plugin's wording with the game's, and they part company at exactly this row.
> RunLoadout.rowIsNamed now answers for both: the row's label, and the game's own name for the
> row's item id (ItemNames where recorded, the client's definition otherwise - the name path's
> one caller is PostMenuSort, which is the client thread). The store's Compost/Supercompost/
> Ultracompost slots also showed in the log sizing to 0 - that is correct and unchanged, the
> compost tier rows are deliberately unsized.
> Test: theGamesOwnNameForTheItemSizesTheRowToo.

noting potato cactus step didn't highlight the potato cactus or the lep
> RESOLVED. The session log shows why nothing lit: "4922.7909=HARVEST; 4922.4775=NOTE_AT_LEPRECHAUN"
> - the note LISTED but never CURRENT, and the overlays light the current step only. The note is
> raised inside the plan of the patch whose produce fills the pack, so it led only when THAT patch
> was the working patch; here the working patch was another harvest entirely - one the pack, at
> zero free slots, could not receive a single item of. The order said "harvest first, then note";
> the game only permits the reverse, and the player followed the note by hand, unlit.
> GuideTracker.noteLeadsWhenThePackIsFull: when nothing fits and the final list leads with a
> HARVEST, the one surviving note (the collapse keeps exactly one) moves to the front - same
> freeSlots <= 0 gate that raises the note in GuidePlan. A leading non-harvest step is left
> alone: paying, planting and bin work need no free slot. Tests: FullPackNoteOrderTest.

never got routed back to the bank for watermelons to the big bin / just filled it and now it's
sending me to ardougne instead of to get the rest of my melons
> RESOLVED - the coarseness compost-redesign-spec §9.2 recorded as "a scope call" got called, by
> two reports in one session. Mid-run supplies (the emptied bin's fill is the case that exists)
> had exactly one collector: divertForSupplies, which by design fires only when the run would
> otherwise END. So the run walked away from the guild's own chest twice - after the emptying,
> and again between fill loads - with the melons fourteen tiles from the bin. The log shows it
> plainly: "Stop complete at Farming Guild" at 20:21:36, FILL_BIN only at 20:21:55 after the
> player collected unprompted, then Ardougne at 20:24 with the remainder still banked.
> New RunPlanner.reviewNearbySupplies, once a tick from the plugin: a stop that is COMPLETE, with
> supplies outstanding and a supply point IN ITS OWN REGION, re-enters the bank leg before the
> run commits to the next stop - the contract path's canBankHere shape, generalised. The gates
> are the point: complete-here is the anti-yank rule (the fill row opens mid-stop, stood at the
> bin, with harvests left - nothing may be interrupted), in-region is start()'s own "standing on
> work beats going shopping is about TRAVEL" line, and a waived leg still wins. Per tick rather
> than on the completion edge because at the edge the pack is often still full and the fill row
> asks for nothing until the noting and depositing free the slots.
> Everywhere else, the end-of-run divert keeps the job unchanged. Tests:
> aFinishedStopWithASupplyPointHereCollectsBeforeMovingOn (walks the whole lifecycle),
> aFinishedStopWithNothingOutstandingIsNotSentToTheBank.

when withdraw count is equal or more than free inv space, just use the "all" option
> DONE. For an UNSTACKABLE item, Withdraw-All takes what fits and stops - the pack is the cap -
> so once the run wants at least a whole pack, All is min(freeSlots, bank) <= wanted and the
> no-overshoot property survives intact: thirty melons becomes one click instead of three that
> all meant "fill the pack". RunLoadout.fillsThePack owns the guard rails, and they are the
> point:
> - Fills and buckets only. A payment is counted NOTED (that is how anyone carries thirty), and
>   a bank in noted-withdrawal mode hands All of a noted item over as one stack - the overshoot
>   again by another door.
> - Never volcanic ash: it stacks, so All takes the bank's whole pile into one slot and
>   "wanted vs free slots" measures nothing. It is the one stackable these categories hold, so
>   it is excluded by id - deterministic, and honest about being a list of one.
> The ladder is untouched everywhere else, including everything below a packful. Works at the
> lep by name like the rest of the swap ("Remove-All"). Tests: aPackFillingWithdrawalTakesAll,
> hisStoreTakesAllByName, wantingAWholePackAnswersAll, wantingLessThanAPackKeepsTheLadder,
> ashNeverAnswersAllHoweverMuchIsWanted.

and swap to examine when we're done withdrawing
> DONE. With the count satisfied, the game's own default - Withdraw-1, or whatever the quantity
> toggle says - is the one remaining way to overshoot, sitting under the very click the player
> has been spamming; Examine is the option that does nothing, which is exactly what is wanted of
> the next stray click. The safety line is RunLoadout.doneWithdrawing: "done" means TRACKED AND
> NOW COVERED, never merely "answers zero" - stillWantedNow answers zero for a satisfied row,
> for a seed the swap must never size, and for an item that was never the run's business, and
> Examine-swapping either of the last two would break ordinary banking. Covered either way it
> shows: the cached row reads WITHDRAW with a live carried count for the rest of the tick after
> the last withdrawal, and HAVE from the next build - both are done. Works at the lep by name
> ("Examine" on his store slots), logs "fully collected -> promote Examine" at debug, and the
> deposit side can never see it (no Withdraw/Remove candidates there means the scan exits first).
> Tests: aCollectedCountSwapsToExamine, hisStoreSwapsToExamineByName, doneMeansTrackedAndCovered,
> aSeedIsNeverDoneWithdrawing.
