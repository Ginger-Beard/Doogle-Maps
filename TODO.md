- White checkboxes are still almost black on the hide patches drop down
- Inspect patch seems to update the compost listing, but at least for the farming guild if I inspect the south alotment then the north, the icon gets removed from the south, and vise versa, should keep cached. Also can we get this from time tracking instead so its up to date without clicking inspect?
- Paid protection icon could use some work, maybe a farmer head icon instead? idk
- Hidden patches aren't presisting logout/login
- Hide patches drop down triggers a scroll bar on the right on smaller windows and especially on things like allotments where there are a lot of them, its slow as hell for some reason, and does not match the runelite UI, its blue/white. Isn't this a standard runelite feature, can we get that code from something else?
- Seed listing should always be cached, and persist login, it should include counts as well as inventory the seed vault and seedbox
- Seed listing should be a drop down or check box? Dropdown is probably cleaner
- Geomancy spell should be scraped, ideally I'd like to overlay the same info we have in the side pannel over it (but I want to save this for our last feature), or even be able to re-use it all of the time even off lunar spell book, theres some yeild info that is given through this that I'm not sure if we can get another way? if we can that would be awesome. But at the very least we need to update all of our statuses when it's casted.
- Repeatably harvestable patches should indicate when they're ready to be harvested again, sometimes like if you're farming coconuts for magic tree patch payments, you won't necessarily want to clear the patch, and that timer is different for reharvest

